# 소때잡 — 요구사항 정의서

**버전:** v2.14 | **기준일:** 2026-09-09

> **v2.14 변경 (2026-09-09):** **`FR-02-05` 가맹점명 정규화를 본선 제외로 표시**합니다 (01 v2.30 **E-106**). server에 `merchantNormalized`를 읽는 코드가 없어 구현해도 데모에서 보이는 차이가 없습니다. 항목 수는 그대로(FR 78건)이고 우선순위도 P1 그대로입니다 — 제외는 본선 범위이지 요구사항 삭제가 아닙니다.
>
> **v2.13 변경 (2026-09-09):** **`FR-11-05` 신설 — 채팅 소비 분석 채널의 자유 질문** (P2, 01 v2.29 **E-104**). AI `ANALYSIS` 작업을 `POST /chat/analysis`(05 #28)로 부릅니다. **FR 77 → 78건**입니다(P2 +1 · NFR 9 그대로).
>
> **v2.12 변경 (2026-09-08):** NFR-09에 AI 자연어 응답의 해요체, 한두 문장·한 문단, 반말·격식체·이모지 금지, 지킬 소비 우선 원칙을 추가했습니다 (E-90). **FR 77건 · NFR 9건은 그대로입니다.**
>
> **v2.11 변경 (2026-09-08):** **`FR-10-04` 신설 — Web Push 발송** (P1). **FR 76 → 77건**입니다(P1 +1 · NFR 9 그대로).
> 바로 아래 v2.10에서 "전달 경로라 FR을 만들지 않는다"고 적었던 판단을 뒤집습니다 — 발송은 server가 구현하고
> 구독은 client가 하는 **별도 작업**이라, FR 행이 없으면 담당과 진행 상태를 어디에도 적을 수 없습니다.
> FR-10 절 제목과 담당도 같이 고쳤습니다(인앱 알림 → 알림 · 인앱 · Web Push).
> `요구사항 정의서.xlsx`를 **v2.11 기준으로 다시 생성**했습니다 (FR 77 · NFR 9).
>
> **v2.10 변경 (2026-09-08):** **앱 외부 푸시를 범위 내로 옮겼습니다** (01 E-68). §0 범위표에서 "푸시 알림(앱 외부)"을 범위 외에서 빼고 **Web Push**를 범위 내에 적었으며, FR-10 아래의 "앱 외부 푸시 알림은 범위 외입니다"를 지웠습니다. **FR·NFR 건수는 그대로입니다(FR 76 · NFR 9)** — Web Push는 새 요구사항이 아니라 FR-10-01·03이 사용자에게 닿는 **전달 경로**입니다.
>
> **v2.9 변경 (2026-09-08):** NFR-04의 LLM 타임아웃 잠정값을 **8초에서 6초**로 조정했습니다 (E-88). 재시도 1회를 포함한 최악 12초를 Spring `/chat` 15초 예산 안에 맞추기 위한 값이며, 실제 회고 추출 프롬프트로 재확인해야 합니다.

> **v2.7 변경 (2026-09-07):** FR 변경 없음. FR-12 아래 각주만 갱신했습니다 — 임베딩 모델이 `text-embedding-3-small`(1536차원)로 확정되고 pgvector 확장이 설치되었습니다 (01 E-85 · E-86). 문서 소스 선정은 여전히 미정입니다.
>
> **v1.9 변경 (2026-09-03):** 시간대 4구간(E-50) — FR-02-04 / 회고 진입 경로 3종(E-48) — FR-03-03 문구 한정 · **FR-03-08·09 신설** / 후보 제외는 상태 미저장(E-49) — FR-03-06 비고. **FR 74 → 76건** (P1 +1 · P2 +1). `요구사항 정의서.xlsx`는 이 문서에서 생성한 산출물이며 v1.9와 같습니다 (FR 76 · NFR 9).
>
> **v1.7 변경 (2026-09-03):** FR-01-04 임계값 → 민감도 프리셋 3종(E-46) / 온보딩 4단계(E-45). 그 외 FR/NFR 변경 없음.
>
> **v1.4~v1.6 변경 없음** (2026-09-02 밤 확인). 그 판들은 스택·레포명·AI 레포 조치(01 §0-23~0-25)만 다뤘고 FR/NFR은 그대로입니다.

> 값의 출처는 `01_결정로그.md`입니다. 충돌 시 결정 로그가 우선합니다.
> `P0` = 없으면 데모 불가 / `P1` = 완성도 / `P2` = 여유 시
>
> **v1.2 변경:** FR-11 소비 분석 신설(P1) / FR-01-01 SNS 로그인 확장 / NFR-08 PWA 신설 /
> FR-07-04 처방 문구 5종으로 정정 / **판정 2종 + 보류 상태 분리(E-11)** / **PROTECT CTA(E-12)** / 시트 표기 오류 6건 정정
>
> **v1.3 변경 (레포 우선):** 규칙 엔진 = Spring(E-18) / FR-04-07·FR-05-01 = AI 후보 + 사용자 확인(E-20) / **FR-12 금융 지식 Q&A 신설(P2, E-22)** /
> 만족도 enum `HIGH/LOW/UNKNOWN`(E-23) / NFR-01 범위·증명 테스트 위치(E-29) / 담당 열 갱신(정민규 → Spring 엔진, 오진호 → AI 레포)

---

## 0. 범위

| 항목 | 내용 |
|---|---|
| MVP 완주 기준 | 본선 30시간 내 핵심 흐름 1회 완주 |
| 핵심 흐름 | 목표 설정 → 거래내역 업로드 → 회고 후보 선별 → 회고 → 만족도 보정 → 만족도 지도 → 행동 조정안 → 다음 달 변화 확인 |
| 범위 내 (v1.3 추가) | **금융 지식 Q&A** — 저축·정책 상품 조건 설명 (FR-12, P2). 투자 상품은 여전히 범위 외 |
| 범위 내 (v2.10 추가) | **Web Push — 앱 외부 알림** (01 E-68). FR-10 알림이 앱을 열지 않아도 닿는 경로다. 종전 "범위 외"를 뒤집은 것이다 |
| **범위 외** | 오픈뱅킹 연동 / 실시간 거래 수집 / 투자 상품 추천 / 다중 사용자 |

> 알림은 **범위 내(P1)** 입니다 — FR-10. **v2.10부터 앱 외부 푸시(Web Push)도 범위 내입니다** (E-68) —
> 앱을 열어야만 알림을 본다면 "그 결제 시각이 다시 올 때 묻는다"(E-71)가 성립하지 않기 때문입니다.
> VAPID 키가 없으면 **푸시만 꺼지고 인앱 알림은 그대로** 동작합니다.
> 클라이언트는 **PWA**입니다 — NFR-08 (v1.2).

---

## FR-01. 사용자 · 목표 — 담당: 고현석(API) · 석정한(화면)

| ID | 요구사항 | 우선 | 비고 |
|---|---|---|---|
| FR-01-01 | 로그인한다 — **SNS 간편 로그인** 지원, 데모는 단일 계정 폴백 허용 | P0 | v1.2 확장 |
| FR-01-02 | 목표 자금(비상금·독립·여행 등)을 등록·수정·삭제한다 | P0 | |
| FR-01-03 | **월 예산**을 입력한다 — 지출 부담 분모 | P0 | |
| FR-01-04 | 이상치 탐지 **민감도를 프리셋 3종(보수적 · 기본 · 민감하게)** 중 하나로 설정한다. 프리셋별 숫자 임계값은 Spring `rules.sensitivity.*`에 둔다 | P0 | v1.7 — E-46 |
| FR-01-05 | 목표별 기대효과(달성률 변화)를 수치로 확인한다 | P0 | |
| FR-01-06 | 온보딩 값(목표·예산·임계값)을 마이페이지에서 수정한다 | P1 | v1.2 신설 — 피그마 4MP 대응 |

## FR-02. 거래내역 수집 — 담당: 고현석(파서) · 석정한(업로드 화면)

| ID | 요구사항 | 우선 |
|---|---|---|
| FR-02-01 | CSV/XLSX 파일을 업로드한다 | P0 |
| FR-02-02 | 3사(국민·하나·신한) 포맷을 파싱해 거래로 저장한다 (선 정제 후 업로드) | P0 |
| FR-02-03 | 3사 카테고리를 내부 통합 카테고리로 매핑한다 (국민/신한은 별도 매핑) | P0 |
| FR-02-04 | 시간대를 자동 분류한다 — `MORNING` 05~11 / `DAY` 11~17 / `EVENING` 17~22 / `NIGHT` 22~05 (v1.9 — E-50) | P0 |
| FR-02-05 | 가맹점명 표기 차이를 정규화한다 (고유명사 유지 / 편의점은 지점 표기 제거) | P1 | **본선 제외** (v2.14 — E-106). 컬럼 `merchantNormalized`는 남기고 규칙은 후속 |
| FR-02-06 | 파싱 실패 행을 표시하고 무시한다 | P1 |
| FR-02-07 | 중복 업로드 시 동일 거래를 중복 저장하지 않는다 | P1 |

## FR-03. 회고 대상 선별 (규칙 엔진) — 담당: 고현석(선별 쿼리) · 정민규(기준 정의)

| ID | 요구사항 | 우선 |
|---|---|---|
| FR-03-01 | 시간대별 이상치 + 사용자 임계값으로 후보를 검출한다 | P0 |
| FR-03-02 | 결제 후 **D+1** 경과 거래만 후보가 된다 | P0 |
| FR-03-03 | **인앱 알림**은 1일 **1건**만 요청한다 (온보딩 20건 연속 회고는 예외) — 채팅·거래내역 경로는 FR-03-08·09 (v1.9 — E-48) | P0 |
| FR-03-04 | 조건 충족 거래가 없으면 회고를 요청하지 않는다 (빈 상태 안내 — S10) | P0 |
| FR-03-05 | 선별 시 규칙 엔진이 선정 근거(`reasonCode`)를 **결정론적으로** 산출한다 — 자연어 설명은 FR-04-11 | P0 |
| FR-03-06 | 사용자는 제안된 거래를 제외(`skip`)할 수 있다 — **제외 상태는 저장하지 않는다.** 알림 읽음 처리로 갈음한다 (v1.9 — E-49) | P1 |
| FR-03-07 | 사용자는 거래내역에서 회고할 거래를 직접 추가할 수 있다 — **기간 제한 없음** | P2 |
| FR-03-08 | AI 채팅에서 회고에 진입하면 **오늘 포함 최근 3일**의 후보를 모두 보여준다 (v1.9 신설 — E-48) | P1 |
| FR-03-09 | 사용자가 채팅에서 날짜·기간을 지정하면 그 범위의 후보를 보여준다 — 3일 창을 적용하지 않는다 (v1.9 신설 — E-48) | P2 |

> ⚠️ FR-03-05는 **규칙 엔진의 책임**입니다. AI가 선정 이유를 만들어내지 않습니다 (NFR-02).
>
> **선별은 "회고할 자격"이 아니라 "서비스가 먼저 말을 거는 시점과 개수"만 정합니다** (v1.9 — E-48).
> 오래된 거래를 후보에서 빼는 **상한은 없습니다.** FR-03-02의 D+1은 하한이고, FR-03-08의 3일 창은
> 채팅 화면의 기본 노출 범위일 뿐 회고 자격이 아닙니다. 사용자는 FR-03-07·09로 언제든 과거 거래를
> 회고할 수 있습니다.

## FR-04. 대화형 회고 (AI) — 담당: 오진호

| ID | 요구사항 | 우선 |
|---|---|---|
| FR-04-01 | 사용자가 회고 등록 메뉴를 선택하면 회고 대화를 시작한다 | P0 |
| FR-04-02 | AI는 회고 후보 거래의 일시·가맹점·금액을 단서로 제시하며 회고를 시작한다 | P0 |
| FR-04-03 | 사용자는 만족도를 3택으로 입력한다 — 만족했어요 / 별로예요 / 잘 모르겠어요 | P0 |
| FR-04-04 | 사용자는 소비 목적을 선택한다 (선택지 7종 확정 — 아래 표) | P0 |
| FR-04-05 | 사용자는 동행인 라벨을 선택한다 (선택지 6종 확정 — 아래 표) | P0 |
| FR-04-06 | 사용자는 반복 의향(Yes / No)을 선택한다 | P1 |
| FR-04-07 | 자연어 응답에서 AI가 목적·동행인·만족도·반복 의향을 **후보로** 구조화하고, 사용자가 확인한 값만 저장한다 (E-20) | P1 |
| FR-04-08 | 자연어에서 항목을 확정할 수 없으면 불명확한 항목만 추가 질문한다 | P1 |
| FR-04-09 | AI는 확인되지 않은 회고 값을 임의로 추론하지 않고 미확정 상태로 유지한다 | P1 |
| FR-04-10 | 사용자는 대화 초반에 "왜 이 거래를 골랐나요?" 등 선정 이유를 볼 수 있다 | P0 |
| FR-04-11 | AI는 규칙 엔진이 반환한 선정 근거(`reasonCode`)를 바탕으로 이유를 자연어로 설명한다 | P0 |
| FR-04-12 | 회고 중 이탈하면 진행 상태를 저장하고 `PAUSED`로 전환한다 | P2 |
| FR-04-13 | 중단된 회고에 재진입하면 미완료 항목부터 대화를 재개한다 | P2 |
| FR-04-14 | 필수 항목 수집 완료 시 회고를 저장하고 `COMPLETED` 처리한다 | P0 |
| FR-04-15 | LLM 장애 시 사전 정의된 템플릿 질문으로 전환해 회고를 계속 진행한다 | P0 |

**선택지 (확정)**

| 항목 | 선택지 |
|---|---|
| 목적 | 식사 / 만남·사교 / 휴식·취미 / 필수품 / 자기계발 / 충동 / 기타 |
| 동행인 | 혼자 / 친구 / 가족 / 연인 / 동료 / 기타 |

## FR-05. 반복 행동 묶음 — 담당: 정민규(엔진 · Spring) · 오진호(후보 추출 · 명명)

| ID | 요구사항 | 우선 |
|---|---|---|
| FR-05-01 | **AI**가 자연어에서 목적·동행인 **후보**를 표준 태그 7/6종 중 하나 또는 미확정으로 제안하고, **사용자가 확인**한다. 자유 문자열은 저장하지 않는다 — FR-04-08 (E-20) | P1 |
| FR-05-02 | **규칙 엔진(Spring)** 이 카테고리+시간대+목적+동행인 키로 묶는다 (시간대는 식사 카테고리에만) | P0 |
| FR-05-03 | 기준 건수 미달 묶음은 상위 키로 롤업한다 | P1 |
| FR-05-04 | 회고가 쌓이면 롤업된 묶음을 다시 분리한다 | P2 |
| FR-05-05 | **AI**가 묶음에 사용자 친화적 이름을 붙인다 | P1 |

> FR-05-02·03·04는 **Spring 규칙 엔진의 결정론**입니다. FR-05-01은 AI 후보 + 사용자 확인이며, 확인된 표준 태그만 저장합니다 (v1.3 — E-20).
> AI가 있는 곳은 FR-05-01(후보) · FR-05-05(묶음 명명) · FR-04(대화) · FR-11-03(문장화) · FR-12(금융 Q&A)입니다.

## FR-06. 만족도 보정 · 판정 — 담당: 정민규 (Spring 규칙 엔진)

| ID | 요구사항 | 우선 |
|---|---|---|
| FR-06-01 | 만족(`HIGH`) +1 / 별로(`LOW`) −1 / 모름(`UNKNOWN`) 제외로 행동 평균을 산출한다 (E-23) | P0 |
| FR-06-02 | 축소 추정으로 보정 만족도를 계산한다 | P0 |
| FR-06-03 | 사분면 4코드로 분류한다 — `PROTECT`/`KEEP`/`MINOR`/`PRIORITY` (화면 비노출, 정렬용) | P0 |
| FR-06-04 | 판정은 **2종**으로 산출한다 — 지켜요(`SUSTAIN`) / 바꿔볼까요(`ADJUST`). 세로축 부호로 결정 | P0 |
| FR-06-05 | 회고 건수가 임계값 미만이면 `evaluationStatus = PENDING`으로 보류하고 `quadrant`·`verdict`를 `null`로 둔다 | P0 |
| FR-06-06 | **분석 기준월 1개월 묶음 합계 금액 ÷ 월 예산**으로 지출 부담을 산출한다 | P0 |

> FR-06-06은 v1.2 신설입니다 (결정로그 C-1·B-11). 평균 단가가 아니라 **월 합계**입니다.
> FR-06-04·05는 v1.2에서 **판정(2) / 보류 상태(2)** 로 층위를 분리했습니다 (결정로그 E-11).
> `더 알아볼게요`는 판정이 아니라 "아직 판정할 수 없음"이므로 별도 필드입니다.

## FR-07. 만족도 지도 — 담당: 석예림

| ID | 요구사항 | 우선 |
|---|---|---|
| FR-07-01 | 지출 부담(월 합계 ÷ 예산) × 보정 만족도 산점도를 표시한다 | P0 |
| FR-07-02 | 점 색상으로 **판정 2종**을 구분한다 — 지켜요 / 바꿔볼까요 (처방 문구를 색상에 쓰지 않는다) | P0 |
| FR-07-03 | 괄호 안에 회고 건수를 표시한다 | P0 |
| FR-07-04 | 상세 패널에 **처방 문구 5종**을 표시한다 — Great! / Awesome!! / Umm... / Hmm! / 보류 문구 | P1 |
| FR-07-05 | 점 선택 시 묶음 상세(월 합계·평균 단가×횟수·거래 목록)를 표시한다 | P1 |
| FR-07-06 | `evaluationStatus = PENDING` 묶음은 **회색 반투명 점**으로 표시한다 (색상 팔레트 밖의 상태 표현) | P0 |
| FR-07-07 | `PROTECT` 묶음 상세 패널에만 **`예산 확보하기` CTA**를 노출한다 (목표 배분으로 연결) | P1 |

> ⚠️ `Out` 표기는 폐기되었습니다 (결정로그 §7).
> FR-07-06·07은 v1.2 신설입니다 (결정로그 E-11·E-12). **점 색상 팔레트는 2색입니다.**
> `PROTECT`는 조정 제안 목록(FR-08-01)에 넣지 않습니다 — 그 목록은 '줄일 것'의 목록입니다.

## FR-08. 행동 조정안 · 절감액 — 담당: 정민규(계산 · Spring) · 오진호(제안 이유 문장) · 석예림(화면)

| ID | 요구사항 | 우선 |
|---|---|---|
| FR-08-01 | 규칙 엔진이 `PRIORITY` 순으로 줄여볼 행동을 고르고, AI가 그 이유를 문장으로 제안한다 | P0 |
| FR-08-02 | 사용자가 **조정 횟수**를 선택한다 | P0 |
| FR-08-03 | 평균 거래금액 × 조정 횟수로 예상 절감액을 계산한다 | P0 |
| FR-08-04 | 절감액을 목표 자금에 배분하고 달성률 변화를 표시한다 | P0 |
| FR-08-05 | 제안을 거절하거나 횟수를 수정할 수 있다 (제안 발생 시 인앱 알림 — FR-10-03) | P1 |
| FR-08-06 | 전월 대비 실제 감소액을 비교 표시한다 | P0 |
| FR-08-07 | 아쉬운 소비 건수·반복 횟수 변화를 보조 지표로 제공한다 | P1 |

## FR-09. 온보딩 (콜드스타트) — 담당: 오진호(흐름) · 석정한(화면)

| ID | 요구사항 | 우선 |
|---|---|---|
| FR-09-01 | 26.06~07 거래에서 표본(20건 내외)을 추출해 **연속 회고**를 진행한다 | P0 |
| FR-09-02 | 온보딩 완료 시 초기 만족도 지도를 생성하고 `onboardingCompleted` 플래그를 저장한다 | P0 |
| FR-09-03 | 최초 진입 시 플래그로 온보딩/홈을 분기한다 | P0 |

> 온보딩 **4단계** (v1.7 — E-45): 목표 자금 → 월 예산 + 분석 민감도 → 거래내역 업로드 → 표본 회고.
> 26.08은 전월 대비 비교용으로 보존합니다.

## FR-10. 알림 (인앱 · Web Push) — 담당: 석정한(구독 · 화면) · 고현석(서버 발송)

| ID | 요구사항 | 우선 |
|---|---|---|
| FR-10-01 | D+1 회고 요청 발생 시 인앱 알림을 생성한다 | P1 |
| FR-10-02 | 알림 목록을 조회하고 읽음 처리한다 (우측 상단 진입) | P1 |
| FR-10-03 | 행동 조정 제안 발생 시 인앱 알림을 생성한다 | P2 |
| FR-10-04 | 구독한 브라우저에 **앱 외부 푸시(Web Push)로 알림을 발송한다** — VAPID 키가 없으면 푸시만 꺼지고 인앱 알림은 그대로 동작한다 (v2.11 신설 — E-68) | P1 |

> **v2.10 — 앱 외부 푸시(Web Push)는 범위 내입니다** (E-68). 종전의 "범위 외입니다"를 뒤집었습니다.
> **v2.11 — 그 발송을 `FR-10-04`로 세웠습니다.** 브라우저 구독·발송 계약은 05 §2 Web Push 3종 ·
> 04 `PushSubscription` · 07 §3 VAPID 키 3종에 있습니다.
> 알림이 **언제** 가는지는 E-71입니다 — 후보 거래의 결제 시각 −1시간을 07:00~21:00으로 자른 값입니다.
> 푸시는 **스케줄 경로에서만, 저장을 커밋한 뒤에** 나갑니다 — 목록을 여는 사용자는 이미 앱을 보고 있습니다 (05 §2).

## FR-11. 소비 분석 (v1.2 신설) — 담당: 정민규(집계 · Spring) · 오진호(문장 재구성)

| ID | 요구사항 | 우선 |
|---|---|---|
| FR-11-01 | 만족도 지도 **판정 라벨별 요약**을 제공한다 (라벨별 묶음 수 · 월 합계 금액 · 비중) | P1 |
| FR-11-02 | **카테고리별 요약**을 제공한다 (예: 배달 → 주된 주문 시간대 · 평균 금액 · 판정 라벨) | P1 |
| FR-11-03 | AI가 위 집계값을 '나만의 특징' 문장으로 재구성한다 — **집계 수치 외 임의 서술 금지** | P1 |
| FR-11-04 | 채팅 초기 메뉴에서 '소비 분석'으로 진입한다 | P1 |
| FR-11-05 | 사용자는 소비 분석 채널에서 집계에 대해 자유롭게 질문할 수 있다 — AI는 **집계 안에서만** 답한다 (v2.13 신설 — E-104 · NFR-02) | P2 |

> 집계(FR-11-01·02)는 **Spring 규칙 엔진**, 문장화(FR-11-03)만 AI입니다 (결정로그 §2 ⑧⑨). 문장화는 AI `/chat`의 `ANALYSIS_NARRATE` 작업으로 호출합니다 (05 §3).
> 자유 질문(FR-11-05)은 AI `/chat`의 `ANALYSIS` 작업입니다 — Spring은 `POST /chat/analysis`(05 #28)로 질문과 최근 대화만 넘기고, 집계는 AI가 내부 API로 읽습니다 (E-104).
> 새 연산은 없습니다 — 만족도 지도 산출값의 재집계입니다.

## FR-12. 금융 지식 Q&A (v1.3 신설 · P2) — 담당: 오진호

| ID | 요구사항 | 우선 |
|---|---|---|
| FR-12-01 | 사용자는 채팅에서 저축·정책 금융 상품(예: 청년도약계좌)의 조건·특징을 질문할 수 있다 | P2 |
| FR-12-02 | AI는 적재된 공개 금융 문서에서 검색한 근거(출처·기준 시점) 안에서만 답한다. 근거가 없으면 모른다고 답한다 | P2 |
| FR-12-03 | 원금 손실 위험 상품과 개인화된 투자 권유는 답하지 않는다 (NFR-05) | P2 |
| FR-12-04 | 공개 금융 문서를 청크·임베딩해 pgvector에 적재한다 (`scripts/ingest_financial_docs.py`) | P2 |

> 레포 `app/rag/`·`financial_rag_tool.py`가 이 요구사항의 골격입니다 (결정로그 E-21·E-22). 화면은 S16 · IA 3-2-5.
> 거래·회고·개인 분석은 RAG 대상이 아닙니다 — Spring 내부 AI API로 조회합니다.
> ⚠️ 임베딩 모델은 `text-embedding-3-small`(1536차원)이고 pgvector 확장과 `financial_chunks`는 server `V8`로 들어갔습니다 (01 E-85).
> **문서 소스(어느 기관의 어떤 문서)는 아직 미정**입니다 — 원금 손실 위험 상품 문서를 넣으면 FR-12-03이 막아야 할 답에 근거가 생깁니다.

---

## 비기능 요구사항

| ID | 항목 | 기준 |
|---|---|---|
| NFR-01 | **재현성** | 동일 데이터 → 항상 동일한 분석 결과. **사용자가 확인한 입력 이후 전 과정(묶음·보정·판정·집계)이 Spring 규칙 엔진의 결정론.** **`RuleEngineDeterminismTest`(JUnit)로 증명** (v1.3 — E-20·E-29) |
| NFR-02 | **설명가능성** | 모든 판정에 근거 데이터 제시 가능 — AI 설명은 `reasonCode`·집계 수치 재구성만 허용 |
| NFR-03 | 응답 시간 | 업로드 파싱 3개월분(약 1,000건) 기준 **10초** 이내 (⚠️ 잠정 — 9/7 재확인) |
| NFR-04 | LLM 폴백 | AI 서버 안에서 LLM **6초** 초과 시 템플릿 대체, 재시도 1회 (**FR-04-15** · E-88). Spring → AI `/chat`은 15초 초과 시 Spring 템플릿 (⚠️ 잠정 — 실제 회고 추출 프롬프트로 재확인) |
| NFR-05 | 규제 준수 | 원금 손실 위험 상품 미취급 |
| NFR-06 | 개인정보 | 화면 노출은 익명화 데이터만 |
| NFR-07 | 브라우저 | Chrome 최신 (데모 환경) |
| NFR-08 | **클라이언트** | **PWA** — manifest·설치형 진입 지원, 모바일 뷰포트 우선 (v1.2 신설) |
| NFR-09 | **톤** | AI 자연어 응답은 **해요체**, **한두 문장의 한 문단**. 반말·격식체·이모지와 후회 언어, 판결 어휘(탈락·아웃·`Out`)를 쓰지 않고 지킬 만한 소비가 있으면 먼저 언급한다. 문서·시트·피그마·발표자료에도 판결 어휘를 쓰지 않는다 (E-90) |

---

## 제약 조건

- 오픈뱅킹 API 사용 불가 → **파일 업로드 전제**
- 본선 개발 시간 약 30시간 (무박 3일)
- 발표 시간 팀당 10분
- 팀원 5인
