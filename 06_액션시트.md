# 소때잡 — 미결 항목 & 액션 시트

**버전:** v2.57 | **기준일:** 2026-09-09 | **본선까지:** 1일 (9/9 ~ 9/10)

> **v2.57 (9/9):** **server #69 종결 — `HANDOFF.md`가 PR #68 병합 뒤 상태다.** PR #70(`82cf8fd`) 병합 · CI · Deploy success. §2 상태 표(`main` `7e6cfee` → 이제 `82cf8fd` · 테스트 575건 · 열린 PR #63 · 열린 이슈 #58 · docs 01 v2.36 · 05 v2.36 · 06 v2.56), §3-1의 #67 행 제거(v2.56이 적은 HANDOFF 비차단 3건이 함께 사라졌다), §3-4 짝 이슈에 client #24 · #16 · #30 · ai #55 닫힘 반영, §8에 E-112 한 줄(`@Size` UTF-16 유지 근거). 코드 변경 없음. 남은 코드 잔재 하나 — `TransactionServiceImplTest` javadoc의 "(#20 · E-110)" 표기는 그 파일을 다음에 만질 때. 원격 브랜치 `fix/#67-review-followups` · `docs/#69-handoff-status-after-68`은 지웠다. server 열린 것은 여전히 **#58(→ PR #63)** 하나다.
>
> **v2.56 (9/9):** **server #67 종결 — 리뷰 후속 잔손질 3건(구 #60 · #66 · #57)이 배포본에 들어갔다.** PR #68(`7e6cfee`) 병합 · CI · Deploy success · 리뷰 APPROVED(575건 · 실패 0 · 건너뜀 0, 리뷰어는 CI와 정본 대조로 판단하고 로컬 빌드는 돌리지 않았다). ① `POST /retrospects/chat`의 `message`가 **최대 500자**(E-112 · 05 v2.36 §2 #11) — 넘으면 400 `INVALID_INPUT`, `INTRO` 생략은 그대로. **단위는 `@Size`(UTF-16)로 둔다** — UTF-16 단위 수는 항상 코드 포인트 수 이상이라 500 UTF-16은 500 코드 포인트보다 같거나 엄격하고, ai `app/schemas/chat.py`의 `message`는 `min_length=1`뿐이라 맞출 AI 계약이 없다. 통일하려면 #24 · #28 DTO까지 셋을 한 번에 바꾸는 별도 이슈다. ② 업로드 `TransactionServiceImpl.truncate`가 **코드 포인트 경계**로 자른다(`ClusterNameTemplate.truncate` · #20과 같은 셈법, 세 컬럼 255 · 50 · 100 공통). `importHash`는 절단 전 원본으로 계산하므로 중복 판별은 그대로다. 남은 한계: ZWJ · 피부색 수식자 이모지는 코드 포인트로 잘라도 자소 클러스터가 깨진다 — 두 `truncate`를 모을 때 `BreakIterator`와 함께 본다(비차단). ③ `PendingSummary` javadoc이 두 경로를 나눠 적는다 — `ANALYSIS_NARRATE`의 `state`에는 안 싣고(E-75), 내부 Tool 응답 `InternalAnalysisResponse`에는 싣는다(ai #50). 코드 변경 없음. **리뷰 비차단 3건은 병합에 반영되지 않았다** — server `HANDOFF.md`의 테스트 행 "540건대"(실제 575) · `truncate`(`:177`) 행 번호(지금 182) · "(#20 · E-110)" 표기(셈법 출처는 #20뿐). §2 "열린 PR #68 · #67 리뷰 대기"도 이제 낡았다. 다음 HANDOFF 갱신 때 함께 고친다. 이로써 server 열린 것은 **#58(→ PR #63 리뷰 대기)** 하나이고, 입력 대기는 R29 · R33이다.
>
> **v2.55 (9/9):** **client #30 종결 — #16에서 시작한 라벨·값 분리가 4종(목적 · 동행인 · 만족도 · 반복 의향) 모두 끝났다.** PR #31(`1d9e866`) 병합 · CI · Vercel preview pass · softcatch 승인. `TagOption<V>`의 `V extends string` 제약을 없애고 `REPEAT_OPTIONS: TagOption<boolean>[]`를 `tagOptions.ts`에 뒀다(PR #26 리뷰 스케치 그대로). `OnboardingView.vue`의 `repeatIntent: value === '네'` 비교가 사라지고 `tagValue` 결과가 `saveRetrospect`로 바로 간다 — 라벨을 바꿔도 `false`로 조용히 저장되는 자리가 없다. 리뷰 후속으로 `answerReview`의 마지막 분기를 `repeat`로 명시하고 남는 경로에 `never` 대입을 두어 단계 누락을 `type-check`가 잡게 했고, 쓰기만 하던 `ReviewAnswer.repeat`을 지웠다. 계약 변경 없음(`repeatIntent`는 05 §0 그대로 boolean). 화면 문구 그대로. 남은 잔재: `reviewRecords[].answers` 필드는 읽는 곳이 없다(#16 이전부터) — 회고 기록 표시 기능이 생길 때 같이 본다.
>
> **v2.54 (9/9):** **R28 — XLSX 선계수의 문턱을 계약과 메모리로 나눴다 (01 v2.36 E-111 정정 · server PR #63 리뷰).** v2.49에 적은 "**워크북 전체 장을 더한다**"는 **상한 안의 파일을 거절한다** — 파서가 읽는 것은 첫 장뿐인데 뒷장의 안내·약관 줄까지 합쳐 대면, 첫 장 15,000행 + 안내 6,000행짜리 파일이 합계 21,000으로 400 `TOO_MANY_ROWS`가 된다(종전에는 15,000건이 정상 적재됐다). 이제 **계약 문턱은 첫 장 물리적 행 20,030**, **메모리 문턱은 모든 장 워크시트 XML 32MB**로 따로 건다. 바이트로 재는 이유는 힙을 정하는 것이 행이 아니라 칸 수여서다 — 운영 힙 512m 실측에서 60,000행 × 10칸(35.9MB)은 열리고 80,000행 × 10칸(48.0MB)은 OOM이라 행수로는 선을 그을 수 없다. 같은 라운드에 `<row` 뒤 공백을 XML 규격대로 넷 다 인정한다 — 띄어쓰기만 세면 줄바꿈을 쓴 정상 워크시트가 **0행**이 되어 가드가 말없이 꺼진다. 계약(20,000행 · 400 `TOO_MANY_ROWS` · 한 건도 저장 안 함)은 그대로이고 **R28의 상태도 ✅ 그대로**다. 남은 것은 PR #63 리뷰뿐이다.
>
> **v2.53 (9/9):** **client #16 종결 — R31 ✅.** PR #26(`6d8c95a`) 병합 · CI · Vercel preview pass · softcatch 승인. 표준 태그 값 7/6종은 `src/api/enums.ts`의 `PURPOSE_TAG` · `COMPANION_TAG`, `{ value, label }` 쌍은 `src/components/chat/tagOptions.ts`로 나눴다. `OnboardingView.vue`는 칩에 라벨을 그리고 저장할 때 값을 보낸다 — `POST /retrospects`의 `purpose`가 `"휴식 · 취미"`에서 정본 `"휴식·취미"`로 바뀌고 화면 문구는 그대로다. 리뷰에서 같은 결함 클래스 두 곳을 더 닫았다: `saveRetrospect`의 `purpose` · `companion` 타입을 `PurposeTag` · `CompanionTag`로 좁혀 라벨을 넣으면 컴파일이 막고, 만족도 3택도 `SATISFACTION_OPTIONS`로 옮겨 `satisfactionMap[...] ?? 'UNKNOWN'`이 조용히 `UNKNOWN`을 저장하던 경로를 없앴다. 반복 의향 2택(`repeatIntent: value === '네'`)만 값이 `boolean`이라 **client #30**으로 분리했다. E-99(서버가 받는 자리의 공백 정규화)는 그대로 둔다 — 방어선이지 계약 변경이 아니다. `/retrospects/chat` 배선은 `PURPOSE_OPTIONS`의 `value`로 응답을 대조한다.
>
> **v2.52 (9/9):** **server 이슈 정돈 — 열린 이슈 5건 → 2건.** 리뷰 후속 잔손질 셋을 **server #67 하나로 묶었다** — #60(`/retrospects/chat`의 `message` 1~500자) · #66(업로드 `truncate`가 UTF-16 경계에서 잘라 이모지가 반으로 끊긴다 — v2.50이 굳힌 코드 포인트 규약을 저장 경로에도 적용) · #57(`PendingSummary` javadoc이 `ANALYSIS_NARRATE` 경로만 말한다, Tool 응답에는 실린다). 각각 파일 하나 · 10~30분이고 서로 다른 파일이라 충돌하지 않는데, 본선까지 하루인 지금 **이슈 → 브랜치 → PR → 리뷰 → 06 반영을 세 번 치르는 비용이 코드보다 크다** — 브랜치 하나 · PR 하나로 끝낸다. 원본 세 이슈는 본문을 줄이지 않고 #67 `작업 범위`로 옮긴 뒤 닫았다. 두 `truncate`를 공통 헬퍼로 모으는 것은 #67 제외 범위다. **#49는 닫고 R33으로만 추적한다** — 문서 몫(01 v2.28 E-102 보강, "적중률은 가드레일 통과율만큼 깎인다")은 이미 끝났고 남은 것은 리허설의 `fallback: true` 비율뿐이라 GitHub에서 밟을 다음 한 걸음이 없다. 비율이 나오면 R33에서 A/B/C를 01에 E-번호로 확정하고, 코드가 필요하면 그때 새 이슈를 낸다. 이로써 server 열린 것은 **#58(→ PR #63 리뷰 대기) · #67**뿐이고, 입력 대기는 R29(매핑표 — 담당 미정) · R33(리허설 비율)이다. server `HANDOFF.md` §2 · §3도 같은 상태로 맞춘다(#67 PR에 함께 싣는다).
>
> **v2.51 (9/9):** **client #24 종결 — R28의 마지막 조각.** PR #25(`5b706b0`) 병합 · CI · Vercel preview pass. `TOO_MANY_ROWS`의 서버 `message`("20,000건 이하로 나눠서 올려 주세요")가 거래내역 화면과 온보딩 3단계 모두에 그대로 보인다 — `errorMessage.ts`에 `case 'TOO_MANY_ROWS': return error.message`, `TransactionsView.vue`의 `catch {}`를 `apiErrorMessage`로. 다른 오류 코드의 문구는 그대로다(테스트 6건이 고정). 05 v2.35. R28에서 남은 것은 server #58(PR #63 리뷰 대기)뿐이다.
>
> **v2.50 (9/9):** **server #61 종결 — E-110이 배포본에 들어갔다.** PR #62(`bf5efd8`) 병합(softcatch APPROVED) · CI · Deploy success. `recentMessages` 항목 `content` 상한이 `user` 500 · `assistant` 2,000이고 **코드 포인트**로 센다 — 리뷰 3라운드의 핵심은 단위였다. 리뷰어가 처음 UTF-16 통일을 권했다가 `ClusterNameTemplate.truncate`(#20 · PR #44)의 선례를 근거로 스스로 뒤집었고, validator를 `codePointCount`로 고쳤다(`48966cf`, 이모지 경계 테스트 2건 — UTF-16 계수로 되돌리면 그 2건만 실패, 리뷰어 돌연변이 검사로 재현). 이 저장소 규약: **AI와 주고받는 텍스트 길이는 코드 포인트**. 리뷰 후속 — ai #55 본문 완료 조건에 프롬프트 예산 재측정(2,000자 × 6건이 NFR-04 6초 안에 드는지, 안 들면 상한이 아니라 NFR-04 값을 고친다) 추가 · **server #66** 신설(`TransactionServiceImpl.truncate`가 UTF-16 경계 `substring` — 업로드 데이터라 AI 계약과 무관, 반쪽 문자 위험만, 코드 판독) · client #22에 단위 정정(`[...content].length`). 남은 것: ai #55 · server #60 · #66 · client #22 배선. server `HANDOFF.md`(PR #65)의 상태 스냅샷은 #62 병합 전 기준이다
>
> **v2.49 (9/9):** **R28의 남은 조건이 사라졌다 — server #58 · PR #63 리뷰 대기.** v2.44가 R28 ✅에 붙였던 **"운영 JVM 힙 1GB 이상"** 은 이 인스턴스에 맞지 않는 조건이었다. 운영은 **한 대(메모리 1.9 GiB · swap 2 GiB)에 `db` · `server` · `ai`가 같이 뜨고** `docker stats` 실측이 각각 **497.7MB · 62.8MB · 39.7MB**다 — 서버 하나에 힙 1GB를 떼어 줄 자리가 없다. 그래서 힙을 키우는 대신 **XLSX를 시트를 열기 전에 세도록** 바꿨다(01 v2.34 **E-111**): POI `XSSFReader`로 워크시트 XML을 흘려보내며 `<row>`만 세고, 물리적 행이 `20,000 + 머리글 탐색 30줄`을 넘으면 `XSSFWorkbook`을 부르지 않는다. ~~워크북 전체 장을 더한다~~ — **v2.54에서 문턱을 둘로 나눴다**(계약 = 첫 장 행수 · 메모리 = 모든 장 워크시트 XML 32MB). 실측: 200,000행 xlsx가 힙 **256m · 320m · 512m 모두 0.2초 안에 400**(고치기 전 512m는 5.9초 OOM → 500), 경계인 20,000행 xlsx는 256m에서 0.6초에 정상 파싱. 함께 **운영 JVM 힙을 `-Xmx512m` · 컨테이너 상한을 `mem_limit: 1g`으로 명시**하고 07 **§12**에 적었다 — 명시 전 실효 힙(476MB)보다 크게 잡았다. 목적은 줄이는 것이 아니라 호스트 메모리에 딸려 다니지 않게 못 박는 것이다. 자리는 `deploy/docker-compose.yml`이고, 같은 이미지를 로컬에서도 쓰므로 `Dockerfile`에는 넣지 않는다. **문서에 있던 "t3.micro · 1 GiB"는 실측과 달랐다** — 사이징 값은 문서가 아니라 EC2에서 잰다. 계약은 그대로다(상한 20,000행 · 400 `TOO_MANY_ROWS` · 한 건도 저장 안 함). 남은 것은 **client #24**(서버 `message`를 화면에 띄우기)뿐이다.
>
> **v2.48 (9/9):** **server 인수인계.** server 담당이 9/9 저녁부터 client 작업을 돕는다. 이어받는 백엔드 팀원을 위해 server `HANDOFF.md`를 만들었다(server #64 · **PR #65** 리뷰 대기) — 상태 스냅샷 · 남은 작업의 다음 한 걸음 · 정본→이슈→PR→리뷰 회신→06 반영 절차 · 55432 검증 레시피 · docs 갱신 함정 · E-100~E-110 한 줄 맥락. 이 시점의 server 열린 것: PR #62(#61) · PR #63(#58) · PR #65(#64) · 이슈 #49 · #57 · #60. **결정 없이 바로 되는 것은 #57 · #60**, 입력 대기는 R29(매핑표 — 담당 미정) · #49(리허설 폴백 비율). 이 표의 `고현석(server)` 담당 칸은 그대로 두고, 실제 착수자는 PR 작성자로 본다.
>
> **v2.47 (9/9):** **server #56 종결 — R34 파생 첫 건 닫힘.** PR #59(`a8ca135`) 병합 · CI · Deploy success. `recentMessages` 규격 밖 항목(`role ∉ {user, assistant}` · 빈 `content` · `null`)이 503 `LLM_UNAVAILABLE` 대신 **400 `INVALID_INPUT`**이고 AI를 부르지 않는다(E-109 · 05 v2.33, 두 채널 공통). 리뷰(softcatch)는 코드 지적 없이 후속 셋을 냈다 — ① 6건 절단 전 검증이라 잘려 나갈 항목도 400 → client가 보내기 전 6건으로 자르고 자체 검증(client #22 코멘트). ② `assistant` 발화에도 500자는 근거가 없다(ai에 `max_tokens` 없음 · server 자신이 `FINANCE_QA`에서 TEXT를 그대로 실음) → **E-110 확정 `user` 500 · `assistant` 2,000**(01 v2.33 · 05 v2.34), server #61 → **PR #62**(#59 위에 쌓았다가 병합 뒤 `main`으로 리베이스, `17b7106`), `reply` 2,000자 보장은 **ai #55**. ③ `/retrospects/chat`의 `message`에 상한이 없다 → **server #60**. 남은 것: PR #62 리뷰 · 병합, ai #55, server #60, client #22 배선(규칙 코멘트 3건 반영)
>
> **v2.46 (9/9):** **R34 server 몫 종결** — PR #55(`79f6123`) 병합 · CI · Deploy success. `POST /chat/analysis`(05 #28)가 열렸다. 리뷰 3라운드에서 서버 코드 지적은 없었고, 리뷰어가 격리 스택으로 실제 AI 왕복까지 확인했다(1.0~1.5초 · 수치 전부 집계 안 · 되물음 · 유효 묶음 없음 · 키 없음 · 401 · 400). 리뷰에서 나온 것 둘은 각자 자리로 — `ANALYSIS` 응답 반말 7/7(ai #48에 얹음, `ANALYSIS_INSTRUCTION` 해요체 한 줄 · 지시문 상수 검사 + 실측) · `recentMessages` 규격 밖 값이 503으로 보임(server #56, 두 채널 공통, 05 먼저). **client #22 · ai #48이 남는다** — 병합 직후라 사용자에게는 아직 고정 문장이 보이고, ai가 먼저 고쳐지지 않으면 client 배선 뒤 반말 답변이 그대로 노출된다. ai #48을 먼저 닫는 순서를 권한다.
>
> **v2.45 (9/9):** **server #52 종결 — R28 닫힘.** PR #54(`cbecb86`) 병합 · CI · **Deploy success**. 업로드 행수 상한 20,000행이 배포본에 들어갔다(E-105 · 05 v2.29). 리뷰 후속은 v2.44 그대로 — server #58(운영 힙 1GB 조건) · client #24(`message` 표시). 이로써 9/9 오후 결정 D · E · F 중 코드가 있던 둘(PR #53 · #54)이 모두 병합됐다. 남은 server 열린 코드: #50(PR #55) · #49(결정 대기) · #57 · #58.
>
> **v2.44 (9/9):** **server PR #54(R28 행수 상한) 리뷰 반영(softcatch, 통과 · 비차단).** 리뷰어가 CI와 같은 조합으로 543건 · 건너뜀 0을 재현했고, 본문에서 "하지 않았다"고 적은 HTTP 왕복(MockMvc)까지 대신 확인했다 — 20,001행 400 본문 · 20,000행 200 · 저장소 · 재계산 무호출. 남은 것 둘, 둘 다 이 PR의 회귀가 아니다. **A.** XLSX는 `XSSFWorkbook`이 시트를 통째로 올린 **뒤에** 행을 세므로 읽는 비용이 상한에 막히지 않는다 — 200,000행 · 3.2MB xlsx가 `-Xmx1g`에서는 1.85초에 400, **`-Xmx512m`에서는 세기 전에 OOM → 500**(`Error`라 catch에 안 걸린다). `Dockerfile`에 `-Xmx`가 없고 compose에 메모리 제한이 없어 힙은 컨테이너 메모리의 25%다. **R28 ✅에는 "운영 힙 1GB 이상" 조건이 붙는다** — **server #58**(힙 명시 또는 XLSX 스트리밍). CSV는 17MB · 299,593행도 0.16초에 거절된다. **B.** client가 서버 `message`를 버린다(`TransactionsView.vue:218` `catch {}` · `OnboardingView.vue:408` `apiErrorMessage` default) — "20,000건 이하로 나눠서"라는 해결책이 사용자에게 안 보인다. **client #24** · 05 v2.31. XLSX는 POI 행 이터레이터라 물리적으로 없는 행은 세지 않고 CSV는 빈 줄도 센다 — 느슨한 쪽이라 문제는 아니다.
>
> **v2.43 (9/9):** **server #51 종결** — PR #53(`a80ccfe`) 병합 · CI · **Deploy success**. `highlight` 4종째 문구가 배포본에 들어갔다(E-89 확정, 01 v2.30). 리뷰 2라운드(softcatch, 비차단)를 ai #50에 보탰다 — ① `byCategory`가 비는 상태는 **셋**(거래 0건 · 거래 있음∧묶음 0개 · 묶음≥1∧합계 0)이고 server는 앞 둘을 `NO_RETROSPECT`로 접는다. `NO_RETROSPECT`에 "이번 달"이 없는 것은 1번에 회고할 이번 달 거래가 없어서다. B안(둘 다 참인 문장)을 고르면 뒷절의 월 표기까지 빼야 한다. 리뷰어 권고는 A(server와 같은 식으로 유효 묶음 수 계산). ② A 구현 시 `_has_valid_data` 호출 지점을 `result.data`로 바꿔야 한다 — `_prompt_analysis`가 `pending`을 먼저 자른다. ③ server `PendingSummary` javadoc "AI에게는 보내지 않는다"는 `ANALYSIS_NARRATE` 경로만이고 Tool 응답에는 실린다 — **server #57**(주석 정정)으로 뺐다.
>
> **v2.42 (9/9):** **server PR #53 리뷰 반영(softcatch, 병합 비차단).** ① v2.39 메모의 ai 조건 "묶음 0개"는 **틀렸다** — ai `_has_valid_data`는 `byCategory`가 비어 있는지만 보므로 안내문 하나가 server의 두 분기(`NO_RETROSPECT` · `NO_MONTH_ACTIVITY`)를 통째로 덮는다. 그래서 E-89 확정 문구를 ai에 그대로 복사하면 묶음 0개 사용자에게 틀린 말이 나간다. **ai #50**으로 넘겼다(조건 분리 A · 둘 다 참인 문장 B, 갈래까지 적음). ② 05 §2 #28의 안내문 줄을 실제 조건으로 정정(05 v2.30). ③ 참고 — 템플릿 4종의 "이번 달"은 캘린더 달이 아니라 기준월(E-60, 최근 거래월)이다. 8월 거래만 올린 사용자가 9월에 보면 "이번 달"이 8월이다. 이 PR이 만든 문제가 아니고 4종 공통이라 손대지 않았다 — 표기를 바꾸려면 4종 + ai 안내문을 한 번에 봐야 한다.
>
> **v2.41 (9/9):** **R34 세 저장소 이슈 발행** — server #50 → **PR #55**(`b0ac287`, CI pass, 리뷰 대기) · client **#22**(`ChatView.vue:413` 고정 문장을 `POST /chat/analysis`로 교체, 채널 이력을 `recentMessages`로) · ai **#48**(`ANALYSIS` Handler에 숫자 가드 — 근거는 Tool 응답 camelCase라 `ANALYSIS_NARRATE` 가드를 공용화해 키를 맞춘다). 팀 채널 `[계약변경]` 공지는 병합 전에 올린다(07 §6).
>
> **v2.40 (9/9):** **server #47 종결** — PR #48(`d3709f7`) 병합 · CI · Deploy success. `HighlightServiceImpl` 주석 두 곳을 ai PR #43 이후 사실로 맞췄다 — 캐시 키 전제(전용 핸들러가 있으나 도구는 부르지 않는다, E-102) · `fallback: true`의 두 번째 원인(숫자 가드레일, E-79 · E-101) · 인라인 주석의 재호출 전제(가드레일 폴백은 반복될 수 있다 → R33 · server #49). 리뷰가 양쪽 저장소를 전수 확인해 고칠 자리가 이 파일뿐임을 굳혔다. 리뷰 정정 하나 — 가드레일 폴백은 **성공 응답**이라 비용은 15초가 아니라 정상 LLM 왕복 한 번이다(R19 문구에 반영, R33은 이미 그렇게 적혀 있다).
>
> **v2.39 (9/9):** **결정 셋을 닫습니다 — D · E · F.** ① **R28 ✅** 업로드 행수 상한 20,000행 → 400 `TOO_MANY_ROWS`(01 v2.30 **E-105** · 05 v2.29 · server #52 · **PR #54** 리뷰 대기, 543건 통과 · 건너뜀 0). client 타임아웃 분리는 하지 않는다. ② **E-89 문구 확정** — "이번 달 거래 중 돌아본 것이 아직 없어요"(server #51 · **PR #53** 리뷰 대기, 540건 · 건너뜀 0). R22의 "문구 확정만 남는다"가 닫힌다. ai `app/agent/handlers/analysis.py:16`의 "유효 묶음 없음" 안내문이 옛 문구("이번 달에 돌아본 소비가 아직 없어요")를 들고 있다 — ~~조건은 다르지만(묶음 0개)~~ **v2.42 정정: ai는 `byCategory`만 보므로 server 두 분기를 함께 덮는다 — ai #50.** 같은 모호성이라 ai 몫으로 맞춘다. ③ **FR-02-05 가맹점명 정규화 본선 제외**(01 **E-106** · 02 v2.14) — `merchantNormalized`를 읽는 코드가 없어 구현해도 보이는 차이가 0이다. 남은 P0 갭은 R29(매핑표)뿐이다.
>
> **v2.38 (9/9):** **`ANALYSIS` 서버 경로를 살립니다 — R34 신설.** 01 v2.29 **E-104** · 05 v2.28 #28 `POST /chat/analysis` · 02 v2.13 FR-11-05(P2). 상태 없는 프록시(E-63과 같은 모양)라 마이그레이션이 없습니다. R34는 세 몫입니다 — server 프록시 · client 고정 문장(`ChatView.vue:413`) 교체 · ai `ANALYSIS` 핸들러 숫자 가드(E-79 적용).
>
> **v2.37 (9/9):** **R33 신설 — server #49.** `highlight` 캐시가 숫자 가드레일 폴백을 매 진입 다시 부른다(server PR #48 리뷰에서 나온 지적). 결정(A/B/C)은 리허설의 `fallback: true` 비율을 본 뒤로 미루고, 01 E-102에 "적중률은 가드레일 통과율만큼 깎인다"를 보탰다(01 v2.28). **R29의 문서 절반을 닫았다** — 04 §4(v2.15)에 "매핑표 전까지 `category` · `sourceCategory` 둘 다 원본"을 명시. R29의 코드 절반(변환 · 백필 · 재계산)은 매핑표에 묶인 채 ☐.
>
> **v2.36 (9/9):** **server #43 종결** — PR #46(`0f507da`) 병합 · CI · Deploy success. 제안 `reason`을 AI 문장(`ACTION_PLAN`)으로 채우는 경로가 `main`에 올랐다(01 v2.25 **E-103** · 04 v2.14 · 05 v2.27). 540건 통과 · 건너뜀 0(작성자 로컬 · CI). 리뷰 3라운드에서 결함 셋을 닫았다 — v2.34의 둘(롤업 리프 · 숫자 가드)에 더해 ③ `@Async` 전용 executor의 거절 정책이 기본(`AbortPolicy`)이라 큐가 차거나 종료 중이면 `TaskRejectedException`이 `RetrospectServiceImpl.save`로 올라와 **회고가 커밋된 뒤 `POST /retrospects`가 500**이 되던 것을, 거절을 로그 후 버리는 핸들러로 고쳤다(`config/AsyncConfig` · `AsyncConfigTest` 2건 · server `cfba4bc`). 리허설에서 볼 것 — `@Async` 배선(프록시가 `suggestionReasonExecutor`를 타는지)은 단위 테스트 밖이라, 회고 저장 직후 `GET /suggestions`가 템플릿이었다가 다음 조회에서 AI 문장으로 바뀌는지 실제 환경에서 확인한다. **server가 ai#40보다 먼저 병합됐으므로 이 시점부터 반말 이유 문장이 쌓인다** — R32의 `UPDATE suggestions SET reason = NULL` 1회가 실제로 필요해졌다.
>
> **v2.35 (9/9):** **R19 종결** — 정본 ai PR #43(`be82917`) 병합 · CI · Deploy success · `pytest` 167건 통과. `ANALYSIS_NARRATE` 전용 핸들러 + 숫자 가드(E-79 · E-101). 리뷰 3라운드: 정상 `YYYY-MM`의 연·월 허용 · `%`가 붙은 한 자릿수도 검사 · 퍼센트는 `share × 100` 전용 집합과만 대조(월 `8`이 `8%`의 근거가 되지 않는다) · `%` 앞 공백 우회 차단. 이 PR로 낡은 전제 하나를 정정합니다 — E-102의 "전용 핸들러 없이"는 "전용 핸들러가 있으나 도구는 부르지 않는다"가 맞고 캐시 키의 결론은 그대로입니다(01 v2.26 · server 이슈 #47). `ANALYSIS`(ai #38)의 서버 호출 경로는 미결입니다.
>
> **v2.34 (9/9):** **server PR #46(#43) 리뷰를 반영했습니다 — 제안 이유를 AI 문장으로(E-103).** 리뷰가 결함 둘을 잡았습니다. ① 리프가 롤업되면 제안은 상위 묶음에 달리는데 리프 id로만 찾아 **영영 채워지지 않았습니다** — `parentId`를 따라가도록 고쳤습니다. ② "숫자가 바뀌면 옛 문장을 버린다"는 가드가 `reason == null`만 봐서 동작하지 않았습니다 — AI를 부르기 전 `adjustCount` · `expectedSaving`을 붙잡아 두고 대조합니다. 응답 예산은 **비동기(`@Async`)로 결정**했습니다 — 동기로 두면 저장 응답이 최악 30초라 클라이언트 타임아웃 20초에 걸립니다. 정본은 01 v2.25(E-103) · 04 v2.14(`Suggestion.reason`) · 05 v2.27입니다. **R32는 ai 몫으로 남습니다.**
>
> **v2.33 (9/9):** **server #19 종결** — PR #45(`61c777e`) 병합 · CI · Deploy success. `GET /analysis`의 `highlight`를 사용자별 한 칸으로 캐시하고 키는 집계 그 자체다 — 무효화 훅 없이 집계가 바뀌면 miss(01 v2.24 **E-102** · 05 v2.26). 503 · 빈 문장 · `fallback: true`는 캐시하지 않는다. 507건 통과 · 건너뜀 0(작성자 로컬 · CI). R 항목 없이 v2.5 메모의 #19를 닫는다. 리뷰에서 남긴 전제 — ai가 `ANALYSIS_NARRATE`에 도구를 붙이면 키를 다시 본다(`HighlightServiceImpl.Key` 주석). `OPENAI_API_KEY`가 없는 환경에서는 캐시가 채워지지 않으므로 리허설 환경에 키가 있어야 체감된다.

> **v2.32 (9/9):** **R19가 정본 ai PR #43에서 구현됐고 리뷰 보강을 기다립니다.** 숫자 가드의 한 자릿수 예외는 `%`가 바로 뒤에 붙지 않은 정수에만 적용하고, `analysis_year_month`의 연도·월은 허용 숫자로 넣습니다(01 v2.23 **E-101** · 05 v2.25). 병합 전이라 상태는 ☐로 유지합니다.

> **v2.31 (9/9):** **R18 후속 server #20 종결** — PR #44(`5b1a3eb`) 병합. 제안 `reason` 3종에서 묶음 이름 뒤 `은(는)`을 없앴고, 리뷰(문구 담당)에서 **이름 뒤에는 받침과 무관한 조사(`의` · `에`)만 쓴다**로 확정했다(05 v2.23 → v2.24, 새 E 없음 — E-84 안의 문구 갱신). `ClusterNameTemplate.truncate`가 12자를 코드 포인트로 잘라 이모지가 반으로 끊기지 않는다 — ai `fallback.py`의 `[:12]`와 처음으로 같은 뜻이 됐다(05 §3). 509건 통과 · 건너뜀 0. 리뷰 후속은 ai #42(픽스처 옛 문구) · client #17 코멘트(`MapView.vue` 목업 `prescription` 삭제).
>
> **v2.30 (9/9):** **R27 후속 server #36 종결** — PR #42(`4195272`) 병합. `PUT /goals/{id}`의 전체 행 쓰기가 실적 배분을 덮던 것을 `Goal`의 `@DynamicUpdate`로 닫았고, 리뷰에서 같은 조건의 `User` · `Suggestion` · `BehaviorCluster`까지 셋 더 닫았다(01 v2.21~v2.22 **E-100** · 04 v2.13). 504건 통과 · 건너뜀 0. client 목표 수정 배선 때 `PUT`에 `currentAmount`를 싣지 않는다는 메모는 client #17에 있다.
>
> **v2.29 (9/9):** **R25 종결** — server PR #35(`b192382`) 병합을 상태 칸에 반영. 코드는 10:51에 들어갔는데 문서가 ☐로 남아 있었다.
>
> **v2.28 (9/9):** **server PR #41(이슈 #38)이 회고 저장 400을 닫았습니다.** 표준 태그를 받는 자리에서 공백을 지워 정본 표기로 되돌립니다 — 01 v2.20 **E-99** 신설 · 05 v2.22. 저장값 · 응답값은 여전히 한 표기라 E-20 · E-41은 그대로입니다. 근본 원인인 **client의 라벨 · 값 혼용은 R31**로 세웠습니다(client 이슈 #16).
>
> **v2.27 (9/9):** **R26을 server PR #37이 반영했습니다**(`e6356ec`로 병합, 이슈 #29 종료). `GET /transactions` — Page 검색 · `hasRetrospect` 필터 · 회고 요약 · 페이징. 리뷰 마지막 라운드의 메모로 **R29 처방에 백필과 재계산을 보탰습니다** — 매핑표 반영은 `category()` 한 줄이 아니라 기존 행 백필(마이그레이션 1회) + 전체 묶음 재계산 1회(E-58)까지다.
>
> **v2.26 (9/9):** **server PR #37(R26 · 거래 목록) 리뷰 결론을 반영했습니다.** ① `GET /transactions`의 `category`는 400을 걸지 않는다(05 v2.21) — 그 뿌리인 04 §4 `category`/`sourceCategory` 갭을 **R29**로 세웠습니다. ② client 거래내역 화면의 정렬 · 검색은 API에 더하지 않고 client에서 뺍니다(리뷰 (가) 안, R26에 기록). ③ client 배선 때 걸릴 네 가지(정렬 · 검색 삭제 · `cardIssuer` 잔재 · `page` 클램프 · 항목 매핑)를 **R30**으로 세웠습니다.
>
> **v2.25 (9/9):** **server PR #35(R25) 리뷰를 반영했습니다.** E-92 ③의 "간격 = 전체 ÷ `sampleSize`"가 정수 나눗셈의 나머지를 버려 기간에서 가장 오래된 구간을 통째로 놓치는 것이 드러나(59건에서 20건이면 34%가 빠집니다) **`i`번째 표본의 자리를 되짚는 식으로 교체**했습니다. 전체가 `sampleSize`의 두 배 미만이면 종전 규칙은 최신순 절삭과 같아졌습니다 — 이 결정이 막으려던 바로 그 동작입니다. 읽는 범위 상한 **2,000건**과 **D+1을 보지 않는다**도 05 §2에 못 박았습니다(E-92 ① 보강 · 명시). server는 `d692cdd`(fix) · `d9e095f`(test)로 반영했고 DB 통합 3건이 붙었습니다. **R25는 병합 대기입니다.**
>
> **v2.24 (9/9):** **R27을 server PR #32가 반영했습니다**(`4d0be5d`로 병합, 이슈 #30 종료). `GET /reports/monthly` · `MonthlySnapshot` 첫 조회 확정 · `Goal.currentAmount` 실적 배분. 리뷰 2라운드에서 정본이 세 번 보강됐습니다 — 01 v2.17 E-94 ③④⑤(직전 달만 배분 · 확정된 달의 전월 값 · 첫 거래월 하한) · 04 v2.11 · 05 v2.18, 그리고 전월 두 값의 출처 통일(docs `b231308`). `PUT /goals/{id}`와 확정 배분의 경합은 server 이슈 #36으로 분리했습니다.
>
> **v2.23 (9/9):** **R24를 server PR #31이 반영했습니다**(`6aead02`로 병합, 이슈 #27 종료). 업로드를 커밋한 뒤 파사드 빈이 묶음을 다시 계산하고, 재계산이 실패해도 업로드는 200입니다(E-95). 건너뛰는 조건은 회고 0건이고 판단은 재계산 안에서 합니다(E-96). DB 통합을 켠 상태에서 **408건 통과 · 건너뜀 0**입니다. **남은 것은 R28**(업로드 응답 시간과 client 20초 타임아웃)뿐이고, 재계산이 더한 비용은 전체의 2~3%로 이미 실측했습니다.
>
> **v2.22 (9/9):** **server PR #31(R24) 리뷰를 반영했습니다.** E-95 ⑤의 `importedCount == 0` 가드가 E-95 ④의 복구 경로를 막는 것이 드러나 **E-96으로 조건을 "회고 0건"으로 교체**했습니다(판단은 재계산 안에서 합니다). 업로드 뒤 `GET /suggestions`의 `PROPOSED`가 비는 것은 **E-97**로 05 §2에 명시했습니다. 업로드 응답 시간도 실측했습니다 — 재계산이 더하는 비용은 **9~93ms(전체의 2~3%)** 이고, client 20초 타임아웃을 위협하는 것은 파싱과 저장입니다. 그 몫은 **R28**로 뺐습니다.
>
> **v2.21 (9/9):** **R25를 채웠습니다 — 온보딩 슬라이스(server 05 #18 · #21).** v2.19에서 #28이 채우기로 비워 둔 자리입니다. 표본을 만드는 방법은 01 **E-92**로 채번했고(기간 균등 추출 · 미회고 거래만 · 상한 100), 05 §2(v2.16)에 Response를 신설했습니다. server 이슈 #28의 코드는 로컬에서 온보딩 1~4단계 왕복까지 확인했습니다 — `clusterCount`와 `GET /satisfaction-map`의 점 개수가 같습니다. **클라이언트는 `startOnboarding` 응답을 쓰도록 한 줄 고쳐야 합니다**(client 이슈 #3) — 지금은 응답을 버리고 `GET /retrospects/candidates`를 다시 부르는데 그 경로는 온보딩 시점에 0건입니다.
>
> **v2.20 (9/9):** **R24의 배치 자리를 E-95로 채번**했습니다 — 업로드를 커밋한 뒤 파사드 빈이 재계산을 별도 트랜잭션으로 부르고, 재계산이 실패해도 업로드는 200입니다. 컨트롤러 안(`가`)과 이벤트(`다`)를 고르지 않은 이유, 한 트랜잭션(`나`)이 치르는 값은 01 v2.14에 적었습니다. 05 §2(v2.15)에 동작을 명시했고, R24는 server 이슈 #27의 코드 PR을 기다립니다. **창은 좁아지고 닫히지는 않습니다** — 재계산이 실패하면 다음 회고 저장까지 이 이슈의 화면이 남습니다.
>
> **v2.19 (9/9):** 남은 외부 API 두 개의 계약을 정했습니다 — **E-93** 거래 목록 · **E-94** 월간 리포트(산식 · 확정 시점 · 목표 실적 배분). 레포 조치 **R26 · R27** 신설, server 이슈 #29 · #30이 대응합니다. R25(온보딩 · E-92)는 #28이 채웁니다.
>
> **v2.18 (9/9):** R13 · R14 · R15 · R16 · R17의 **server 몫을 본선 PR 번호로 갱신**(PR #2 · #4 · #6 · #10 · #16). demo `server/` 체크포인트 8개 이식이 PR #22(V8)로 끝났고 R22 fix(PR #25)까지 `main` 401건 통과. client · ai 칸은 각 담당자 이식 뒤 채운다. demo #18 · #19는 server 이슈 #29(거래 목록) · #30(월간 리포트)로 재생성, 온보딩은 #28.
>
> **v2.17 (9/9):** **R22를 server PR #25가 반영**했습니다(`33f218b`로 병합, 이슈 #24 종료). `HighlightTemplate` 4종 분기 · `AnalysisServiceImpl`의 E-91 가드 · 테스트 3종(단위 2 · 통합 1)이 들어갔고, DB 통합을 켠 상태에서 **401건 통과 · 건너뜀 0**입니다. **남은 것은 E-89 잠정 문구의 확정뿐**이고, 확정되면 상수 한 줄과 테스트 기대값만 바뀝니다. R24(이슈 #27)는 그대로 열려 있습니다.
>
> **v2.16 (9/8):** **레포 조치 R24 신설 — CSV 업로드 뒤 묶음 재계산.** server PR #25 재검증에서 실측으로 나왔습니다. 거래를 넣는 유일한 경로가 재계산을 부르지 않는데, 기준월이 바뀌는 순간이 바로 그 순간입니다. PR #25가 만든 문제가 아니라 E-60 · E-78 · E-61의 조합이라 **이슈 #27로 분리**했습니다. 순환 참조 때문에 한 줄로 못 고치고 배치 자리를 정하는 결정이 붙습니다. 본선 데모 경로(온보딩: 업로드 → 회고)는 이 창을 밟지 않습니다.
>
> **v2.15 (9/8):** **R22의 범위를 넓혔습니다 — `AnalysisServiceImpl`의 AI 미호출 조건(01 E-91)이 함께 갑니다.** server PR #25가 `HighlightTemplate` 분기만 추가했는데, 그 문장이 나오는 조건(`유효 묶음 ≥ 1 ∧ byCategory 비어 있음`)이 곧 AI를 부르는 조건이라 `OPENAI_API_KEY`가 있는 환경에서는 새 문장이 한 번도 나가지 않습니다. 데모(키 없음)에서만 의도대로 보입니다. 테스트 대상도 `AnalysisServiceImplTest`를 더해 셋으로 적었습니다.
>
> **v2.14 (9/8):** **금융 RAG 스키마의 server 절반을 PR #22가 반영**했습니다(13:45 `b118e1e`로 병합, 이슈 #21 종료). **EC2 배포까지 성공했습니다** — 13:50에 `server UP (b118e1e)`입니다. 병합 전에 걸어 둔 위험 둘이 실측으로 닫혔습니다: EC2의 `db` 컨테이너가 실제로 pgvector여서 `CREATE EXTENSION vector`가 통과했고, V1~V3 체크섬도 맞아 Flyway가 멈추지 않았습니다. **ai 절반은 R23으로 남습니다.**
>
> **v2.13 (9/8):** **레포 조치 R23 신설 — 금융 RAG의 `ai` 절반을 정본 레포로 이식.** R12를 `✅ demo 9/7 (#20)`로 닫았는데 그 근거가 demo 모노레포입니다. 정본 `jittaejap/sottaejap-ai` `main`에서 `retriever` · `embedding` · 적재 스크립트는 모두 스텁이고 `financial_chunks`를 부르는 곳이 한 곳도 없습니다. R12만 보면 다 된 것으로 읽혀 놓치기 쉬운 자리라 항목을 따로 세웁니다. server 절반은 PR #22가 옮깁니다. 04 §1도 v2.9로 `createdAt` · `source NOT NULL`을 반영했습니다.
> 레포 조치 건수를 **23건**으로 고쳤습니다 — 종전 “21건”은 R22까지 22행일 때부터 한 건 뒤처져 있었습니다.
>
> **v2.12 (9/8):** **R18을 server PR #18이 반영**했습니다(12:23 `675f660`으로 병합). R18은 server 단독이라 이것으로 닫힙니다.
> 리뷰에서 나온 후속 둘은 server 이슈로 옮겼습니다 — #19(`GET /analysis` highlight 캐시 — **PR #45로 종결, v2.33**) · #20(`reason` 문구의 `은(는)` 표기 · `ClusterNameTemplate.truncate` 이모지 잘림).
>
> **v2.11 (9/8):** `highlight` 템플릿 4종화(01 E-89)를 server에 반영하기 위한 **레포 조치 R22** 신설. server PR #16 리뷰에서 "묶음은 있는데 기준월 합계 0"에 NO_ADJUST 문장이 나가는 것이 확인됐고, E-75가 3종으로 못 박고 있어 결정으로 올렸습니다.
>
> **v2.10 (9/8):** **R20의 server 몫과 R21을 server PR #12가 반영**했습니다(19:31 `02c005f`로 병합). R20은 client 적재 순서와 `[계약변경]` 공지·통합 확인만 남습니다.
>
> **v2.9 (9/8):** LLM 타임아웃 잠정값을 **8초에서 6초**로 조정(E-88)하고, server 문구 동기화 **R21**과 통합 환경 실측 항목 **#21**을 추가했습니다. AI 코드는 PR #9 `bc1d687`에 반영됐습니다.
>
> **v2.9 (9/8):** §9/2 항목 3 정정 — 카카오 리다이렉트 URI 등록 대상은 **2개**(로컬 · Vercel 운영 도메인)입니다. EC2는 서버라 대상이 아닙니다 (E-55 · 07 §10 리스크 3). server PR #8 리뷰에서 07 §3와의 충돌을 찾았습니다.
>
> **v2.8 (9/8):** 최근 대화 정렬 계약(E-87)을 세 코드 저장소에 반영하기 위한 **레포 조치 R20** 신설.
> AI는 PR #6으로 반영 완료했고, Server·Client 구현과 팀 채널 `[계약변경]` 공지·통합 확인은 남았습니다.
>

> **v2.7 (9/7):** 금융 RAG 개통(01 E-85 · E-86) — **레포 조치 R12 종결.** `V8__financial_chunks.sql`과 `DATABASE_URL` 기반 기동 배선이 들어갔고, 적재할 공개 문서 선정(FR-12-04)만 남습니다. 새 레포 조치는 없습니다.
>
> **v2.2 (9/7):** 회고 슬라이스를 서버에 붙이기 위해 **규칙 파라미터 잠정값 주입(E-57)** · 롤업 상위 키(E-59) · `analysisYearMonth` = 최근 거래월(E-60, **R11 종결**) · `POST /retrospects/chat` 본문(E-63) · `skip`은 알림 읽음으로 갈음(E-65) 종결 — §종결된 결정 6차. **레포 조치 R16** 신설. #15~#18 · #20은 "잠정값 주입 · 튜닝 대기"로 남습니다.
>
> **v2.1 (9/7):** 카카오 로그인 흐름(E-55) · 소셜 계정 식별과 이메일 nullable(E-56) 종결 — §종결된 결정 5차. `POST /auth/login` 계약 변경이라 **레포 조치 R15** 신설.
> 9/2 확정 항목 1(Boot 스파이크)은 E-55로 `oauth2Login`을 안 쓰게 되어 **대상이 바뀌었습니다** — `curl` 왕복 스파이크로 대체. 항목 3(리다이렉트 URI)에 카카오 콘솔 준비 4건을 붙였습니다.
>
> **v2.0 (9/7):** **#11 카드사별 컬럼 매핑표 폐기**(E-51) · `cardIssuer` 삭제(E-52) · 통장 카드결제 화이트리스트(E-53) ·
> 합계·요약 행 조용히 무시(E-54). `cardIssuer`는 세 레포 공유 enum이라 **레포 조치 R14** 신설.
> 카테고리 통합 매핑표는 남았습니다 — 지금 `category`는 전부 `기타`입니다.
>
> **v1.9 (9/3):** 회고 진입 경로 3종(E-48) · 후보 제외 미저장(E-49) · **시간대 4구간(E-50)** 종결 — §종결된 결정 4차.
> 시간대는 세 레포 공유 enum이라 **레포 조치 R13** 신설. 선별 규칙 수치는 **데이터 확보 후 #20**으로 넘겼습니다.
>
> **v1.8 (9/3):** AI `taskType` `FINANCE_QA` 신설(E-47) — 레포 조치 **R12**.
>
> **v1.6 (9/2 밤):** 레포 조치 **R1~R10 완료.** ai(R1~R6·R10)는 `66d1e62`, server(R7~R9)는 `f8e3067`에 포함. 세 레포 스캐폴딩 완료.
> 9/2 성공 기준 **`ai-ping` 200 실측 통과.** 남은 미결은 **작업 1건(매핑표) · 9/2 확정 2건 · 레포 조치 5건(R11~R15) · 데이터 확보 후 5건 · 발표 7건 · 동기화 2건**입니다.
>
> v1.3: 9/2 **정합성 감사 3차(문서 ↔ `sottaejap-ai` 레포)**. 팀 결정 **"레포 우선"** — 코드가 앞선 곳은 문서가 따라갔고(E-18~E-31),
> 문서가 양보하지 않은 5건은 **레포 조치 R1~R9**로 넘겼습니다.

---

## ✅ 종결된 결정

### 1차 (9/1 오전 — v1.1)

| # | 항목 | 확정값 |
|---|---|---|
| 1 | 규칙 엔진 위치 | ~~**FastAPI (Python)** — 선별 쿼리만 Spring(고현석)~~ → **v1.3 E-18로 Spring** |
| 2 | 통합 담당 | **고현석** |
| 3 | 팀장 | **오진호** |
| 4 | 익명화 스크립트 담당 | **정민규** |
| 5 | LLM 대체 문구 담당 | **오진호** |
| 6 | 3사 CSV 헤더 확보 담당 | **석정한** |
| 7 | 역할표 표기 | **석정한**이 맞음 |
| 9 | 회고 큐 소진 | 1일 1건 + **온보딩만 연속 진행** |
| 10 | 온보딩 표본 수 | **20건 내외** (26.06~07 추출, 26.08 보존) |
| 12 | 목적·동행인 선택지 | **초안 확정** (목적 7종 / 동행인 6종) |
| 13 | 회고 시점 | **D+1 유지** |
| 14 | 정민규 부하 분산 | **채택** |

### 2차 (9/1 오후 — v1.2 · 3원 정합성 감사)

| # | 항목 | 확정값 |
|---|---|---|
| E-1 | 만족도 지도 가로축 분자 | **분석 기준월 1개월 묶음 합계 ÷ 월 예산** (`avgAmount`는 절감액 전용) |
| E-2 | `verdict` enum | **`SUSTAIN`/`ADJUST`** (`quadrant.KEEP` 충돌 제거) |
| E-3 | 처방 문구 종수 | **5종** (PENDING 포함) |
| E-4 | 판정 표시 층위 | **판정 2색(점 색상) + 보류(회색 상태) / 처방 5문구(상세 패널)** 노드 분리 |
| E-5 | `Out` 표기 | **전면 폐기** → `PENDING`(더 알아볼게요) |
| E-6 | "ai 둘러대기" 표기 | **삭제** — `reasonCode` 재구성으로 통일 |
| E-7 | IA 번호 체계 | **탭 기준 재정비** (`3-1`홈 `3-2`채팅 `3-3`지도 `3-4`거래내역 / 채팅 하위 `3-2-x`) |
| E-8 | 소비 분석 화면 | **FR-11 신설 · P1 · S15 · `GET /analysis`** |
| E-9 | PWA · SNS 로그인 | **범위 내 확정** (FR-01-01 확장 / NFR-08 신설) |
| E-10 | 심야 배달 단가 | **12,000원** (데모 데이터 기준) |
| E-11 | 판정 라벨 구조 | **판정 `verdict` 2종 + 보류 `evaluationStatus` 분리** — 3라벨은 판정 2 + 상태 1의 혼재였음 |
| E-12 | `PROTECT` 행동 비대칭 | **배지는 `지켜요`로 동일 / 상세 패널에만 `예산 확보하기` CTA** (조정 제안 목록에는 미포함) |
| E-13 | 프론트엔드 스택 | **Vue 3 + Vite + Pinia** (PWA) |
| E-14 | 레포 구성 | **3개 레포 분리** — `sottaejap-client` / `sottaejap-server` / `sottaejap-ai` (E-28) |
| E-15 | SNS 로그인 | **카카오 OAuth 1종 실구현** + 데모 계정 폴백 |
| E-16 | pgvector | ~~**드롭** — 목적·동행인 정규화를 태그 임베딩 + 코사인 최근접(numpy)으로 결정론화~~ → **v1.3 E-20·E-21로 대체** |
| E-17 | 개발 환경 | **macOS + Windows 혼용 확정** — 인코딩·줄바꿈·도구 규칙 `07` §5 |

### 3차 (9/2 — v1.3 · 레포 우선 · 팀장 결정 4건 포함)

| # | 항목 | 확정값 |
|---|---|---|
| E-18 | 규칙 엔진 위치 | **Spring(Java) — 정민규 구현 · 고현석 통합·리뷰** ← 팀장 결정 |
| E-19 | 호출 방향 | Spring → AI `POST /chat` 1종 + AI → Spring `/internal/ai/*` 6종 (`/internal/*` 7종 폐기) |
| E-20 | 목적·동행인 | **AI 후보 제안 + 사용자 확인.** 저장값은 표준 태그만 ← 팀장 결정 (E-16 폐기) |
| E-21·22 | pgvector · 금융 RAG | **복원 · 범위 내 P2 — FR-12 신설** ← 팀장 결정. NFR-05 유지 |
| E-23 | 만족도 enum | **`HIGH/LOW/UNKNOWN` 3택 유지 — 레포 `MEDIUM` 삭제** ← 팀장 결정 |
| E-24 | 명명 | `/chat` 경계 snake_case · reflection · `ACTIVE` / 외부 camelCase 유지 |
| E-25 | LLM | **OpenAI `gpt-4o-mini`** 확정 |
| E-26~28 | AI 스택 · 브랜치 · 레포명 | Python 3.12+ · requirements.txt / `main/feature/*` / ~~`sottaejab-*`~~ → **`sottaejap-*` (v1.4 E-32)** |
| E-29~31 | 증명 테스트 · README · ⑦ 담당 | Spring JUnit / Spring README 첫 줄 / 정민규 |

### 4차 (9/3 — v1.8 AI taskType · v1.9 회고 진입 경로 · 시간대)

| # | 항목 | 확정값 |
|---|---|---|
| E-47 | AI `taskType` | **6종으로 확장 — `FINANCE_QA` 신설** (v1.8). `state`는 빈 객체, 출처는 `reply` 문장에 언급 → **R12** |
| E-48 | 회고 진입 경로 | **알림 1일 1건 · 채팅 오늘 포함 최근 3일 · 날짜 지정·거래내역 직접 선택은 제한 없음.** 오래된 거래를 빼는 상한 없음 (02 FR-03-03·08·09) |
| E-49 | 후보 제외(`skip`) | **상태를 저장하지 않는다.** 알림 읽음 처리로 갈음 — 별도 컬럼·테이블 없음 (04 스키마 변경 없음) |
| E-50 | 시간대 구간 | **4개** — `MORNING` 05~11 · `DAY` 11~17 · `EVENING` 17~22 · `NIGHT` 22~05. `AFTERNOON` 폐기 → **R13** |

### 5차 (9/7 — v2.1 카카오 로그인 흐름)

| # | 항목 | 확정값 |
|---|---|---|
| E-55 | 카카오 로그인 흐름 | **클라이언트가 인가 코드를 받고 서버가 카카오와 교환.** `POST /auth/login { provider: KAKAO, code, redirectUri }` → 서버가 토큰 교환 · 프로필 조회 · 우리 JWT 발급. Spring `oauth2Login` 미사용(STATELESS · 교차 도메인). 오류 코드 `OAUTH_CODE_INVALID` 400 · `OAUTH_PROVIDER_ERROR` 502 (05 §0) → **R15** |
| E-56 | 소셜 계정 식별 · 이메일 · 표시 이름 | **`(authProvider, providerUserId)` 유일 제약 · `email` nullable · `nickname` 신설.** 카카오 이메일은 선택 동의, 닉네임은 기본 동의라 프로필 표시는 닉네임이 맡음. 별도 회원가입 API 없음 — 첫 로그인에서 `users` 행 생성. server `V4` → **R15** |

### 6차 (9/7 — v2.2 회고 슬라이스 · 규칙 파라미터 잠정값)

| # | 항목 | 확정값 |
|---|---|---|
| E-57 | 규칙 파라미터 잠정값 (#15~#18 · #20) | **yml 기본값 주입** — `k=3` · 롤업 3 · 보류 3 · `Bx=0.1` · `By=0` · 민감도 3.0/2.0/1.5 · `candidate.*`(기준선 90일 · 표본 5 · 예산 비율 0.1 · 반복 LOW 2) · `cluster.meal-categories`. `RULES_*`로 덮어씀. 잠정 → **R16** |
| E-58 | 묶음 키 시간대 자리 | 식사 목록(`meal-categories`)이거나 **`기타`(미분류)면 시간대 포함.** CSV 3종이 전부 미분류라 없으면 키가 퇴화. 매핑표(#11) 반영 시 전체 재계산 1회 |
| E-59 | 롤업 상위 키 | `카테고리\|시간대\|\|`. 리프 회고 수 < 롤업 기준이면 `parentId`, 저장마다 재계산. 응답·`behaviorId`는 리프 |
| E-60 | `analysisYearMonth` | **사용자의 최근 거래월(KST).** `GET /users/me`도 같은 값 → **R11 종결** |
| E-61 | 재계산 범위 · null | 저장 시 **사용자 전체 묶음** + `avgSatisfaction` 재계산. 축소 추정 n = UNKNOWN 제외. 예산 없으면 `burdenRatio`·`quadrant` null, `verdict`는 세로축만 |
| E-62 | 후보 선별 세부 (#20) | D+1 · 회고된 거래 제외 · `THRESHOLD_EXCEEDED`(예산 비율) > `TIMESLOT_OUTLIER`(중앙값 × 배수) > `REPEATED_LOW_SATISFACTION`. **`reason`은 Spring 템플릿** — 05 §2 문구 변경(계약) |
| E-63 | `POST /retrospects/chat` | **상태 없는 프록시** — 클라이언트가 `step`·확정값을 들고 다님. 회고 행 미생성. 05 §2 신설 |
| E-64 | 묶음 명명 시점 | 커밋 후 `displayName` 없는 묶음만 `CLUSTER_NAMING`. 실패 시 템플릿. 한 번 정하면 유지 |
| E-65 | `skip`(05 #9) | 엔드포인트 없음. 알림 읽음 #20(이슈 #5)으로 갈음 |
| E-66 | 내부 `save_reflection` | `source` 기본 `CANDIDATE` · 응답은 외부와 같은 객체 · 본문은 `@JsonProperty` |

---

## ✅ 담당 지정 — 종결 (9/1)

| # | 항목 | 상태 |
|---|---|---|
| **19** | 발표 자료 담당 | ✅ **전원 공동 — 본선 현장 제작으로 확정** |
| **20** | 데모 시연 담당 | ✅ **본선 현장에서 결정** |

> ⚠️ **단, '사회적 파급효과'는 예외로 9/7 전에 초안을 만들어 두십시오.**
> 심사표 필수 항목이고 현재 0건입니다. 현장 30시간 안에서 백지부터 쓰면 개발 시간을 그만큼 잃습니다.

---

## 🟠 진행할 작업 — 1건 (+ 완료 2건)

### 0. 레포 3개 생성 · 부트스트랩
✅ **완료 (v1.6)** — `sottaejap-ai` `f9d3c78` (9/2 13:24) · `sottaejap-server` `f8e3067` · `sottaejap-client` `580f9ad`. GitHub `sottaejap-ai` rename 완료 (R10). `07_기술스택_레포구성.md` §11 체크리스트.
**9/2 성공 기준은 `ai-ping` 200 하나입니다.**

### 8. Spring ↔ FastAPI 연동 골격 (v1.3 규격 — 05 §3)
**v1.2의 `/internal/*` 7종은 폐기.** Spring `AiClient` → AI `POST /chat` 왕복 1건 + AI `SpringClient` → Spring `/internal/ai/*` 1건이 200이면 성공.
**양쪽 코드는 모두 있습니다 (v1.6)** — Spring `AiClient` · `/internal-test/ai-ping` · `/internal/ai/*` 6종(server `f8e3067`), AI `/chat` 시크릿 검사 · `SpringClient` 6종(ai R5). ✅ **9/2 밤 실측 통과** — `curl localhost:8080/internal-test/ai-ping` → `200 {"success":true,"data":{"reply":"최근 소비 하나를 함께 돌아볼까요?","fallback":true}}` (OpenAI 키 없음 · 공유 시크릿 양쪽 주입 · DB `pgvector:pg18`). 역방향 AI → Spring `/internal/ai/users/1/*` GET 5종도 200 (`transactions` · `reflections` · `analysis` · `suggestions` · `memory`).
남은 것은 팀 공유 시크릿 값을 양쪽 `.env`에 고정하는 일입니다. 두 쪽 다 시크릿이 비면 401입니다 (E-37).

### 11. 3사 카테고리 통합 매핑표
~~석정한이 헤더 확보 → 고현석이 매핑표 작성. 국민/신한은 별도 매핑.~~

**컬럼 매핑표는 폐기했습니다 (v2.0 · E-51).** 실제 파일 3종을 받아 보니 카드사가 같아도 서식이 다르고,
컬럼은 머리글 키워드로 찾는 편이 짧고 안 깨집니다 (04 §4). **카테고리 통합 매핑표는 아직 남았습니다** —
받은 파일 3종 모두 카테고리 칸이 비어 있어 근거가 없습니다. 그전까지 `category`는 전부 `기타`이고,
가맹점명 기반 추정도 하지 않습니다.
**v2.2 메모:** 묶음 키는 `기타`일 때 시간대를 포함합니다 (E-58). 매핑표를 반영하면 `category`가 바뀌어 묶음 키가 달라지므로, 반영 시 **전체 묶음 재계산을 1회** 돌려야 합니다 (회고 재저장 없이는 재계산이 일어나지 않음).

### 🔴 9/2까지 확정할 2건 + 완료 1건

1. ~~**Spring Boot 버전** — Java 21 + Boot 4.1.1로 카카오 OAuth **30분 스파이크** → 막히면 Boot 3.5.16으로 내리고 기록~~ → **v2.1: 대상 변경.** E-55로 `oauth2Login`을 쓰지 않으므로 Boot 버전은 로그인과 무관 — **4.1.1 유지**(E-36). 스파이크는 Spring 없이 **인가 코드 → `/oauth/token` → `/v2/user/me`** 왕복 1회 성공(07 §10 리스크 1). ✅ **9/7 실측 통과** — 토큰 200 · `user/me` 200 · 같은 코드 재교환은 **400 `invalid_grant` (KOE320)** → 05 `OAUTH_CODE_INVALID` 근거. 발견 3건은 05 §2 로그인 흐름 아래 주석에 기록
2. ~~LLM · 임베딩 제공자~~ → ✅ **OpenAI `gpt-4o-mini` 확정 (E-25).** 키 발급만 남음(오진호). 임베딩 모델은 금융 RAG(P2) 착수 시
3. **EC2 도메인 확보** → 카카오 리다이렉트 URI ~~3개(로컬 · EC2 · Vercel)~~ 사전 등록. 본선 당일 도메인이 바뀌면 로그인이 죽습니다.
   **v2.1 (E-55):** 리다이렉트 URI는 **클라이언트 콜백**입니다 — 로컬 `http://localhost:5173/auth/callback`은 지금 등록, ~~EC2·Vercel은~~ **Vercel 운영 도메인은** 도메인 확보 후 추가. 카카오 콘솔 준비 4건을 같이 끝냅니다: ① 앱 생성 → REST API 키 ② 카카오 로그인 활성화 ③ Client Secret 생성 후 "사용함" ④ 동의항목 **닉네임(필수) · 이메일(선택)**. 키·시크릿은 `.env`에만 (`KAKAO_CLIENT_ID` · `KAKAO_CLIENT_SECRET` · `KAKAO_REDIRECT_URIS` — 07 §3). ☐
   **v2.9 (9/8) 정정:** 콘솔 등록 대상은 **2개**입니다 — 로컬 `http://localhost:5173/auth/callback`과 Vercel 운영 도메인의 `/auth/callback`. EC2는 서버라 리다이렉트 URI가 아닙니다 (07 §10 리스크 3). EC2 도메인 확보 자체는 클라이언트의 `VITE_API_BASE_URL`과 server `AUTH_ALLOWED_ORIGINS` 때문에 그대로 필요합니다.

---

## 🔧 레포 조치 — 26건 (v1.6: **10건 완료** · R12~R16 남음, R11은 v2.2 종결 · **v2.5: R17 · R19 신설** · **v2.6: R18 신설** · **v2.7: R12 종결** · **v2.8: R20 신설** · **v2.9: R21 신설** · **v2.12: R18 종결** · **v2.13: R23 신설** · **v2.15: R22 범위 확대** · **v2.16: R24 신설** · **v2.17: R22 종결** · **v2.20: R24 배치 자리 채번** · **v2.21: R25 신설** · **v2.22: R28 신설** · **v2.23: R24 종결** · **v2.35: R19 종결** · **v2.38: R34 신설** · **v2.46: R34 server 종결**)

> 문서를 레포에 맞췄지만, 아래는 **레포가 문서에 맞춰야** 합니다. 팀장 결정(9/2)과 심사·규제 방어선입니다.

| # | 레포 | 조치 | 담당 | 근거 | 상태 |
|---|---|---|---|---|---|
| R1 | ai | `Satisfaction`에서 **`MEDIUM` 삭제** → `HIGH/LOW/UNKNOWN` (`app/reflection/schemas.py`) | 오진호 | 팀장 결정 · E-23 | ✅ v1.6 |
| R2 | ai | `ReflectionExtraction.purpose/companion`을 **표준 태그 enum(7/6) + `None`** 으로 좁힘. 자유 문자열 금지 | 오진호 | 팀장 결정 · E-20 | ✅ v1.6 |
| R3 | ai | **LLM 폴백** — `app/ai/fallback.py` 템플릿 + `LLM_TIMEOUT_SECONDS=6` + `ChatResponse.fallback` | 오진호 | FR-04-15 **P0** · NFR-04 · E-88 | ✅ v1.6 · 타임아웃 값은 ai PR #9 `bc1d687`에서 갱신 |
| R4 | ai | `TaskType`에 `CLUSTER_NAMING`·`ANALYSIS_NARRATE` 추가 (⑤⑨ 호출 경로) | 오진호 | 05 §3 | ✅ v1.6 |
| R5 | ai | `SpringClient` 6개 메서드에 05 §3 경로 연결 · `X-Internal-Secret` 헤더 · `INTERNAL_SHARED_SECRET` | 오진호 · 고현석 | 05 §3 · E-19 | ✅ v1.6 |
| R6 | ai | `.gitattributes` · `PYTHONUTF8=1` · `requirements.lock` · `Dockerfile` | 오진호 | E-17 · A-4 · 07 §11 | ✅ v1.6 |
| R7 | server | `rules/` 패키지 + `RuleParams` + **`RuleEngineDeterminismTest`** | 정민규 | E-18 · E-29 | ✅ `f8e3067` |
| R8 | server | `AiClient`(snake_case 변환) · `/internal-test/ai-ping` · `/internal/ai/*` 6종 · README 첫 줄 | 고현석 | E-19 · E-30 | ✅ `f8e3067` |
| R9 | server | `docker-compose` 이미지 `pgvector/pgvector:pg18` (확장 설치는 `V8`) | 고현석 | E-21 | ✅ `f8e3067` |
| R10 | ai | GitHub 레포 `sottaejab-ai` → `sottaejap-ai` rename · 팀원 `git remote set-url origin` 공지 (v1.4) | 오진호 | E-32 | ✅ v1.6 |
| R11 | docs | `GET /users/me`의 `analysisYearMonth` 산출 규칙 결정 (04 User에 필드 없음 — 최근 거래월 / 사용자 설정 중 택1) 후 04·05 반영 (v1.5) | 고현석 · 석정한 | 05 §2 갭 | ✅ v2.2 — **최근 거래월** (E-60). 서버 반영은 R16 |
| R12 | ai | `TaskType`에 `FINANCE_QA` 추가(`app/schemas/common.py`) + `agent.py` 분기 로직 · `financial_rag_tool` 연결 구현 (v1.8) | 오진호 | E-47 · E-85 · E-86 | ✅ **demo 9/7 (#20)** — server `V8__financial_chunks.sql` · ai `main.py` lifespan이 `DATABASE_URL`이 있을 때만 풀을 열어 `FINANCIAL_RAG`를 등록. 없으면 기동은 되고 금융 질문만 "확인할 수 없다". `POST /chat/finance` 왕복 200 실측. **문서 적재(FR-12-04)는 남음** · ✅는 demo 기준이다 — **정본 레포 이식은 server 절반 ✅ PR #22(`b118e1e` 병합 · EC2 배포 성공) · ai 절반은 R23** |
| R13 | server · client · ai | **`timeSlot` 4종 반영 (계약 변경).** server: `TimeSlot` enum + `V2` 마이그레이션(`time_slot` `CHECK` 제약 + 저장된 `AFTERNOON` 값 이관) + 파서 분류 경계 / client: `src/api/enums.ts` / ai: 시간대 문구·프롬프트 | 고현석(통합) · 정민규(경계) · 석정한(client) · 오진호(ai) | E-50 · 05 §0 · 07 §6 | ☐ — **server ✅ PR #2**(`7c62fa4` 병합) `TimeSlot` 4종 · V2(옛 CHECK drop 후 전 행 재계산 · `FlywayUpgradeFromV1Test`) · client ☐ · ai ☐ |
| R14 | server · client | **`cardIssuer` 삭제 (계약 변경).** server: 엔티티·업로드 API 파라미터 제거 + `V3` 마이그레이션(`transactions.card_issuer` 컬럼 DROP) / client: `src/api/enums.ts`와 업로드 화면의 카드사 선택 제거 | 고현석(통합) · 석정한(client) | E-52 · 05 §0 · 05 §2 · 07 §6 | ☐ — **server ✅ PR #2**(`7c62fa4` 병합) `cardIssuer` 파라미터 제거 · V3 컬럼 DROP · `CardIssuer` enum 삭제 · client ☐ |
| R15 | server · client | **카카오 로그인 E-55 · E-56 반영 (계약 변경).** server: ✅ 9/7 `V4` 마이그레이션(`users.email` NOT NULL 해제 · `uq_users_email` 삭제 · `users.nickname` 추가 + 데모 계정 `데모 사용자` — 부분 유일 인덱스는 V1에 이미 있음) + `User` 엔티티 동기화 + `UserMeResponse.nickname` + `UserRepository.findByAuthProviderAndProviderUserId` / ✅ 9/7 `auth/kakao/KakaoOAuthClient`(`RestClient` — 토큰 교환 · 프로필 조회 · `KakaoProperties` · `AuthErrorCode` OAUTH 2종 · yml `auth.kakao.*`) / ✅ 9/7 `LoginRequest`에 `code` · `redirectUri` + `AuthServiceImpl` `KAKAO` 분기(조회 → 없으면 `User.social` 생성 → JWT) — **실제 카카오 계정으로 `/auth/login` 200 · `/users/me` nickname · 코드 재사용 400 실측** / ✅ `AuthErrorCode` 2종 + `AuthControllerTest`(본문 매핑 · 봉투 · 오류 코드 8건) / ✅ `application.yml` 주석 블록 → `auth.kakao.*` / ✅ `.env.example`(server · 루트) `KAKAO_REDIRECT_URIS` + compose 배선 / ✅ `spring-boot-starter-security-oauth2-client` 제거 / ✅ server `AGENTS.md`에 "카카오는 서버 리다이렉트가 아니다" 전제 추가. **server 쪽 R15 완료 (9/7).** client(본선): S7 카카오 버튼 + `/auth/callback`(`state` 검증) → `POST /auth/login` → `GET /users/me` 분기 (v2.1) | 고현석(server) · 석정한(client) | E-55 · E-56 · 05 §0·§2 · 07 §3·§6 | ☐ — **server ✅ PR #4**(`e73b4fb` 병합) `auth/kakao/` · V4 · `KAKAO_REDIRECT_URIS` · 리뷰 반영 2건(동시 첫 로그인 idempotent · `OAUTH_CODE_INVALID` 400) · client ☐ |
| R16 | server | **회고 슬라이스 (E-57~E-66).** ① `rules/` 순수 함수: `cluster/`(묶음 키 · 롤업 · `ClusterEngine`) · `shrinkage/` · `verdict/` · `candidate/`(⓪) + `RuleParams` 확장 + yml 잠정값 + 결정론 골든 테스트 ② `retrospect/` — `Retrospect` · `BehaviorCluster` 엔티티 · `GET /retrospects/candidates` · `POST /retrospects` · `POST /retrospects/chat` · `Transaction.behaviorId` 배정 · `User.avgSatisfaction` 갱신 ③ 내부 AI `reflections` GET/POST · `memory` 실구현 ④ `GET /users/me`의 `analysisYearMonth`. V5 없음. `skip`은 두지 않음(E-65). 연습: `sottaejap-demo` PR 2건(규칙 엔진 → API) + `retrospect-playbook.md` | 정민규(구현) · 고현석(리뷰) | E-57~E-66 · 05 §2·§3 · 04 §3 · 07 §7 | ✅ **demo 9/7** — 규칙 엔진 PR #12(①) · API 슬라이스 PR(②③④) 완료, DB 포함 223건 통과, 실제 형식 CSV로 후보 → 저장 ×3(PENDING → RESOLVED) → 409 → 내부 AI → chat INTRO 실측. **본선 `sottaejap-server` ✅ PR #6**(`32f1999`, 규칙 엔진 ①) **· PR #10**(`4fbfc47`, API ②③④) — 리뷰 반영 포함 — `retrospect-playbook.md` |
| R17 | server · client | **분석 슬라이스 (E-72~E-78 · E-80).** server: `rules/aggregate/`(순수 집계 · 지도 정렬) · `analysis/`(`GET /satisfaction-map` · `GET /analysis` · `GET /behaviors` 2종 · 처방 5종 · CTA 3종 · '나만의 특징' 폴백) · 내부 AI `analysis` 실구현 · `PUT /users/me/settings`(예산 변경 시 재계산) · `analysisYearMonth` 단일 산출 / client: `ctaType` 2종 · **`TIME_SLOT` 4종(R13 동반)** · 예산 미설정 nullable 계약 반영 | 정민규(구현) · 고현석(리뷰) | E-72~E-80 · 05 §0·§2·§3 | ☐ — **server ✅ PR #16**(`128636e` 병합) `rules/aggregate/` · `analysis/` 4 API · `PUT /users/me/settings` · 내부 AI `analysis` · 리뷰 반영 2건(state 중첩 키 snake_case · `NO_RETROSPECT` 조건) · client ☐(`MapView` 목 데이터) |
| R18 | server | **제안 · 목표 슬라이스 (E-81~E-84).** `rules/saving/`(절감액 · 제안 대상 · 정렬 · 목표 달성률, 순수 함수) · `suggestion/`(재계산 훅 동기화 · `GET /suggestions` · `adopt`/`reject` · 내부 AI `suggestions`) · `goal/`(`GET`·`POST`·`PUT`·`DELETE /goals` · 채택 제안 합산) · `V7__suggestion_indexes.sql`(부분 유일 인덱스 포함). 제안 발생 알림(FR-08-05)은 범위 밖 | 정민규(구현) · 고현석(리뷰) | E-81~E-84 · 05 §0·§2·§3 · 04 §3 | ✅ **server PR #18**(`675f660` 병합) — `rules/saving/` 4종 · `suggestion/` · `goal/` 4종 · V7 · 내부 AI `suggestions`. 399건 통과. 리뷰 6건 반영(재채택 `goalId` 생략은 유지 · `PUT /goals/{id}` 전체 교체 → 05 v2.12). `reason` 문구의 `은(는)`은 후속 이슈 #20 → ✅ **PR #44**(`5b1a3eb`, v2.31) — 이름 뒤에는 `의` · `에`만 · `truncate` 코드 포인트 |
| R19 | ai | **`analysis_narrate` 숫자 가드 보강 (E-79 · E-101).** 소수를 한 수로 파싱해 `share × 100`과 견주고(`3.6%` 통과 · `9.9%` 거절), 가드가 걸리면 `fallback=True`를 세워 Spring이 자기 템플릿으로 갈음할 수 있게 한다. `share`의 분모가 월 예산임을 프롬프트에 명시한다. **리뷰 보강:** 정상 `analysis_year_month`의 연도·월을 허용 숫자에 넣고, 한 자릿수 정수라도 바로 뒤에 `%`가 붙으면 반드시 근거와 대조한다(`1번째` 예외는 유지) | 오진호 | E-79 · E-101 · 05 §3 | ✅ **ai PR #43**(`be82917` 병합 9/9, 이슈 #41 종료) — CI · Deploy success · `pytest` 167건 통과. 리뷰 3라운드 반영: 정상 `YYYY-MM`의 연·월 허용(잘못된 형식은 거절) · `%`가 붙으면 한 자릿수도 검사(`1번째` 예외 유지) · 퍼센트는 `share × 100` 전용 집합과만 대조 · `%` 앞 공백 우회 차단. 남은 것: 실사용 폴백 비율 관찰(가드가 거절한 사용자는 캐시가 안 되어 매 진입 정상 LLM 왕복 한 번을 더 치른다 — R33 · server #49). server 주석 정정 #47은 ✅ PR #48(`d3709f7`, v2.40) |
| R20 | server · client | **`recent_messages` 오름차순 반영 (계약 변경).** server: `chat_messages` 최근 6건을 오래된 → 최신 순서로 전달(`FINANCE_QA`) · `POST /retrospects/chat`의 Client 배열 순서 보존 / client: `recentMessages`를 오래된 발화부터 적재 / 공통: 팀 채널 `[계약변경]` 공지 · 통합 담당의 세 레포 반영 확인. ai는 PR #6으로 반영 완료 | 고현석(server · 통합) · 석정한(client) | E-87 · 05 §2·§3 · 07 §6 | ☐ — ai ✅ · **server ✅ PR #12**(`02c005f` 병합) · client 미반영 · 공지/통합 확인 필요 |
| R21 | server | server `AGENTS.md`의 **“AI 내부 LLM 8초”** 문구를 **6초**로 갱신. `.env.example`에도 같은 문구가 있었다 | 고현석(server) | E-88 · 05 §3 · 07 §10 리스크 4 | ✅ **PR #12**(`02c005f` 병합) — `AGENTS.md` · `.env.example` 두 줄. `AI_TIMEOUT_MS`는 15000 그대로 |
| R22 | server | **`highlight` 템플릿 4종째 (E-89) + AI 미호출 조건 확대 (E-91).** ① `HighlightTemplate`에 "유효 묶음 ≥ 1 ∧ `byCategory` 비어 있음" 분기와 잠정 문구 추가 ② `AnalysisServiceImpl.highlight`의 AI 미호출 조건에 `byCategory` 비어 있음을 더한다 — ①만 하면 키가 있는 환경에서 새 문장이 나가지 않는다 ③ 테스트: `HighlightTemplateTest`(분기) · `AnalysisServiceImplTest`(AI 미호출) · `HighlightServiceImplTest`(폴백 경로). 문구는 문구 담당 확인 후 확정 | 고현석(server) | E-89 · E-91 · E-75 · server PR #16 리뷰 #2 · PR #25 리뷰 #1 | ✅ **PR #25**(`33f218b` 병합, 이슈 #24 종료) — 401건 통과 · 건너뜀 0. 문구는 **E-89 확정**(v2.39) — server #51 · **PR #53 `a80ccfe` 병합 · Deploy success**(v2.43). ai 쪽 안내문은 ai #50 |
| R24 | server | **CSV 업로드 뒤 묶음 재계산 (E-95).** 거래를 넣는 유일한 경로 `TransactionServiceImpl.upload`가 `recomputeAll`을 부르지 않는다. `analysisYearMonth`는 실시간(E-78)이고 `monthlyTotalAmount` · `burdenRatio` · `quadrant`는 행에 저장된 값(E-61)이라, 새 달 CSV를 올리면 **새 기준월 이름표에 지난달 금액**이 붙는다(S2 흐름 — 온보딩은 순서가 반대라 걸리지 않는다). **배치 자리는 파사드 빈으로 채번했다** — `transaction/service/TransactionUploadFacade`가 `upload` 커밋 뒤 `recomputeAll`을 별도 트랜잭션으로 부르고, 재계산이 실패해도 업로드는 200이며 WARN만 남긴다. **건너뛰는 조건은 "회고 0건"이고 판단은 재계산 안에서 한다**(E-96 — `importedCount`로 판단하지 않는다). `upload`에 `ClusterRecomputeService`를 직접 주입하면 순환 참조로 기동이 죽는다(`spring.main.allow-circular-references` 기본값 false). 05 §2에 동작을 명시했다(v2.15 · v2.17). 테스트는 통합 1건(7월 회고 → 8월 CSV 업로드 → `GET /analysis`가 새 기준월 금액)과 파사드 단위(재계산 실패 삼킴 · `importedCount` 0건이어도 재계산 호출 · 트랜잭션 경계)다 | 고현석(server) | E-60 · E-61 · E-78 · **E-95** · server PR #25 재검증 · **이슈 #27** | ✅ **PR #31**(`6aead02` 병합, 이슈 #27 종료) — 408건 통과 · 건너뜀 0. 리뷰 2라운드 반영: E-96(건너뜀 조건 교체) · E-97(제안 목록 명시) · jakarta 애너테이션까지 검사 · 온보딩 경로 정정. **남은 것은 R28**(업로드 응답 시간)이고 이 조치와 별개다 |
| R23 | ai | **금융 RAG의 `ai` 절반을 정본 레포로 이식 (R12의 나머지).** `app/rag/retriever.py`를 pgVector 실구현으로 (`embedding <=> $1::vector` Top-K · `RetrieverUnavailableError`) · `app/rag/embedding.py`에 `EMBEDDING_DIMENSIONS = 1536`과 `text-embedding-3-small` · `scripts/ingest_financial_docs.py`에 `ON CONFLICT (chunk_id) DO UPDATE` 적재 · `main.py` lifespan이 `DATABASE_URL`이 있을 때만 풀을 열고 `FINANCIAL_RAG`를 등록 · **`app/rag/schemas.py`의 `source`를 `str | None`에서 필수로** (04 §1 v2.9 — DB가 `NOT NULL`이라 지금 상태로 적재하면 23502가 난다) | 오진호 | E-85 · E-86 · 04 §1 v2.9 · server PR #22 리뷰 | ☐ — demo `#20`에는 있고 정본 레포에는 없다. server 절반은 PR #22 |
| R25 | server | **온보딩 슬라이스 (E-92).** `onboarding/` 패키지 — `POST /onboarding/start`(기간 안 미회고 거래를 균등 간격으로 `sampleSize`건, `reasonCode = ONBOARDING_SAMPLE`, 응답은 후보 API와 같은 `candidates` 배열) · `POST /onboarding/complete`(`onboarding_completed` 저장 + `recomputeAll` + 유효 묶음 수를 `clusterCount`로). 마이그레이션 없음(컬럼은 V1에 있음) · 표본 저장 없음(E-49 · E-65) · 클라이언트는 `startOnboarding` 응답을 쓰도록 한 줄 고쳐야 함(client 이슈 #3) | 오진호 | E-92 · 05 §1 #18·#21 · 05 §2 · 02 FR-09-01,02 · server 이슈 #28 | ✅ **PR #35**(`b192382` 병합, 9/9 10:51) — `onboarding/` · `POST /onboarding/start`(균등 간격 표본 · `ONBOARDING_SAMPLE`) · `complete`(플래그 · 재계산 · `clusterCount`). 리뷰에서 E-92 ③ 편향 잡아 규칙 교체(docs `883d4db`). client 배선은 client #3 |
| R26 | server | **거래 목록 `GET /transactions` (E-93).** `TransactionRepository.search`를 `Page`로(내부 AI `findForAi`는 `.getContent()`), `TransactionListQuery` · `TransactionView` · `TransactionListResponse`(회고 요약 `retrospectId` · `satisfaction`) · `hasRetrospect` 필터 · `TransactionService.list` · 컨트롤러 · 테스트(봉투 · 필터 · 페이징 · 400 · 남의 거래 제외). R24(#27)가 끝났으니 바로 시작할 수 있다. **컨트롤러가 둘을 주입한다** — 쓰기(업로드)는 `TransactionUploadFacade`, 조회(`list`)는 `TransactionService` 직접이다(PR #31 리뷰 합의). 조회를 파사드에 통과시키면 아무 일도 하지 않는 위임 메서드가 생긴다. `TransactionService.upload`의 javadoc이 "재계산은 여기서 하지 않는다"를 지키고 있으니 새 쓰기를 붙일 때 그 자리를 보면 된다 **PR #37 리뷰에서 정한 것(9/9):** ① `category`는 400을 걸지 않는다 — §0에 enum이 없고 저장값이 카드사 원본이다(05 v2.21 · R29). ② client `TransactionsView`의 **정렬(최신순 · 오래된순) · 검색(가맹점 부분일치) 컨트롤은 API에 더하지 않고 client에서 뺀다**(리뷰 (가) 안). 03 S12는 "목록·필터링"까지이고, 서버 페이징이 붙으면 둘 다 현재 페이지 20건 안에서만 동작해 조용히 틀린 화면이 된다. `q`는 `LIKE %…%`라 인덱스도 못 탄다. 필요해지면 05 §2 "정렬 파라미터는 없습니다"와 E-93을 먼저 고치는 별도 이슈다(R30) | 고현석(server) | E-93 · 05 §2 · FR-02-02 · FR-03-07 | ✅ — server PR #37 `e6356ec` 병합(9/9), 이슈 #29 종료. client 배선은 R30(#15) |
| R27 | server | **월간 리포트 (E-94).** `MonthlySnapshot` 엔티티 · 리포지토리(`uq_monthly_snapshots`) · `rules/`의 `MonthlyDeltaRule`(순수, 현재 연월은 인자) · `MonthlySnapshotService`(지난달 첫 조회 확정 upsert · 이번 달 미저장 · 확정 시 `Goal.currentAmount` 배분, 한 트랜잭션) · `GET /reports/monthly` · 테스트(전월 없음 · 미래 달 400 · 음수 · 예산 null · 확정 후 불변 · 배분 멱등 + DB 통합) | 고현석(server) | E-94 · 04 §3 · 05 §2 · FR-08-06 P0 | ✅ **PR #32**(`4d0be5d` 병합, 이슈 #30 종료) — 454건 통과 · 건너뜀 0. 리뷰 2라운드 반영: 실적은 **직전 달** 확정에서만 · `Goal.currentAmount`는 DB 원자적 증가 · 확정된 달은 전월 값도 굳음(`savedAmount` 역산 · 전월 스냅샷 또는 null, 전월 없음이면 둘 다 null) · 첫 거래월 이전 미저장. 후속 server #36(`PUT /goals` 경합)은 **PR #42**(`4195272`) 병합으로 종결 — `Goal` · `User` · `Suggestion` · `BehaviorCluster`에 `@DynamicUpdate`(E-100) |
| R28 | server · client | **업로드 응답 시간과 client 20초 타임아웃.** client가 `src/api/httpClient.ts`에서 모든 요청에 `timeout: 20_000`을 공통으로 걸고 업로드만 늘리지 않았다. 업로드 응답은 파싱 + 저장이 지배하고 **E-95가 더한 재계산은 9~93ms로 전체의 2~3%**다(실측 — CSV 1,200행 341~702ms · 17,000행 4,785ms · `recomputeAll` 9~12ms). 다만 `multipart.max-file-size` 상한 10MB에 가까운 파일은 20초를 넘긴다 — **E-95 전에도 같았던 위험이다.** 타임아웃이 먼저 끊기면 서버는 성공했는데 화면만 실패로 보인다. 대응은 업로드 행수 상한이나 client 업로드 타임아웃 분리 중 하나 | 고현석(server) · 석정한(client) | E-95 실측 · server PR #31 리뷰 5번 · 05 §2 | ✅ **행수 상한 20,000행**(v2.39 · E-105) — server #52 · **PR #54 `cbecb86` 병합 · Deploy success**(v2.45). 파서가 머리글 직후 레코드 수를 세어 넘으면 400 `TOO_MANY_ROWS` · 한 건도 저장하지 않는다. client 타임아웃 분리는 하지 않는다. **조건 없음(v2.49):** ~~운영 힙 1GB 이상~~ — 힙 조건을 없앴다. XLSX를 **시트를 열기 전에** 세도록 바꿔(01 **E-111** · server **#58**) 힙 256m에서도 0.2초에 400이다. 운영 힙은 `-Xmx512m` · 컨테이너 상한 `mem_limit: 1g`으로 명시했다(07 §12). 화면 `message` 표시는 **client #24 ✅ PR #25 `5b706b0` 병합**(v2.51 · 05 v2.35) |
| R29 | server | **04 §4의 `category` / `sourceCategory` 갭.** 04 §3 표는 `category`=내부 통합 카테고리 · `sourceCategory`=카드사 원본으로 나눠 두었는데, `TransactionServiceImpl`은 `Transaction.of(..., category(row), row.sourceCategory(), ...)`로 **둘 다 원본**을 넣는다(길이 상한만 50 / 100). 업로드 슬라이스 때부터의 상태이고 PR #37이 만든 것이 아니다. 통합 매핑표(#11 · FR-02-03)가 정해지면 `category()`에서 변환하고, 그때 05 §2 `category`를 enum으로 되돌린다(05 v2.21). **`category()`만 고치면 절반이다** — 그 메서드는 업로드 경로라 앞으로 올라오는 거래에만 적용된다. 반드시 함께 한다: ① 이미 `기타`로 들어간 **기존 `transactions.category`를 `source_category`에서 백필**(Flyway 마이그레이션 1회), ② 백필 뒤 **전체 묶음 재계산 1회**(§11 v2.2 메모 · E-58 — `기타`가 실제 카테고리로 바뀌면 `clusterKey`의 카테고리 · 시간대 두 자리가 함께 움직여 지도의 점이 옛 키에 남는다). 그 전까지는 04 §4 문구를 "매핑표 전까지 둘 다 원본"으로 맞춘다 | 고현석(server) | 04 §3 · §4 · 05 v2.21 · server PR #37 리뷰 축 1 | ☐ — 매핑표(#11)에 묶인다 |
| R30 | client | **거래내역 화면(03 S12 · S9)을 `GET /transactions`에 배선.** `TransactionsView.vue`는 목업이다. 배선할 때 반드시 걸리는 것 4개 — ① 정렬 · 검색 컨트롤 삭제(R26 ②), ② `cardIssuer` 잔재 삭제(`issuerLabels` · `issuer: KB as CardIssuer` — E-52 · server V3로 컬럼도 없다. 이 API는 카드사를 줄 수 없다), ③ 마지막 페이지를 넘겨 부르면 서버가 `page: 99 · totalPages: 1`을 그대로 돌려주므로 페이지네이터는 `page`를 `totalPages`로 클램프, ④ 항목 매핑 `at` → `occurredAt`(ISO +09:00) · `retrospected` → `retrospectId` · `satisfaction` · `issuer` → 없음. 카테고리 드롭다운은 현재 페이지에서 유도할 수 없고 저장값이 원본 문자열이라(데모 CSV는 전부 `기타`) **매핑표(#11) 전까지 숨기는 것을 권고**한다. 회고 이력 탭(S9)은 `hasRetrospect=true` | 석정한(client) | 03 S12 · S9 · 05 §2 · E-52 · E-93 · server PR #37 리뷰 축 2 · 3 · 6 | ☐ — client 이슈 #15 |
| R32 | ai | **`ACTION_PLAN` 응답 톤과 "제안 없음" 안내문 (E-103 후속 · `sottaejap-ai#40`).** ① 이유 문장이 반말로 나온다 — 이 저장소의 다른 사용자 문구와 톤이 다르다. ② `handlers/action_plan.py`의 `NO_MATCHING_SUGGESTION_REPLY`("해당하는 제안을 찾을 수 없어요…")는 Spring 조회가 성공한 뒤 나오므로 `tool_results[].success = true`이고 `fallback`도 없어 **server가 설명으로 오인해 저장할 수 있다** — 안내문에는 `fallback: true`를 세운다. 지금은 server의 두 번째 조회 필터가 막아 주지만 계약으로 보장되지 않는다. **ai#40 배포 전에 병합된 제안 이유는 반말로 남으므로, 배포 뒤 `UPDATE suggestions SET reason = NULL` 1회로 다시 짓게 한다** — 클라이언트 S5가 아직 `reason`을 그리지 않아 당장 노출은 없다. **server PR #46은 `0f507da`로 9/9 병합됐다(v2.36)** — 반말 문장이 쌓이기 시작한 시점이다 | 오진호 | E-103 · 05 §3 · server PR #46 리뷰 5·6 | ☐ |
| R31 | client | **표준 태그의 표시 라벨과 전송 값을 나눈다 (E-99).** `ChatView.vue:157-163` · `OnboardingView.vue:193-194`가 화면 라벨과 서버 전송 값에 같은 문자열을 쓴다. 라벨은 `"휴식 · 취미"`, 정본은 `"휴식·취미"`다. 온보딩 저장 400의 원인이었고 **서버가 받는 자리에서 막아 저장은 지금 된다**(server PR #41). 남은 위험은 `POST /retrospects/chat` 배선이다 — 서버가 `reflection.purpose`를 정본으로 돌려주는데 client 목록에는 공백형만 있어 **선택 칩이 눌린 상태로 잡히지 않고 `step`이 같은 질문을 되묻는다. 400이 아니라 조용히 틀린 화면이라 저장 실패보다 늦게 발견된다.** 처방은 `{ value, label }` 분리와 두 화면 공용 상수 한 곳이다. **화면 문구는 바뀌지 않는다.** 저장된 행은 전부 정본이라(공백형은 애초에 400) 보정은 없다 | 석정한(client) | E-99 · 05 §2 · server PR #41 리뷰 | ✅ **client #16 → PR #26 `6d8c95a` 병합**(v2.53). 값은 `enums.ts`, 라벨 쌍은 `components/chat/tagOptions.ts`. 리뷰 후속으로 `saveRetrospect` 타입 좁힘 · 만족도 3택 분리까지 같이 닫혔다. 반복 의향은 **client #30 → PR #31 `1d9e866` 병합**(v2.55). 4종 전부 라벨·값 분리 완료 |
| R34 | server · client · ai | **채팅 소비 분석 자유 질문 — `POST /chat/analysis` (E-104 · FR-11-05 P2).** server: `AnalysisChatController` · `AnalysisChatService`(`FinanceChatServiceImpl`과 같은 모양, 저장 없음) · `state = { analysis_year_month }` · 최근 대화 6건 · 테스트. client: `ChatView.vue:413`의 고정 문장을 이 호출로 교체, 채널 이력을 `recentMessages`로 전송. ai: `handlers/analysis.py`에 `ANALYSIS_NARRATE`의 숫자 가드(E-79 · E-101)를 적용 — 지금은 집계 밖 수치가 그대로 나간다(NFR-02) | 정민규(server) · client 담당 · 오진호(ai) | E-104 · 05 §2 #28 · 05 §3 | ☐ — 정본 01 v2.29 · 05 v2.28 · 02 v2.13 반영(`0ef6203`). **server ✅ PR #55**(`79f6123` 병합 9/9, 이슈 #50 종료, CI · Deploy success, 리뷰어 실측 AI 왕복 확인). **client ☐ #22** · **ai ☐ #48**(+ 응답 반말 — `ANALYSIS_INSTRUCTION` 해요체). 파생: **server #56 ✅ PR #59**(`a8ca135` 병합 9/9, E-109 · 05 v2.33, `recentMessages` 규격 밖 값 → 400, 두 채널 공통) → 2차 파생 **server #61 ✅ PR #62**(`bf5efd8` 병합 9/9, E-110 `assistant` 2,000자 · 코드 포인트 계수) · **ai #55 ☐**(`reply` 2,000자 보장 + 예산 재측정) · **server #60 · #66 → server #67로 통합 ✅ PR #68**(`7e6cfee` 병합 9/9, E-112 `message` 500자 UTF-16 · 업로드 truncate 코드 포인트 · `PendingSummary` javadoc, v2.52 · v2.56). client #22에는 배선 규칙 3건이 코멘트로 얹혀 있다(6건 절단 후 전송 · 자체 검증 · role별 상한). 순서: ai #48 → client #22 (v2.46 · v2.47) |
| R33 | server | **`highlight` 캐시가 가드레일 폴백을 매 진입 다시 부른다 (E-102 후속 · server #49).** E-102는 `fallback: true`를 캐시하지 않는 근거를 "AI가 돌아오면 다시 부른다"(E-38)로 적었다 — 폴백을 일시 장애로만 본 것이다. ai PR #43의 숫자 가드(E-79 · E-101)가 두 번째 원인을 만들었다: LLM은 정상인데 근거 밖 수치를 잡아 200 · `fallback: true`를 돌려주고, 같은 집계에서 다시 뽑아도 또 걸릴 수 있다(E-79의 `9.9%`). 그 사용자는 `GET /analysis`마다 **정상 LLM 왕복 한 번**을 치른 뒤 템플릿을 본다 — 캐시가 없애려던 비용이 그 사용자에게만 남고, **적중률은 가드레일 통과율만큼 깎인다**. `AI_TIMEOUT_MS`(15초)는 이 경로에 걸리지 않는다. 선택지 — **A** 폴백일 때 서버 템플릿을 같은 키로 캐시(값싸지만 LLM 장애 폴백까지 캐시돼 복구 뒤에도 집계가 바뀔 때까지 템플릿이 남는다 — TTL이나 배포 시 비움으로 상한) · **B** 05 §3 응답에 폴백 원인 필드를 더해 가드레일 폴백만 캐시(계약 변경 — 05 먼저) · **C** 그대로 두고 실사용 비율만 본다(건당 비용이 평소 왕복 1회라 비율이 낮으면 비용도 낮다). 유지: `GET /analysis`는 언제나 200 · `highlight`는 비지 않는다(05 §2) · 503 · 빈 문장은 계속 캐시하지 않는다. 검증: `HighlightServiceImplTest`에 원인별 캐시 여부 | 고현석(server) | E-102 · E-79 · E-101 · server PR #48 리뷰 · ai PR #43 | ☐ — **server 이슈 #49는 닫았다(v2.52) — 이 행으로만 추적한다.** 코드로 밟을 다음 걸음이 없어서다. **리허설에서 `fallback: true` 비율을 로그로 먼저 센 뒤** A/B/C 중 하나를 01에 E-번호로 확정하고, 정본을 먼저 고친 뒤 코드가 따라간다 |

> ✅ R1·R2·R3은 9/2 밤에 끝났습니다 (v1.6). 회고 P0 경로가 문서와 일치합니다. AI 레포 `66d1e62` 푸시 완료.

---

## 🟡 데이터 확보 후 — 6건 (9/4 이후)

> 지금 정하면 근거 없는 임의값이 넷 늘어날 뿐입니다.
> 익명화 데이터셋 분포를 보고 한 번에 정하십시오.
> **전부 Spring `application.yml` `rules.*` 파라미터로 분리**해두면 9/7 튜닝이 값 주입만으로 끝납니다 (05 §3 · 07 §7 — v1.3).

| # | 항목 | 파라미터 키 | 참고 |
|---|---|---|---|
| 15 | 보정 강도 `k` | `rules.shrinkage-k` | 잠정 3~5. 묶음당 회고 수 분포 확인 후. **v2.2: `3` 주입 (E-57) — 튜닝 대기** |
| 16 | 롤업 기준 건수 | `rules.rollup-min-count` | 묶음 세분화 정도 확인 후. **v2.2: `3` 주입 — 튜닝 대기** |
| 17 | 보류 임계값 | `rules.pending-min-count` | **보류 임계값 ≤ 롤업 기준** 정합 필요. **v2.2: `3` 주입 — 튜닝 대기** |
| 18 | 만족도 지도 축 경계선 | `rules.axis-x-boundary` / `rules.axis-y-boundary` | 가로: **월 합계의 예산 대비 %** (v1.2 정의 변경 반영) / 세로: 0 또는 사용자 평균. **v2.2: `0.1` / `0` 주입 — 튜닝 대기** |
| 20 | **회고 후보 선별 규칙 파라미터** (v1.9 신설) | `rules.candidate.*` · `rules.sensitivity.*` | 아래 3건을 **E-46 민감도 프리셋과 한 번에** 정합니다. **v2.2: 잠정값 주입 (E-57 · E-62) — 기준선 90일 · 중앙값 · 표본 5 · 예산 비율 0.1 · 반복 LOW 2 · 배수 3.0/2.0/1.5. 튜닝 대기** |
| 21 | **LLM 타임아웃(E-88) 실측** — 6초가 실제 회고 추출 프롬프트 응답 시간에 충분한지 Spring·AI 연결 후 측정 | `LLM_TIMEOUT_SECONDS` | **참고:** E-88 · 07 §10 리스크 4. **착수 조건:** Spring·AI 통합 환경에서 실제 LLM 호출 가능해진 뒤. 부족하면 재시도 0회 등 대안 검토 |

**#20 세부 — 정할 것 3건**

| 항목 | 무엇을 정하나 | 지금 상태 |
|---|---|---|
| 이상치 기준선 | `TIMESLOT_OUTLIER`의 비교 기준 — 같은 (카테고리, 시간대) 거래의 **어느 기간** 값을, **평균이 아니라 중앙값**으로 볼지, 표본이 몇 건 미만이면 카테고리로 롤업할지 | 01 B-8은 "시간대별 이상치 + 사용자 설정 임계값"까지만 정함. 배수는 E-46 프리셋이 담당 |
| 큰 금액 기준 | `THRESHOLD_EXCEEDED`의 기준을 **절대 금액**으로 둘지 **월 예산 대비 비율**로 둘지. 비율이면 `users`에 새 컬럼이 필요 없고 "재정 상황 대비"라는 기획서 원칙과 맞음 | 04 `User`에 절대 금액 컬럼 없음. `monthlyBudget`은 이미 부담 분모로 있음 |
| 반복 아쉬움 기준 | `REPEATED_LOW_SATISFACTION`의 문턱. `rules.pending-min-count`(#17)를 그대로 쓸지 별도 값을 둘지. 회고 **전**이라 목적·동행인을 모르므로 묶음 키를 **부분 매칭**해야 함 | #17과 정합 필요 |

> ⚠️ **#18은 v1.2에서 성격이 바뀌었습니다.** 가로축이 평균 단가에서 월 합계로 바뀌었으므로
> 잠정 예시였던 `x = 0.05`는 무효입니다. API 응답에서도 `null`로 되돌렸습니다.

---

## 📋 발표 자료 — 7건 (9/7까지, **전원 공동 — #19**)

제출본은 수정 불가. 발표 슬라이드에서 복구합니다.

- [ ] **사회적 파급효과 복원** ← 심사표 필수 항목, 현재 0건. **9/7 전 초안 필수 (현장 제작 예외)**
- [ ] 만족도 지도 사분면 도식 (확정 표 기반, 제작 가능)
- [ ] 서비스 플로우 다이어그램
- [x] ~~심야 배달 단가 불일치~~ → **12,000원으로 확정** (E-10). 슬라이드에 "데모 데이터 기준 재산출" 1줄 추가
- [ ] 투자 상품 배제 사유 명시 (유사투자자문 규제 준수)
- [ ] 팀명 '지갑때려잡기' vs 서비스 철학 정리 1줄
- [ ] **(v1.2 추가 · v1.3 문구)** 설명가능성 슬라이드 — "AI는 입구(후보 제안)와 출구(설명)에만, 판단은 **Spring 규칙 엔진**" 1장

---

## 🔄 동기화 필요 (문서 → 외부 산출물)

### 구글 시트 (요구사항 탭 `gid=236528920`) — **v1.1이 한 건도 반영 안 됨**

| 우선 | 반영할 것 |
|---|---|
| ✅ | **v1.3 재갱신 완료 (9/2)** — FR-12 4건 추가 · FR-04-07/05-01/05-02/06-01/08-01 문구 · 담당 열(정민규 Spring · 오진호 AI) · NFR-01·04 문구 · 범위 내 행 · v1.3 변경 표. **구글 시트에 다시 가져오기 필요** |
| 🔴 | FR-03-05 "**ai 둘러대기**" 표현 삭제 → "규칙 엔진이 `reasonCode`를 결정론적으로 산출한다" |
| 🔴 | FR-07-04 "**Out**" 삭제 → Great! / Awesome!! / Umm... / Hmm! / PENDING (5종) |
| 🔴 | **FR-10 인앱 알림 3건 행 추가** |
| 🔴 | **NFR-01~09 시트/섹션 추가** (현재 전무) |
| 🔴 | **FR-11 소비 분석 4건 행 추가** (v1.2 신설) |
| 🔴 | **FR-06-04·05 문구 교체** — 판정 2종 + `evaluationStatus` 보류 (E-11) |
| 🔴 | **FR-07-02 "3라벨" → "2색"**, **FR-07-06·07 행 추가** (보류 회색 점 / PROTECT CTA) |
| 🟠 | FR-04 우선순위 공란 8건 채움 (01·11·14=P0 / 07·08·09=P1 / 12·13=P2) |
| 🟠 | FR-04-04·05 "텍스트로 입력" → "선택한다", 비고의 `(확정 필요)` 제거 |
| 🟠 | **담당 열 전체 채움** |
| 🟡 | FR-01-01 SNS 로그인 확장 / FR-01-02 삭제 추가 / FR-01-06 신설 |
| 🟡 | FR-05-01 "목적" → "목적·동행인" |
| 🟡 | FR-06-06 신설 (월 합계 부담 산출) |
| 🟡 | FR-08-02·03 "예상 소비 횟수" → "조정 횟수" |
| 🟡 | FR-09-02 완료 플래그 / FR-09-03 신설 |
| 🟡 | FR-03-01 "(확인 필요)" 제거 / FR-03-06 "무시" → "제외" |

> 갱신본: **`요구사항 정의서.xlsx`** v1.3 (3시트 — 개요·범위 / 기능 요구사항 **74건**(P0 42 · P1 23 · P2 9) / 비기능 요구사항 9건)
> 구글 스프레드시트 → `파일 → 가져오기 → 업로드` → **스프레드시트 바꾸기**

### 피그마 IA (`node-id 37-3`) — **v1.1 지시 5건 전부 미반영**

03 문서 §5의 **F-1 ~ F-13** 을 그대로 적용하십시오. 요약:

| 우선 | 작업 |
|---|---|
| 🔴 | `2-4. Out` 노드 삭제 (F-5) |
| 🔴 | 판정 라벨(**2색**) / 보류(회색 상태) / 처방 문구(5) 노드 분리 (F-4·F-6) |
| 🔴 | 탭 번호 재정비 `3-1~3-4`, 채팅 하위 `3-2-x` (F-1·F-2) |
| 🟠 | 마이페이지 `4. 목표 자금 관리` 추가 (F-7) |
| 🟠 | 알림 `2. 제안 알림` · `3. 읽음 처리` 추가 (F-8) |
| 🟠 | 가로축 라벨 "월 합계 금액 ÷ 등록한 예산" (F-9) |
| 🟠 | 채팅 `1-3. 회고 대상 없음(빈 상태)` 추가 (F-10) |
| 🟡 | 홈 `3. 보조 지표` 복원, 만족도 지도 `4.`로 (F-11) |
| 🟡 | 레이어명 화석 정리 (F-3·F-12) |
| 🟡 | 프레임명 → `정보 구조도 v1.2` (F-13) |

> **9/1 파이널 체크:** 지시 24건 중 **21건 반영 완료.**
> 잔여 3건은 `피그마_잔여수정_v1.3.md` 참조 — **`1:305`의 `Out!` → `Hmm!` 1건이 최우선입니다.**
> 나머지 2건은 `<ID Index>` 헤더 추가 · `<상단 내비게이션 바 헤더>` 문구, 그리고 2depth 번호 규칙 통일(A/B 택1).

---

## 📅 남은 7일 일정

| 기간 | 목표 | 성공 기준 |
|---|---|---|
| **9/1** | v1.2 문서 배포 · 담당 종결 · 시트/피그마 동기화 | 미배정 0건 ✅ |
| **9/2 (오늘)** | v1.3 문서 배포 ✅ · 스키마 확정 · Flyway `V1` ✅ · **`ai-ping` 200 ✅ (9/2 밤 실측)** · 레포 조치 R1~R3 ✅ · 3사 헤더 확보 | Spring → AI `/chat` 왕복 |
| **9/3 ~ 9/4** | 3사 CSV 파서 · 익명화 시드 데이터 | 데이터가 DB에 적재됨 |
| **9/5 ~ 9/6** | 규칙 엔진(Spring · 정민규 — 묶음·보정·부담·판정) · 만족도 지도 UI | 지도에 점이 찍힘 |
| **9/7** | 미결 15~18 튜닝 · NFR 잠정치 재확인 · 발표 자료 7건 · **v2.1 문서 배포 ✅ · 카카오 콘솔 준비(9/2 항목 3, 로컬 URI) ✅ · 스파이크(항목 1) ✅** | 임의값 제거 완료 · 카카오 `/v2/user/me` 응답 1회 확인 ✅ |
| **9/8 ~ 9/10** | 본선 — 통합 · 시연 완성도 | — |

> **9/2 연동 골격 완성이 남은 일주일의 전부입니다.**
> 백엔드 2종 병행에서 팀이 무너지는 지점은 예외 없이 통합이며,
> 본선 30시간 안에서 처리하려 들면 반드시 늦습니다.

---

## 문서 체계

| 문서 | 역할 | 주 담당 | 외부 산출물 |
|---|---|---|---|
| `01_결정로그.md` | **단일 진실 공급원** — 모든 확정값 | 오진호 | — |
| `02_요구사항_정의서.md` | FR / NFR / 범위 | 오진호 | **구글 시트** |
| `03_IA_화면정의서.md` | 화면 · 네비게이션 · 표시 규칙 | 석예림 | **피그마** |
| `04_데이터모델_ERD.md` | 엔티티 · 파생값 산식 · CSV 파싱 | 고현석 | — |
| `05_API_명세서.md` | 엔드포인트 · 연동 규격 | 고현석 · 석정한 | — |
| `06_액션시트.md` | 미결 항목 · 일정 · 동기화 | 전원 | — |
| `07_기술스택_레포구성.md` | 스택 · 레포 구조 · 환경변수 · 부트스트랩 | 고현석 | **GitHub** |
| `sottaejap-ai/README.md` · `docs/*` | AI 레포 내부 규칙 (v1.3부터 05·07이 이 문서를 반영) | 오진호 | **GitHub** |

> 값이 충돌하면 **`01_결정로그.md`가 우선**합니다.
> 외부 산출물(시트·피그마)이 문서와 다르면 **문서가 정본**이며, 외부를 고칩니다.
> **레포와 문서가 다르면 (v1.3):** 레포 우선으로 05·07을 먼저 고치되, 결정로그 §0 v1.3의 "양보하지 않은 것"은 레포가 따릅니다 (07 §6 절차).
