# 소때잡 — 기술 스택 · 레포 구성

**버전:** v2.15 | **기준일:** 2026-09-09 | **담당:** 고현석(통합) · 오진호(팀장)

> 값의 출처는 `01_결정로그.md`입니다. 충돌 시 결정 로그가 우선합니다.
> 이 문서는 **레포 3개를 만들고 9/2까지 두 서버가 통신하게 만드는** 데 필요한 것만 담습니다.
> **v1.3 (레포 우선):** `sottaejap-ai` 레포(`f9d3c78`)를 기준으로 AI 스택·구조·환경변수·실행 절차를 맞췄습니다. **규칙 엔진은 Spring입니다** (E-18).
> **v1.6 (9/2 밤):** 세 레포 스캐폴딩 완료. AI 레포 조치 R1~R6·R10 반영 — §2 AI 트리 · §3 `.env.example` · §4 실행 · §11 체크리스트.
> **v2.15 (9/9):** **§12 신설 — EC2 운영 메모리 배분** (01 v2.34 **E-111** · 06 v2.49 R28 · server #58). 운영 한 대의 **메모리 1.9 GiB · swap 2 GiB**와 거기에 `db` · `server` · `ai`가 같이 뜬다는 사실, 그리고 **JVM 힙 `-Xmx512m` · 컨테이너 상한 `mem_limit: 1g`** 을 처음으로 문서에 적습니다. 그 전에는 `Dockerfile`에도 compose에도 힙이 없어 `MaxRAMPercentage` 기본 25%(1.9 GiB의 25% ≈ 476MB)에 딸려 다녔고, 06 R28에는 이 인스턴스에 맞지 않는 **"힙 1GB 이상"** 조건이 붙어 있었습니다. **`deploy.yml` 주석과 README의 "t3.micro · 프리티어 메모리"는 실측과 달랐습니다** — 그 값을 믿고 사이징하면 힙을 오히려 줄이는 값이 나옵니다. §1 공통 인프라 표의 `배포 (서버·AI)` 비고에 포인터를 답니다.
> **v2.14 (9/9):** §4에 **C. Flyway 체크섬 불일치**를 추가했습니다 (01 **E-98**) — `server`만 `Exited (1)`로 죽고 로그에 `Migration checksum mismatch`가 뜰 때 `flyway repair`가 아니라 `docker compose down -v`로 볼륨을 지웁니다. `psql \d <테이블>`로 컬럼이 빠진 것을 먼저 확인합니다 (server 이슈 #39 · PR #40 반영).
> **v2.13 (9/8):** §4 A안 보강 — 컨테이너 포트를 `127.0.0.1`에만 엽니다. 같은 네트워크의 다른 사람이 개발 서버에 닿지 못합니다 (server PR #26 리뷰).
> **v2.12 (9/8):** §4 로컬 실행이 **두 갈래**가 됐습니다 — `sottaejap-server`에서 `docker compose up` 한 줄로 db·server·ai를 함께 띄웁니다. compose가 `AI_SHARED_SECRET`을 양쪽에 같은 값으로 넘겨 시크릿 불일치 401(E-37)이 구조적으로 나지 않습니다. 기존 3단계 수동 실행은 핫리로드용으로 남깁니다. §1 표 `로컬 실행` 칸도 함께 갱신 (server #23).
> **v2.11 (9/8):** §6 공유 enum `taskType`에 **`FINANCE_QA`** 추가 — 05 §0·§3은 v1.8부터 6종인데 여기만 5종이었습니다. server PR #12가 `TaskType.FINANCE_QA`를 코드에 넣었습니다. 이 목록이 05 §0의 사본이라는 것과, 아직 옮기지 않은 enum이 무엇인지도 밝힙니다(server PR #12 리뷰).
> **v2.10 (9/8):** §3 `sottaejap-server/.env.example`에 **환경 변수 4종 추가** — `VAPID_PUBLIC_KEY` · `VAPID_PRIVATE_KEY` · `VAPID_SUBJECT` · `NOTIFICATION_DAILY_CRON` (01 E-68 각주 반영). 크론은 **30분 격자 전용**입니다 — 알림 시각이 30분 격자에 내린 값이라(E-71) 시간마다로 바꾸면 목표가 `:30`인 사용자는 스케줄에 걸리지 않습니다(server PR #12 리뷰).
> **v2.9 (9/8):** LLM 타임아웃 잠정값을 **8초에서 6초**로 조정했습니다 (E-88 · `sottaejap-ai` PR #9 `bc1d687`). §2 AI 트리 · §3 `.env.example` · §10 리스크 4를 갱신했으며 실제 회고 추출 프롬프트로 재확인해야 합니다.
> **v2.9 (9/8):** §10 리스크 3 정정 — 카카오 리다이렉트 URI는 **클라이언트 콜백**입니다. 서버 주소(`localhost:8080` · EC2)를 등록 대상으로 적어 둔 E-55 이전 문장이 남아 있어 §3와 어긋났습니다(server PR #8 리뷰).
> **v2.8 (9/8):** §1 서버 표에 **Apache POI 5.5.1** 추가 — 05 §2가 규정한 XLSX 업로드를 구현합니다. CSV만 받던 구현이 명세와 어긋나 있었습니다(server PR #2 리뷰).
> §3 `RULES_*` 안내와 §7 `rules:` 주석을 고쳤습니다 — **값을 비우지 말고 키를 지웁니다.** `${RULES_X:잠정값}`은 키가 없을 때만 잠정값을 주고, 값만 비우면 null(리스트는 빈 리스트)이 들어갑니다(server PR #6 리뷰).
> **v2.7 (9/7):** §1 · §7 · §10 리스크 2 — 금융 RAG 개통 (01 E-85 · E-86). `V8__financial_chunks.sql` · 임베딩 `text-embedding-3-small`(1536) · `DATABASE_URL`은 asyncpg 형식이고 비어 있으면 금융 RAG만 꺼집니다(기동은 정상).
> **v2.2 (9/7):** §7 `rules:` 블록에 **잠정값 주입** + `candidate.*` · `cluster.meal-categories` 신설 (01 E-57 · E-58). 값은 yml 기본값이고 `RULES_*` 환경변수로 덮어씁니다 — §3. 회고 슬라이스 레포 조치는 06 R16.
> **v2.1 (9/7):** 카카오 로그인 흐름 = **클라이언트 인가 코드 → 서버 교환** (01 E-55). Spring `oauth2Login` 미사용 — §0 E-15 · §1 표 · §3 `KAKAO_REDIRECT_URIS` · 클라이언트 `VITE_KAKAO_*` · §10 리스크 1 갱신.
> **v2.0 (9/7):** §6 공유 enum에서 `cardIssuer` **삭제** (01 E-52 — 계약 변경, 반영은 06 R14).
> **v1.9 (9/3):** §6 공유 enum `timeSlot` **4종**으로 변경 (01 E-50 — 계약 변경, 반영은 06 R12) / §7 `rules:` 블록에 `chat-window-days` 추가 (01 E-48).
> **v1.7 (9/3):** §9 브랜치·커밋·PR 규칙을 NA-WA `CONTRIBUTING.md`와 동일하게 확장 — Issue 번호 포함 브랜치명, 타입 6종, PR 제목 `[Type]`, Squash and merge. 본선 30시간 `main` 직접 push 예외는 유지. / §2 문서 저장소 `sottaejap-docs` 신설 — md 7종을 `myDocs/` 공유 폴더에서 독립 레포로 이관.

---

## 0. 이 문서로 확정되는 것

| # | 항목 | 확정 |
|---|---|---|
| E-13 | 프론트엔드 | **Vue 3 + Vite + Pinia** (PWA — `vite-plugin-pwa`) |
| E-14 | 레포 구성 | **3개 레포 분리** — `sottaejap-client` / `sottaejap-server` / `sottaejap-ai` (표기 E-32) |
| E-15 | SNS 로그인 | **카카오 OAuth 1종 실구현** + 데모 계정 폴백. 흐름은 **클라이언트 인가 코드 → 서버 교환** (v2.1 — E-55). Spring `oauth2Login`(OAuth2 Client 스타터)은 쓰지 않음 |
| E-17 | 개발 환경 | **macOS + Windows 혼용** — §5 규칙 준수 |
| E-18 | 규칙 엔진 위치 (v1.3) | **Spring 레포 안의 Java 패키지 `rules/`** — 정민규 구현 · 고현석 통합 |
| E-19 | 호출 방향 (v1.3) | Spring `AiClient` → AI **`POST /chat`** / AI `SpringClient` → Spring **`/internal/ai/*`** (05 §3) |
| E-25 | LLM (v1.3) | **OpenAI `gpt-4o-mini`** — 확정 |
| E-26 | AI 스택 (v1.3) | **Python 3.12+ · requirements.txt · venv** — uv·pyproject 사용 안 함 |
| E-27 | 브랜치 (v1.3) | **`main` / `feature/*`** — `develop` 없음 (§9) |
| E-32 | 레포명 (v1.4) | **`sottaejap-*`** — 팀 결정. GitHub 조직 `jittaejap`. `sottaejab-ai` → `sottaejap-ai` rename **완료** (v1.6 · E-42) |
| E-33 | 클라이언트 언어·스타일 (v1.4) | **TypeScript `~6.0.3`** · **Tailwind 4** · 평면 구조 유지 — §1·§2 |
| E-34~36 | 서버 스택 재확인 (v1.5) | **Java 21** · **Boot 4.1.1** · **springdoc 3.1.0** · **Gradle 9.7.1** — §1 |

---

## 1. 스택 확정표 (2026-09-01 기준 stable latest 확인 완료)

> **버전 고정 규칙:** 아래 확인값을 기준으로 스캐폴딩한 뒤,
> `package-lock.json` · `gradle/wrapper` · **`requirements.lock`**(pip 3.12 교차 해석 — §11 · E-40) 을 **9/2에 커밋해 고정**합니다 (v1.3 — AI 레포는 uv 잠금파일을 쓰지 않음).

### 클라이언트 — `sottaejap-client`

| 항목 | 기술 | 확인 stable | 비고 |
|---|---|---|---|
| 런타임 | **Node.js 24 (Active LTS)** | 24.20.0 | 26은 2026-10에 LTS 진입 — 본선까지는 24 |
| 프레임워크 | Vue 3 (Composition API · `script setup`) | **3.5.42** | 3.6은 아직 rc — stable 아님 |
| 빌드 | **Vite** | **8.2.2** | Rolldown(Rust) 기반 |
| 상태관리 | Pinia | **4.0.3** | ⚠️ **3.x 아닙니다.** 4.0은 ESM 전용 + `@vue/devtools-api` 8 peer (npm이 자동 설치) |
| 라우팅 | **Vue Router** | **5.3.0** | ⚠️ **4.x 아닙니다.** 5.2부터 Pinia 4 peer 허용 |
| PWA | `vite-plugin-pwa` | **1.3.0** | Vite 8 peer 지원 포함 |
| 차트 | ECharts | **6.1.0** | |
| 차트 래퍼 | `vue-echarts` | **8.2.0** | ECharts `^6` peer |
| HTTP | Axios | **1.20.0** | |
| 언어 | TypeScript | **`~6.0.3`** | ⚠️ **7.x 금지.** 7.0은 JS API가 없어 `vue-tsc`·`typescript-eslint`(peer `<6.1.0`)가 동작하지 않음. Vue 프로젝트는 6.0 유지 — MS 공식 안내 |
| Node 타입 | `@types/node` | **24.13.3** | 런타임 Node 24와 메이저 일치 (latest 26.x 사용 금지) |
| 스타일 | Tailwind CSS (`@tailwindcss/vite`) | **4.3.3** | E-33. 색은 `src/assets/styles/tokens.css` 토큰만 — 원시 HEX는 ESLint가 차단 |

> 클라이언트 표는 **2026-09-02 npm 재확인**값입니다. Pinia는 9/1 표기(3.0.4)가 한 메이저 뒤처져 있어 정정했습니다.

### 서버 — `sottaejap-server`

| 항목 | 기술 | 확인 stable | 비고 |
|---|---|---|---|
| 런타임 | **Java 21 (LTS)** | 21.0.12.1 (Temurin) | v1.5 — E-34. Boot 4.1은 Java 17~26 지원. `build.gradle.kts` toolchain 21 고정 |
| 프레임워크 | Spring Boot | **4.1.1** | v1.5 — E-36. 4.2.0-M1은 마일스톤이라 제외. 3.5.x는 2026-06-30 OSS 지원 종료 — §10 리스크 1 |
| 빌드 | Gradle (Kotlin DSL) | Wrapper **9.7.1** | v1.5 — E-36. `gradle/wrapper/gradle-wrapper.properties`가 정본. 버전을 손으로 적지 마십시오 |
| API 문서 | **springdoc-openapi** | **3.1.0** | v1.5 — E-35. ⚠️ **3.0.x는 Boot 4.0용, 2.8.x는 Boot 3.5용**. Boot 4.1은 3.1.x |
| ORM · 인증 · 마이그레이션 | Spring Data JPA · Spring Security(JWT — OAuth2 Client 스타터는 E-55로 미사용, 카카오 교환은 `RestClient`) · Flyway | BOM (Boot 4.1.1: Data JPA 4.1.1 · Security 7.1.1 · Flyway 12.4.0) | Flyway는 이 레포가 단독 소유. Flyway 13을 BOM 위에 덮어쓰지 않음 |
| JSON | Jackson | BOM (**3.1.5**) | ⚠️ Boot 4는 **Jackson 3** — `ObjectMapper`는 `tools.jackson.databind.json.JsonMapper`. `@JsonFormat`·`@JsonInclude` 어노테이션 패키지는 그대로 |
| JWT | jjwt | **0.13.0** | Boot 미관리 — 직접 고정 |
| 엑셀 파싱 | **Apache POI** (`poi-ooxml`) | **5.5.1** | v2.8 — 05 §2 XLSX 업로드. Boot 미관리 — 직접 고정. `.xlsx`(OOXML)만 읽습니다 |

### 규칙 엔진 — `sottaejap-server` 안 (v1.3 — E-18)

| 항목 | 기술 | 비고 |
|---|---|---|
| 규칙 엔진 | **Java 패키지 `kr.sottaejap.server.rules`** | 정민규 구현 · 고현석 리뷰. HTTP·LLM·난수·현재시각 참조 금지 |
| 파라미터 | `application.yml` `rules.*` → `RuleParams` | §7 |
| NFR-01 증명 | **`RuleEngineDeterminismTest`** (JUnit) | E-29 — 같은 입력에 같은 출력 |

### AI — `sottaejap-ai` (v1.6 — R1~R6 반영 · 의존성은 `f9d3c78` 그대로)

| 항목 | 기술 | 레포 지정 | 비고 |
|---|---|---|---|
| 런타임 | **Python 3.12+** | `python3.12 -m venv .venv` | pytest 28건 — 로컬 3.14.7과 Docker `python:3.12.14` 양쪽 통과 (v1.6). 팀은 **3.12로 통일**. lock은 3.12 기준 교차 해석 (E-40) |
| 프레임워크 | FastAPI | `>=0.115,<1.0` | `requirements.txt` 범위 지정 · `requirements.lock`으로 고정 (§11) |
| 서버 | Uvicorn | `>=0.30,<1.0` | |
| 패키지 | `pip` + `venv` | — | uv 사용 안 함 (E-26) |
| 검증 | Pydantic v2 · pydantic-settings | `>=2.8` · `>=2.4` | `/chat` DTO 계약 (05 §3) |
| HTTP | httpx | `>=0.27,<1.0` | `SpringClient` 전용 — Tool에서 직접 사용 금지 |
| LLM | **OpenAI SDK** | `>=1.40,<3.0` | `gpt-4o-mini` (E-25) |
| RAG 저장소 | PostgreSQL + **pgvector** | — | FR-12. `text-embedding-3-small` · **1536차원** (E-85) |
| 테스트 | pytest | `>=8.3,<10.0` | 외부 서비스 무호출 |

### 공통 인프라

| 항목 | 기술 | 확인 stable | 비고 |
|---|---|---|---|
| DB | PostgreSQL | **18.6** | **pgvector 확장** — 금융 RAG용 (E-21). 이미지 `pgvector/pgvector:pg18`, `CREATE EXTENSION`은 **`V8__financial_chunks.sql`** (E-85) |
| 로컬 실행 | Docker Compose | — | `docker compose up` = db·server·ai / `up -d db` = DB만 (§4) |
| 배포 (서버·AI) | AWS EC2 + Docker Compose | — | **한 대(메모리 1.9 GiB · swap 2 GiB · 루트 24GB)에 `db` · `server` · `ai`.** 메모리 배분과 JVM 힙은 **§12** |
| 배포 (클라이언트) | Vercel | — | 정적 빌드 |

> **v1.0에서 정정된 것 5건:** Vite `7.x → 8.x` · **Vue Router `4.x → 5.x`** ·
> FastAPI `0.11x → 0.14x` · Python `3.12 → 3.13` · springdoc `2.x → 3.x`.
> Java는 `21 → 25(LTS)`로 올렸다가 **v1.5에서 21로 되돌렸습니다** (E-34). Node 24 LTS를 추가했습니다.
> **v1.3:** AI 스택은 레포 기준으로 되돌렸습니다 — Python `3.13 → 3.12+`, FastAPI 고정값 → 범위 지정, uv → pip/venv.

## 2. 레포 3개 + 문서 저장소

| 레포 | 담당 | 역할 | 배포 |
|---|---|---|---|
| **`sottaejap-client`** | 석정한 · 석예림 | Vue 3 PWA 클라이언트 | Vercel |
| **`sottaejap-server`** | 고현석 · **정민규(규칙 엔진)** | Spring Boot — 인증 · 저장/조회 API · 파싱 · **규칙 엔진 전부(⓪②③④⑦⑧)** · 알림 · AI 내부 API · **Flyway 소유** | EC2 |
| **`sottaejap-ai`** | 오진호 | FastAPI — **Single Agent · 자연어 · Tool Calling · 금융 RAG · 설명** (①후보 ⑤⑥⑨⑪). 계산·판정 없음 | EC2 |

> 레포를 나눈 대가로 **연동 규격 변경이 3곳을 동시에 건드립니다.**
> 이를 막기 위한 규칙이 §6 '계약 변경 절차'입니다. 이것만은 지켜 주십시오.

**문서 저장소 `sottaejap-docs`** (v1.7 — 9/3 신설): 이 문서를 포함한 기획·설계 md 7종의 정본입니다. 코드 레포 3개의 `AGENTS.md` · `CONTRIBUTING.md` · 소스 주석이 `sottaejap-docs/05_API_명세서.md` §3처럼 **경로 + § 번호**로 참조하므로, 코드 레포와 **같은 부모 폴더에 clone**합니다. 파일명 · § 번호 변경 금지와 편집 규칙은 그 저장소의 `AGENTS.md`에 있습니다. 코드는 없고 배포도 없습니다.

### `sottaejap-client`

```
sottaejap-client/
├── public/                  # PWA 아이콘 · manifest 자산
├── src/
│   ├── api/                 # axios 인스턴스 · 엔드포인트별 함수 (05 문서 1:1)
│   ├── assets/
│   ├── components/
│   │   ├── common/          # Box.vue · Button.vue · Stepper.vue
│   │   ├── chat/            # 채팅 버블 · 3택 버튼 · 목적/동행인 칩
│   │   └── map/             # 산점도 · 범례 · 상세 패널
│   ├── composables/
│   ├── router/              # IA 번호 체계와 1:1 (03 문서 §1)
│   ├── stores/              # Pinia — user · transactions · retrospect · map
│   ├── views/
│   │   ├── LoginView.vue            # 1L
│   │   ├── OnboardingView.vue       # 2-1
│   │   ├── HomeView.vue             # 3-1
│   │   ├── ChatView.vue             # 3-2
│   │   ├── MapView.vue              # 3-3
│   │   ├── TransactionsView.vue     # 3-4
│   │   ├── MyPageView.vue           # 4MP
│   │   └── NotificationView.vue     # 4A
│   ├── App.vue
│   └── main.ts
├── .env.example
├── vite.config.ts
└── package.json
```

**라우터 = IA 번호 체계**

| path | 화면 | IA |
|---|---|---|
| `/login` | 로그인 | 1L |
| `/onboarding` | 온보딩 | 2-1 |
| `/` | 홈 | 3-1 |
| `/chat` | 채팅 | 3-2 (하위 `3-2-1` 초기메뉴 · `3-2-2` 회고 · `3-2-3` 행동제안 · `3-2-4` 소비분석은 뷰 내부 상태) |
| `/map` | 만족도 지도 | 3-3 |
| `/transactions` | 거래내역 | 3-4 |
| `/me` | 마이페이지 | 4MP |
| `/notifications` | 알림 | 4A |

> 라우터 가드: `GET /users/me`의 `onboardingCompleted === false` → `/onboarding` 강제 (FR-09-03).

### `sottaejap-server` (Spring Boot)

```
sottaejap-server/
├── src/main/java/kr/sottaejap/server/
│   ├── auth/            # 카카오 OAuth2 · JWT 발급
│   ├── user/            # User · 설정 · 온보딩 플래그
│   ├── goal/            # 목표 · 달성률 · 채택 제안 합산 (v2.6 — E-83)
│   ├── transaction/
│   │   └── parser/      # KbParser · HanaParser · ShinhanParser + CategoryMapper
│   ├── retrospect/
│   │   └── candidate/   # 후보 선별 쿼리 (규칙 엔진 ⓪ — 결정론적)
│   │                    # ※ behavior/는 두지 않습니다 — BehaviorCluster는 retrospect/, 조회는 analysis/ (E-80)
│   ├── suggestion/      # 재계산 파생 제안 · adopt/reject (v2.6 — E-81 · E-82)
│   ├── notification/
│   ├── analysis/        # 만족도 지도 · 소비 분석 · 묶음 조회 (v2.5 — E-72~E-76)
│   ├── rules/           # ★ 규칙 엔진 (정민규 · Java) — 결정론적. HTTP·LLM·난수·현재시각 금지 (v1.3 — E-18)
│   │   ├── cluster/         # 묶음 키 · 롤업        (②③)
│   │   ├── shrinkage/       # 축소 추정            (④)
│   │   ├── verdict/         # 사분면 · 판정 · 부담   (④)
│   │   ├── saving/          # 절감액 · 목표 배분     (⑦)
│   │   ├── aggregate/       # 소비 분석 집계        (⑧)
│   │   └── RuleParams.java  # ★ application.yml rules.* 바인딩 (§7)
│   ├── ai/              # ★ AiClient — FastAPI POST /chat 호출 (유일한 AI 통신 지점 · snake_case 변환)
│   ├── internalai/      # AI에 열어주는 /internal/ai/* 컨트롤러 6종 (05 §3)
│   └── common/          # ApiResponse · ErrorCode · enum
├── src/main/resources/
│   ├── db/migration/    # ★ Flyway — V1__init.sql ...
│   └── application.yml  # rules.* 파라미터 (§7)
├── src/test/java/kr/sottaejap/server/rules/RuleEngineDeterminismTest.java   # ★ NFR-01 증명 (E-29)
├── build.gradle.kts
└── Dockerfile
```

### `sottaejap-ai` (FastAPI) — v1.6 실제 구조 (R1~R6 반영)

> ⚠️ **역할 주의 (v1.3).** 이 레포에는 규칙 엔진이 **없습니다.** Agent·자연어·Tool·RAG·설명만 있습니다 (E-18).
> 계산·판정을 여기에 넣으면 NFR-02 방어선("판단은 Spring 규칙 엔진")이 무너집니다. 레포 `AGENTS.md`·`docs/DEVELOPMENT.md`의 금지 목록을 따르십시오.
> "규칙 엔진(결정론적) 소유" 문구는 **Spring 레포 README 첫 줄**에 둡니다 (E-30).

```
sottaejap-ai/
├── AGENTS.md · CLAUDE.md        # 에이전트 전제 (CLAUDE.md는 포인터) — server·client와 같은 구조
├── CONTRIBUTING.md              # 브랜치 · 커밋 · PR · 검사 · lock 재생성
├── .github/                     # CI(pytest · docker build) · Issue 4종 · PR 템플릿
├── app/
│   ├── main.py                  # FastAPI 앱 · GET /health ({"service":"sottaejap-ai-server"})
│   ├── api/chat.py              # POST /chat — X-Internal-Secret 검사 (E-37) · Spring이 호출하는 유일한 진입점
│   ├── agent/                   # Single Agent
│   │   ├── agent.py                 # LLM 1회 호출 → 실패 시 fallback (Tool Calling 루프 TODO)
│   │   ├── prompt.py · state.py · tool_registry.py
│   ├── ai/fallback.py           # ✅ LLM 장애 템플릿 — 작업 5종 · 회고 단계 6종 · reason_code 5종 (R3)
│   ├── tools/                   # 얇은 래퍼 — 계산 금지 (transaction · reflection · analysis · action_plan · memory · financial_rag)
│   ├── reflection/              # ① 자연어 → 회고 후보 (E-20)
│   │   ├── schemas.py               # ✅ Satisfaction HIGH|LOW|UNKNOWN (R1) · Purpose 7 · Companion 6 enum, 값은 한국어 (R2 · E-41)
│   │   ├── normalizer.py            # normalize_purpose / normalize_companion — 공백만 무시, 불일치는 None
│   │   ├── extractor.py             # LLM Structured Output (TODO)
│   │   └── validator.py             # uncertain_fields · needs_clarification
│   ├── rag/                     # 금융 RAG (P2 · E-22)
│   ├── clients/spring_client.py # ✅ /internal/ai/users/{userId}/* 6종 · X-Internal-Secret · 봉투 해제 (R5 · E-39)
│   ├── schemas/                 # chat.py(ChatResponse.fallback ✅ R3) · common.py(TaskType 5종 ✅ R4 · ReflectionStep) · tool.py
│   └── core/                    # config.py(Settings — LLM_TIMEOUT_SECONDS · INTERNAL_SHARED_SECRET) · llm.py(6초 · 재시도 1회)
├── scripts/ingest_financial_docs.py   # P2
├── tests/                       # 28건 · 외부 서비스 무호출 (conftest가 시크릿 고정 · 키 비움)
├── docs/ONBOARDING.md · DEVELOPMENT.md
├── requirements.txt             # 범위 (07 §1)
├── requirements.lock            # ✅ 3.12 기준 고정 (R6 · E-40)
├── Dockerfile · .dockerignore   # ✅ python:3.12-slim · PYTHONUTF8=1 · TZ=Asia/Seoul (R6)
├── .gitattributes · .editorconfig   # ✅ LF 강제 (R6 · §5)
└── .env.example                 # 10줄 (§3)
```

> R1~R6·R10은 **v1.6에서 완료**했습니다. 남은 TODO는 Tool Calling 루프 · Structured Output · 금융 RAG(P2)입니다 — 레포 README "현재 구현 범위".

---

## 3. 환경변수 계약 (v1.3)

**`sottaejap-server/.env.example`**
```
SPRING_PROFILES_ACTIVE=local
DB_URL=jdbc:postgresql://localhost:5432/sottaejap
DB_USER=sottaejap
DB_PASSWORD=

AI_BASE_URL=http://localhost:8000
AI_SHARED_SECRET=              # X-Internal-Secret — 양방향 공통값 (05 §3)
AI_TIMEOUT_MS=15000            # Spring → AI /chat (⚠️ 잠정 — AI 내부 왕복 포함, 05 §3)

KAKAO_CLIENT_ID=                                     # REST API 키
KAKAO_CLIENT_SECRET=                                 # 서버에만 둔다 (E-55)
KAKAO_REDIRECT_URIS=http://localhost:5173/auth/callback   # 허용할 클라이언트 콜백 URL, 쉼표 구분 (v2.1 — E-55). 카카오 콘솔에도 같은 값 등록
JWT_SECRET=

# Web Push · 회고 알림 (v2.10 — E-68 · E-71). 키 생성 절차는 server README "Web Push 키 설정"
VAPID_PUBLIC_KEY=              # 클라이언트가 pushManager.subscribe에 넘길 applicationServerKey (87자)
VAPID_PRIVATE_KEY=             # 서버만 갖는다. 커밋 금지 (43자)
VAPID_SUBJECT=                 # mailto: 또는 https URL. 셋을 다 채워야 푸시가 켜지고, 비우면 인앱 알림만 동작한다
# 회고 요청 알림 스캔 주기(Asia/Seoul). 30분 격자 전용이다 — 시간마다로 바꾸면 목표가 :30인 사용자는 스케줄에 안 걸린다
NOTIFICATION_DAILY_CRON=0 0,30 * * * *

DEMO_ACCOUNT_ENABLED=true      # 데모 계정 폴백 (E-15)
# RULES_*: 규칙 파라미터 덮어쓰기 (v2.2 — E-57). 덮어쓸 때만 키를 추가한다. 예: RULES_SHRINKAGE_K=5 · RULES_CANDIDATE_BIG_AMOUNT_BUDGET_RATIO=0.05
# (v2.8) 값을 비우지 말고 키를 지운다 — `RULES_SHRINKAGE_K=`는 잠정값이 아니라 null이다. ${RULES_X:잠정값}은 키가 아예 없을 때만 잠정값을 준다
```

**`sottaejap-ai/.env.example`** — v1.6 현행 10줄
```
OPENAI_API_KEY=                # 비어 있으면 /chat이 전부 템플릿 응답 fallback: true (E-38)
OPENAI_MODEL=gpt-4o-mini
LLM_TIMEOUT_SECONDS=6          # NFR-04 (⚠️ 잠정 · E-88) · 초과 시 재시도 1회 후 템플릿
SPRING_BASE_URL=http://localhost:8080
SPRING_TIMEOUT_SECONDS=10      # AI → Spring 내부 API
INTERNAL_SHARED_SECRET=        # = server의 AI_SHARED_SECRET. 비어 있으면 /chat이 전부 401 (E-37)
DATABASE_URL=                  # pgvector — 비우면 금융 RAG만 꺼진다(기동은 된다). asyncpg 형식(jdbc: 금지)
AI_SERVER_HOST=0.0.0.0
AI_SERVER_PORT=8000
PYTHONUTF8=1                   # Windows 인코딩 (§5-3)
```

**`sottaejap-client/.env.example`**
```
VITE_API_BASE_URL=http://localhost:8080
VITE_KAKAO_CLIENT_ID=                                # REST API 키 — 공개값 (Client Secret은 넣지 않는다)
VITE_KAKAO_REDIRECT_URI=http://localhost:5173/auth/callback   # server의 KAKAO_REDIRECT_URIS에 포함돼야 한다 (E-55)
```

> `AI_SHARED_SECRET`(server) = `INTERNAL_SHARED_SECRET`(ai). **9/2에 팀 채널에 한 번 공유하고 고정**하십시오.
> `.env`는 절대 커밋하지 않습니다. `.env.example`만 커밋합니다.

---

## 4. 로컬 실행 순서 (v2.14)

`sottaejap-server/docker-compose.yml` 하나가 컨테이너를 소유합니다 (레포가 나뉘었으므로 DB 소유는 server).
**A · B 둘 중 하나**를 고릅니다. 기동이 실패하면 **C**를 봅니다.

### A. 한 줄로 전부 (v2.13) — 붙여보기·시연

```bash
cd sottaejap-server && cp .env.example .env           # 최초 1회
docker compose up -d --build                          # db(:5432) · server(:8080) · ai(:8000)
cd ../sottaejap-client && npm run dev                 # :5173 (클라이언트는 호스트에서)
```

- `.env`의 값은 비워 둬도 됩니다. 비어 있으면 그 기능만 꺼집니다 — 카카오 키가 없으면 데모 로그인,
  `OPENAI_API_KEY`가 없으면 `fallback: true` 200(E-38), VAPID 키가 없으면 인앱 알림만 동작합니다.
- compose가 `AI_SHARED_SECRET`을 server와 ai에 **같은 값**으로 넘겨 시크릿 불일치 401(E-37)이 나지 않습니다.
- 포트는 `127.0.0.1`에만 엽니다. 같은 Wi-Fi의 다른 사람은 닿지 못하고, 호스트의 브라우저·`gradlew`는 그대로 씁니다.
- `ai`는 `../sottaejap-ai`의 소스를 굽습니다 — **두 레포가 나란히** 있어야 합니다 (§2). 없으면 `up -d db server`.
- 컨테이너끼리는 서비스 이름으로 부릅니다 (`http://ai:8000` · `http://server:8080` · `db:5432`). localhost가 아닙니다.
- 코드를 고쳐도 자동 반영되지 않습니다. 다시 `--build`합니다.

### B. DB만 컨테이너 (v1.3) — 핫리로드·디버거

```bash
# 1) DB
cd sottaejap-server && docker compose up -d db      # pgvector/pgvector:pg18

# 2) Spring (Flyway가 스키마 생성)
./gradlew bootRun                                 # :8080

# 3) FastAPI  (최초 1회: python3.12 -m venv .venv && .venv/bin/pip install -r requirements.lock && cp .env.example .env → INTERNAL_SHARED_SECRET 채움)
cd ../sottaejap-ai && source .venv/bin/activate && uvicorn app.main:app --reload --host 0.0.0.0 --port 8000

# 4) Web
cd ../sottaejap-client && npm run dev                # :5173
```

**OS별 명령 차이** — §5 참조. `./gradlew` ↔ `.\gradlew.bat`, venv activate 등이 다릅니다.

**연동 확인 (9/2 성공 기준)**
```bash
curl -s localhost:8000/health                     # ai 살아있음  → {"status":"ok","service":"sottaejap-ai-server"}
curl -s localhost:8080/actuator/health            # api 살아있음
curl -s localhost:8080/internal-test/ai-ping      # api → ai POST /chat 왕복 200 (reply 포함)
```
> 세 번째 줄이 200을 뱉는 순간이 **9/2의 목표**입니다. ✅ 달성 (9/2 밤).
> **(v1.6) 9/2 밤 실측 통과.** 세 번째 줄이 `{"success":true,"data":{"reply":"…","fallback":true}}` 200을 돌려줍니다 — OpenAI 키 없음, 공유 시크릿 양쪽 주입, DB `pgvector:pg18`. 역방향 AI → Spring GET 5종도 200. 어느 한쪽이라도 시크릿이 비면 401입니다 (E-37). OpenAI 키는 없어도 됩니다 — `fallback: true`로 200 (E-38).
> AI를 직접 두드릴 때는 헤더가 필요합니다: `curl -X POST localhost:8000/chat -H 'X-Internal-Secret: <값>' -H 'Content-Type: application/json' -d '{"message":"안녕"}'`

### C. Flyway 체크섬 불일치 — `server`만 죽을 때 (v2.14 — E-98)

**증상.** `docker compose up -d --build` 뒤 `docker compose ps`에서 `db` · `ai`는 `(healthy)`인데 `server`만
`Exited (1)`입니다. `docker compose logs server` 첫 화면에 아래가 있습니다.

```text
Migration checksum mismatch for migration version 8
-> Applied to database : 928890964
-> Resolved locally    : -541474916
```

**판별 — `flyway repair`를 먼저 부르지 마십시오.** 그 버전이 만든 테이블의 실제 컬럼을 먼저 봅니다.

```bash
docker compose exec db psql -U sottaejap -d sottaejap -c "\d financial_chunks"
```

마이그레이션 파일이 적는 컬럼이 출력에 없으면 **체크섬만 어긋난 것이 아니라 스키마가 다릅니다.**
`repair`는 기록된 체크섬을 지금 파일 값으로 덮어쓸 뿐 빠진 컬럼을 만들지 않습니다. 그대로 두면 기동은
지나가고, 나중에 그 컬럼을 읽는 코드가 깨집니다.

원인은 대개 **볼륨이 파일보다 오래된 것**입니다. 2026-09-09 실측 사례에서는 server PR #22가 리뷰 중
`V8__financial_chunks.sql`에 `created_at`을 더한 뒤 squash로 병합됐고, 로컬 볼륨
`sottaejap-server_sottaejap_db_data`는 그 추가 이전 브랜치로 만든 DB였습니다. `\d financial_chunks`에
`created_at`이 없다는 것이 근거입니다.

**해결.** 볼륨을 지우고 다시 띄웁니다. Flyway가 `V1`부터 다시 적용합니다.

```bash
cd sottaejap-server
docker compose down -v            # 이 compose 프로젝트의 볼륨만 지운다
docker compose up -d --build
```

- **로컬 개발 DB에서만 씁니다.** EC2에서는 쓰지 않습니다 — 배포 DB는 `deploy/docker-compose.yml`이 쓰고,
  거기서 볼륨을 지우면 실제 사용자 데이터가 사라집니다.
- 지워지는 것은 그 볼륨에 든 전부입니다. 로컬에는 로그인으로 생긴 `users` 몇 행뿐이라 다시 로그인하면
  그대로 생기고, 올려 둔 거래 CSV가 있으면 다시 올립니다.
- `down -v`는 **명령을 실행한 폴더의 compose 프로젝트 볼륨만** 지웁니다. `sottaejap-server`에서 실행하면
  `sottaejap-server_sottaejap_db_data` 하나이고, `sottaejap-demo_db_data`는 사정권 밖입니다.

---

## 5. OS 혼용 환경 규칙 (macOS + Windows)

팀원이 두 OS를 섞어 씁니다. **아래 4가지는 9/1에 정해두지 않으면 반드시 사고가 납니다.**

### 5-1. 🔴 CSV 인코딩 — FR-02와 직결된 진짜 위험

**Windows Excel은 CSV를 CP949로, macOS Excel·Numbers는 UTF-8로 저장합니다.**
3사 CSV를 "선 정제 후 업로드"(FR-02-02)하는데, **정제한 사람의 OS에 따라 다른 인코딩 파일이 나옵니다.**

- 파서의 `EUC-KR / UTF-8 자동 감지`(04 문서 §4)는 이미 이걸 전제합니다 — 빼지 마십시오.
- **테스트 픽스처를 두 인코딩 모두 준비**하십시오. 한쪽만 두면 다른 OS에서 파서가 터집니다.
- CRLF로 저장된 CSV는 마지막 컬럼 값 끝에 `\r`이 붙습니다. `배달\r ≠ 배달`이 되어
  **카테고리 매핑(FR-02-03)과 묶음 키(FR-05-02)가 조용히 어긋납니다.** 파서에서 `.strip()` 필수.

### 5-2. 줄바꿈 — `.gitattributes`를 3개 레포 모두에 커밋 (9/1)

```gitattributes
* text=auto eol=lf
*.bat  text eol=crlf
*.cmd  text eol=crlf
gradlew      text eol=lf
gradlew.bat  text eol=crlf
*.png  binary
*.jpg  binary
*.xlsx binary
*.pdf  binary
```
> 한 줄에 패턴 하나만 씁니다. `*.png *.jpg ... binary`처럼 붙여 쓰면 git이 `*.jpg`를 속성 이름으로 읽어 첫 패턴만 적용됩니다 (클라이언트 첫 커밋에서 확인, v1.4).
그리고 각자 한 번:
```
macOS    git config --global core.autocrlf input
Windows  git config --global core.autocrlf false
```
> CRLF가 섞이면 **diff가 파일 전체로 뜹니다.** 30시간 안에서 리뷰가 불가능해집니다.

### 5-3. UTF-8 강제

| 대상 | 조치 |
|---|---|
| **Java** | Windows 기본 인코딩이 MS949입니다. `build.gradle.kts`에 반드시:<br>`tasks.withType<JavaCompile> { options.encoding = "UTF-8" }`<br>`tasks.withType<Test> { systemProperty("file.encoding", "UTF-8") }`<br>실행 시 `-Dstdout.encoding=UTF-8 -Dstderr.encoding=UTF-8` (콘솔은 여전히 cp949) |
| **Python** | 모든 `open()`에 `encoding=` 명시. `.env`에 `PYTHONUTF8=1` |
| **파일·폴더명** | **영문 소문자 + 하이픈만.** 한글 파일명은 macOS가 NFD로 저장해 Windows·Git에서 자소가 분리됩니다 |
| **`.env` 생성** | 탐색기 말고 터미널로. Windows 탐색기는 확장자를 숨겨 `.env.txt`를 만듭니다<br>`cp .env.example .env` / `Copy-Item .env.example .env` |

### 5-4. 명령·도구 대응표

| 작업 | macOS | Windows (PowerShell) |
|---|---|---|
| Gradle | `./gradlew bootRun` | `.\gradlew.bat bootRun` |
| Python 실행 | `source .venv/bin/activate` → `uvicorn ...` | `.\.venv\Scripts\Activate.ps1` → `uvicorn ...` |
| 일회성 환경변수 | `KEY=v cmd` | `$env:KEY="v"; cmd` |
| 파일 복사 | `cp a b` | `Copy-Item a b` |

| 도구 설치 | macOS | Windows |
|---|---|---|
| Java 21 | `sdk install java 21-tem` | `winget install EclipseAdoptium.Temurin.21.JDK` |
| Node 24 | `brew install fnm` → `fnm use 24` | `winget install Schniz.fnm` → `fnm use 24` |
| Python 3.12 | `brew install python@3.12` | `winget install Python.Python.3.12` (venv는 `py -3.12 -m venv .venv`) |

> **SDKMAN은 Windows에서 네이티브로 동작하지 않습니다.** Windows는 `winget`으로 통일하십시오.
> AI 레포는 `venv`를 씁니다 (v1.3 — E-26). Windows에서는 `python` 대신 `py -3.12`로 venv를 만들고, `.venv/`는 gitignore에 이미 있습니다.

### 5-5. Docker Desktop

- **Windows는 WSL2 백엔드 필수.** Hyper-V 백엔드는 볼륨 I/O가 눈에 띄게 느립니다.
- Windows에서 **8080 · 5173이 Hyper-V 예약 포트 범위**에 물려 있을 수 있습니다.
  `netsh int ipv4 show excludedportrange protocol=tcp` 로 확인하고, 겹치면
  **포트를 개인적으로 바꾸지 말고** 예약을 해제하거나 팀 전체가 같은 포트로 문서를 고치십시오.
- DB 볼륨은 **named volume만** 사용합니다. 호스트 바인드 마운트는 경로 표기가 OS마다 달라 깨집니다.

---

## 6. 계약 변경 절차 (레포 3개의 유일한 방어선)

연동 규격(`enum` · DTO · 내부 엔드포인트)이 바뀌면 **3개 레포가 동시에 깨집니다.**

1. `05_API_명세서.md`를 **먼저** 고칩니다 — 문서가 계약입니다.
2. 팀 채널에 `[계약변경] verdict enum 2종으로` 형태로 한 줄 공지.
3. **통합 담당(고현석)** 이 세 레포의 반영 여부를 확인합니다.
4. 코드에서 문서를 앞서지 않습니다. 급하면 문서를 먼저 고치고 코드를 따라가십시오.

**공유 enum (세 레포 동일 문자열 — v1.3)**
```
satisfaction      HIGH | LOW | UNKNOWN                              ← v1.3 (E-23) · MEDIUM 없음
timeSlot          MORNING | DAY | EVENING | NIGHT                    ← v1.9 (E-50) · AFTERNOON 폐기
retrospectStatus  ACTIVE | PAUSED | COMPLETED                       ← v1.3 (E-24)
repeatIntent      true | false | null                               ← v1.3 (E-24)
taskType          REFLECTION | ANALYSIS | ACTION_PLAN | CLUSTER_NAMING | ANALYSIS_NARRATE
                  | FINANCE_QA                                       ← AI /chat 전용 · v2.11 (E-47)
quadrant          PROTECT | KEEP | MINOR | PRIORITY | (null)
verdict           SUSTAIN | ADJUST | (null)
evaluationStatus  RESOLVED | PENDING
notificationType  RETROSPECT_DUE | SUGGESTION
reasonCode        TIMESLOT_OUTLIER | THRESHOLD_EXCEEDED | REPEATED_LOW_SATISFACTION
                  | ONBOARDING_SAMPLE | MANUAL_PICK
```

> **이 목록은 05 §0의 사본입니다.** 어긋나면 **05가 정본**입니다 (위 절차 1). 05 §0에는 `reflectionStep`(E-69) ·
> `suggestionStatus`(E-81) · `ctaType`(E-76) · `authProvider`도 있으나 아직 이 목록에 옮기지 않았습니다 —
> 해당 슬라이스를 맡은 사람이 옮깁니다.
>
> **명명 규칙 (E-24):** 클라이언트 ↔ Spring은 camelCase, Spring ↔ AI `/chat`은 snake_case. 변환은 Spring `AiClient` 한 곳에서만 합니다.
> AI 레포 안에서는 "reflection"(회고)을 씁니다. Spring·클라이언트·문서는 "retrospect"를 유지합니다. 같은 개념입니다.

---

## 7. Flyway · 규칙 파라미터 (v1.3)

**마이그레이션은 `sottaejap-server`가 단독 소유합니다.** FastAPI는 스키마를 만들지 않습니다.

```
V1__init.sql                 users, transactions, retrospects, behavior_clusters,
                             goals, suggestions, monthly_snapshots, notifications
V2__time_slot_four_slots.sql time_slot 4구간 (E-50 · R13)
V3__drop_card_issuer.sql     transactions.card_issuer 제거 (E-52 · R14)
V4__kakao_login_user.sql     users.email nullable · nickname (E-56 · R15)
V5__push_subscriptions.sql   Web Push 구독 (E-68)
V6__chat_messages.sql        금융 Q&A 대화 이력 (E-67)
V7__suggestion_indexes.sql   (v2.6 · E-81) ix_suggestions_behavior · ix_suggestions_goal ·
                             uq_suggestions_proposed(behavior_id) WHERE status='PROPOSED'
V8__financial_chunks.sql     (v2.7 · E-85 · FR-12) CREATE EXTENSION vector · financial_chunks
                             (chunk_id UNIQUE · embedding vector(1536)). 벡터 인덱스는 두지 않는다
```

> **(v2.7)** 종전 목록의 `V2__seed_demo.sql` · `V3__financial_chunks.sql`은 실제 레포에 없습니다.
> 금융 RAG 테이블은 예고대로 **`V8__financial_chunks.sql`** 로 들어갔습니다.
> `V2__seed_demo.sql`은 여전히 없습니다.
> 브랜치를 파기 전에 `main`에서 다음 번호를 확인하십시오. `FlywayMigrationVersionTest`가 중복을 잡지만
> **병합한 뒤에야** 걸립니다.

**규칙 파라미터는 `sottaejap-server/src/main/resources/application.yml`의 `rules:` 블록에 모읍니다** (v1.3 — E-18).
`RuleParams`(`@ConfigurationProperties("rules")`)로 바인딩합니다. 9/7 튜닝이 값 주입만으로 끝나야 합니다.

```yaml
rules:                                                     # v2.2 — 잠정값은 ${RULES_X:기본값} 형태 (E-57). 근거 확보 후 값만 바꾼다
                                                           # (v2.8) 기본값은 환경변수 키가 없을 때만 붙는다. `RULES_X=`로 값만 비우면 null(리스트는 빈 리스트)이다
  shrinkage-k: ${RULES_SHRINKAGE_K:3}                      # #15 잠정 3
  rollup-min-count: ${RULES_ROLLUP_MIN_COUNT:3}            # #16 잠정 3
  pending-min-count: ${RULES_PENDING_MIN_COUNT:3}          # #17 잠정 3 (≤ rollup-min-count — 기동 시 검증). (v2.8) rollup보다 작게 두면 롤업된 리프가 RESOLVED로 상위 묶음과 함께 지도에 떠 같은 금액이 두 번 세어진다
  axis-x-boundary: ${RULES_AXIS_X_BOUNDARY:0.1}            # #18 잠정 — 월 합계 ÷ 예산 = 10%
  axis-y-boundary: ${RULES_AXIS_Y_BOUNDARY:0}              # #18 잠정 — 중립
  chat-window-days: ${RULES_CHAT_WINDOW_DAYS:3}            # 확정 v1.9 (E-48) — 채팅 회고 진입 노출 창, 오늘 포함
  sensitivity:                                             # E-46 프리셋 3종 — 이상치 중앙값 배수 (잠정)
    conservative: ${RULES_SENSITIVITY_CONSERVATIVE:3.0}
    standard: ${RULES_SENSITIVITY_STANDARD:2.0}
    sensitive: ${RULES_SENSITIVITY_SENSITIVE:1.5}
  candidate:                                               # ⓪ 후보 선별 #20 (E-62, 잠정)
    outlier-baseline-days: ${RULES_CANDIDATE_OUTLIER_BASELINE_DAYS:90}
    outlier-min-samples: ${RULES_CANDIDATE_OUTLIER_MIN_SAMPLES:5}
    big-amount-budget-ratio: ${RULES_CANDIDATE_BIG_AMOUNT_BUDGET_RATIO:0.1}
    repeated-low-min-count: ${RULES_CANDIDATE_REPEATED_LOW_MIN_COUNT:2}
  cluster:
    meal-categories: ${RULES_CLUSTER_MEAL_CATEGORIES:식사,식비,음식점,외식,배달,한식,중식,일식,양식,분식,패스트푸드,카페}   # B-10 · E-58 (`기타`는 코드가 아니라 규칙으로 포함). (v2.8) 빈 목록도 거부한다 — 비우면 모든 카테고리가 시간대 자리를 잃고 키가 퇴화한다
```
> **v2.2:** 현재 server 마이그레이션은 `V1__init` · `V2__time_slot_four_slots` · `V3__drop_card_issuer` · `V4__kakao_login_user`입니다 (01 §0-1 · §0-2). 위 목록의 `V2__seed_demo` · `V3__financial_chunks`는 v1.3 계획이며 번호는 다음 빈 번호를 받습니다. 회고 슬라이스(R16)는 **V5를 만들지 않습니다.**
> **코드에 숫자를 직접 쓰지 마십시오.** 9/7에 `grep`으로 찾아다니게 됩니다.
> 임베딩 모델·태그 유사도 임계값 설정은 E-20으로 삭제되었습니다.

---

## 8. 만족도 지도 렌더링 계약 (프론트)

```
x = point.burdenRatio            (월 합계 ÷ 월 예산)
y = point.adjustedSatisfaction   (-1 ~ +1)

if (point.evaluationStatus === 'PENDING')
    → 회색 반투명, 배지 없음, 툴팁만        # 색상 팔레트 밖 (E-11)
else
    → 색상 = point.verdict                # SUSTAIN | ADJUST — 2색
      배지 = 지켜요 | 바꿔볼까요

라벨 = `${point.name} (${point.retrospectCount})`
정렬 = ADJUST 먼저, 그 안에서 burdenRatio 내림차순
상세 패널 = prescription
          + `월 합계 ${point.monthlyTotalAmount}원 (평균 ${point.avgAmount}원 × ${point.txCount}회)`
          + (point.cta ? CTA 버튼 : 없음)
```
> **점 색상은 2색입니다.** 처방 문구(Great!/Awesome!!/Umm.../Hmm!)를 색상에 쓰지 마십시오.
> `boundaries`가 `null`이면 축 경계선을 그리지 않습니다 (미결 #18).

---

## 9. 브랜치 · 커밋 · PR (v1.7 — E-27 · NA-WA `CONTRIBUTING.md`와 동일)

```
main                                   배포 가능 상태만 · 통합 브랜치 겸용 · develop 없음
<type>/#<issue-number>-<short-desc>    type = feature · fix · refactor · chore · test · docs
                                       예: feature/#12-csv-parser · fix/#27-jwt-expiry
```

- **Issue 먼저.** Issue 제목과 PR 제목은 `[Type] 한글 설명` (`[Feat]` `[Fix]` `[Refactor]` `[Docs]` `[Chore]` `[Test]`).
- 커밋: `타입(범위): 한국어 설명 (#이슈)` — 예: `feat(parser): 국민카드 CSV 헤더 매핑 (#12)`. `Co-Authored-By` 트레일러 없음.
- 흐름: `Issue → 작업 브랜치 → main 대상 PR(Closes #이슈) → 통합 담당(고현석) 리뷰 → Squash and merge → 원격 브랜치 삭제`.
  squash commit 제목은 PR 제목을 그대로 씁니다. 리뷰·승인 전 작성자 직접 병합 금지, CI 실패 상태 병합 금지.
- **본선 30시간 중에는 Issue 없이 `main` 직접 push 허용** (리뷰 대기로 막히지 않도록). 그 밖의 기간에는 위 흐름.
- 세부는 각 레포 `CONTRIBUTING.md` §1~§4 (세 레포 동일). GitHub `main` 브랜치 보호(NA-WA는 승인 1명 필수)는 아직 켜지 않았습니다 — 켜면 본선 예외와 충돌하므로 시점은 별도 결정.

---

## 10. 리스크 — 9/1 안에 판단

### 리스크 1. Spring Boot 4.x 이관 비용 🔴

최신 GA는 **4.1.x**이고, 3.5.x는 **2026-06-30 OSS 지원이 종료**되었습니다.
다만 Spring Boot 4는 Spring Framework 7 / Spring Security 7 기반이라 **설정 방식이 3.x와 다릅니다.**
팀의 Spring 경험이 3.x라면, 카카오 OAuth2 설정에서 반나절이 사라질 수 있습니다.
`springdoc-openapi`도 Boot 3용 2.x가 아니라 **Boot 4 호환 라인**을 찾아야 합니다.

> **판단 방법:** 오늘 30분만 써서 `start.spring.io`로 Java 21 + Boot 4.1.1 프로젝트를 만들고
> **카카오 OAuth2 로그인 1회 성공**까지 가보십시오. 막히면 `Boot 3.5.16 + Java 21`로 내리고 문서에 기록합니다.
> 지원 종료는 해커톤 7일에는 아무 영향이 없습니다.
>
> **v2.1 (E-55):** `oauth2Login` 설정은 더 이상 이 리스크의 대상이 아닙니다. 스파이크는 Spring 없이 `curl`로 **인가 코드 → `kauth.kakao.com/oauth/token` → `kapi.kakao.com/v2/user/me`** 왕복을 먼저 확인하고, 같은 호출을 `RestClient`로 옮깁니다. Boot 버전과 무관합니다. ✅ **9/7 실측 통과** (06 §9/2 항목 1). 이 리스크는 종결.

### 리스크 2. pgvector — ✅ v2.7 개통 (E-21 · E-85 · E-86)

`docker-compose.yml`의 이미지는 처음부터 `pgvector/pgvector:pg18`이었고, `CREATE EXTENSION vector`와
`financial_chunks` 테이블은 **`V8__financial_chunks.sql`** 로 들어갔습니다 (v2.7). AI는 `DATABASE_URL`이
있을 때만 풀을 열어 `FINANCIAL_RAG`를 등록합니다 — 없으면 기동은 되고 금융 질문만 "확인할 수 없다"가 됩니다.
목적·동행인 정규화는 임베딩을 쓰지 않습니다 — AI 후보 + 사용자 확인 (E-20). 벡터 연산 라이브러리·임베딩 모델 설정 불필요.

### 리스크 3. 카카오 리다이렉트 URI 🟡

~~로컬(`localhost:8080`) · EC2 · Vercel 도메인 **셋을 카카오 개발자 콘솔에 모두 등록**해야 합니다.~~
~~**9/2에 EC2 도메인을 먼저 확보하고 등록**해 두십시오.~~
본선 당일 도메인이 바뀌면 로그인이 죽습니다.

> **v2.9 (9/8) 정정:** 위 취소선 두 줄은 E-55 이전의 **서버 리다이렉트**(`oauth2Login`)를 전제로 쓴 문장입니다.
> 리다이렉트 URI는 **클라이언트 콜백**입니다 (01 E-55 · §3). 서버 주소(`localhost:8080` · EC2)는 등록하지 않습니다.
> 카카오 개발자 콘솔에 등록할 값은 **클라이언트 주소 2개**입니다.
>
> | 환경 | 등록할 값 | 상태 |
> | --- | --- | --- |
> | 로컬 | `http://localhost:5173/auth/callback` | 등록 |
> | 운영 | `https://<Vercel 운영 도메인>/auth/callback` | 도메인 확보 후 등록 (06 §9/2 항목 3) |
>
> 세 곳의 값이 **글자까지 같아야** 합니다 — 카카오 콘솔 · server `KAKAO_REDIRECT_URIS` · client `VITE_KAKAO_REDIRECT_URI` (§3).
> **Vercel 프리뷰 배포 URL로는 로그인할 수 없습니다.** 배포마다 도메인이 달라지는데 카카오는 등록값과 정확히 일치할 때만 통과시킵니다. 운영 도메인 하나(또는 고정 별칭)를 정해 등록하고, 로그인은 그 도메인에서 확인하십시오.
> EC2 도메인은 그대로 확보해야 합니다 — 클라이언트가 서버를 부르는 `VITE_API_BASE_URL`과 server의 `AUTH_ALLOWED_ORIGINS`에 씁니다. 카카오 콘솔과는 무관합니다.

### 리스크 4. Spring ↔ AI 상호 호출의 중첩 타임아웃 🟡 (v1.3 신설)

Spring → `/chat` 안에서 AI가 다시 Spring 내부 API를 부릅니다 (E-19).
Spring 쪽 타임아웃(`AI_TIMEOUT_MS` 15초)이 AI 내부(LLM 6초 × 최대 2회 + Spring 조회 10초 × 호출 횟수)보다 짧으면 **정상 응답도 잘립니다.**
**규칙:** AI가 한 요청 안에서 Spring을 몇 번 순차 호출하는지 세고, 그 합보다 `AI_TIMEOUT_MS`를 크게 잡습니다. 9/7 실측 후 확정.
Tool Calling 도입 시 Spring 조회분(10초)을 포함해 이 예산을 다시 계산해야 합니다 (**아직 초과 상태**).
Spring MVC 스레드가 두 번 점유되므로(외부 요청 + AI의 내부 조회) 데모 규모에서는 문제없지만, 부하 테스트는 하지 않습니다.

---

## 11. 9/2 레포 부트스트랩 체크리스트 (v1.3)

| 담당 | 할 일 | 완료 |
|---|---|---|
| 오진호 | GitHub `sottaejab-ai` → **`sottaejap-ai` rename** (E-32) · 팀원 로컬 `git remote set-url origin https://github.com/jittaejap/sottaejap-ai.git` 공지 | ☑ rename (9/2 밤) · ☐ 공지 |
| 오진호 | GitHub 조직/레포 3개 생성 · 팀원 초대 (`sottaejap-*` 표기 통일 — E-32) | ☑ ai `f9d3c78` · ☑ server `f8e3067` · ☑ client `580f9ad` |
| 고현석 | `sottaejap-server` 스캐폴딩 · `docker-compose.yml`(`pgvector/pgvector:pg18`) · `V1__init.sql` | ☑ `f8e3067` |
| 고현석 | `AiClient` + `/internal-test/ai-ping` (POST /chat 왕복) · `/internal/ai/*` 컨트롤러 6종 뼈대 (05 §3) | ☑ 코드 `f8e3067` · ☑ 왕복 실측 (9/2 밤 · 200 · 역방향 GET 5종 200) |
| 고현석 | Spring README 첫 줄 "규칙 엔진(결정론적) 소유" (E-30) | ☑ |
| 정민규 | `rules/` 패키지 뼈대 + `RuleParams` + **`RuleEngineDeterminismTest` 빈 테스트 통과** (E-29) | ☑ `f8e3067` |
| 오진호 | `sottaejap-ai` — `Satisfaction`에서 `MEDIUM` 삭제 · `purpose/companion` 표준 태그 enum · `TaskType` 2종 추가 · `ChatResponse.fallback` · `app/ai/fallback.py` (06 R1~R4) | ☑ `66d1e62` |
| 오진호 | `sottaejap-ai` — `.env.example` 3줄 추가 · `requirements.lock` · `Dockerfile` · `SpringClient` 경로 6종 연결 · `/chat` 시크릿 검사 · `AGENTS.md`·`CONTRIBUTING.md`·`.github` (06 R5·R6) | ☑ `66d1e62` |
| 석정한 | `sottaejap-client` 스캐폴딩 · 라우터 8개 · `vite-plugin-pwa` | ☑ `580f9ad` |
| 석정한 | **3사 CSV 헤더 확보** (⚠️ 9/3 파서 착수 — 실질 마감 9/2) | ☐ |
| 오진호 | `AI_SHARED_SECRET` 생성·공유 · OpenAI 키 발급 (E-25 확정) | ☐ |
| 전원 | `.env.example` 커밋 · `.env` gitignore 확인 | ☑ ai · ☑ server · ☑ client |
| 전원 | **lockfile 커밋** — `package-lock.json` · `gradle/wrapper` · `requirements.lock` | ☑ client · ☑ server · ☑ ai `66d1e62` |
| 고현석 | **`.gitattributes` 3개 레포에 커밋** · 각자 `core.autocrlf` 설정 (§5) | ☑ 3개 레포 · ☐ autocrlf 각자 |
| 전원 | Docker Desktop **WSL2 백엔드**(Windows) · 포트 8080/5173 예약 충돌 확인 | ☐ |
| 오진호 | **AI 레포 v1.6 변경 커밋·푸시** — R1~R6 · 문서 · `/health` 문자열 | ☑ `main` `66d1e62` |

> **9/2 종료 시점의 유일한 성공 기준:** `curl localhost:8080/internal-test/ai-ping` → 200 (`reply` 포함).
> 나머지가 다 늦어도 이것만 되면 남은 일주일이 굴러갑니다.

---

## 12. EC2 운영 메모리 배분 (v2.15 — E-111)

운영은 **AWS EC2 한 대**입니다. **메모리 1.9 GiB · swap 2 GiB**(`free -h` 실측 2026-09-09),
루트 볼륨 24GB. 그 한 대에 `deploy/docker-compose.yml`의 컨테이너 **셋(`db` · `server` · `ai`)이 모두** 뜹니다.
이미지는 EC2에서 굽지 않습니다 — GitHub Actions가 구워 Docker Hub에 올린 것을 받습니다.

> **문서에 남아 있던 "t3.micro · 1 GiB"는 실측과 달랐습니다.** `deploy.yml` 주석과 README의
> "프리티어 메모리" 서술을 그대로 믿고 사이징하면 힙을 **줄이는** 값(320m)이 나옵니다 — 실제로
> 그렇게 잡았다가 `docker stats` 실측에서 되돌렸습니다. **값은 문서가 아니라 EC2에서 재고 적습니다.**

### 12-1. 값

| 항목 | 값 | 자리 |
|---|---|---|
| JVM 힙 (`server`) | **`-Xmx512m`** | `sottaejap-server/deploy/docker-compose.yml` → `server.environment.JAVA_TOOL_OPTIONS` |
| 컨테이너 메모리 상한 (`server`) | **`mem_limit: 1g`** | 같은 파일 `server.mem_limit` |
| `db` · `ai` | 상한 없음 | 실측 없이 postgres에 천장을 씌우면 없던 OOM-kill을 만듭니다 |

**힙과 상한은 같은 파일 같은 자리에 둡니다.** 한쪽만 고치면 다른 쪽이 조용히 어긋납니다.

### 12-2. 왜 `Dockerfile`이 아니라 compose인가

같은 이미지를 로컬에서도 씁니다 (§4 A안). `Dockerfile` ENTRYPOINT에 `-Xmx512m`을 박으면
16GB 개발 머신도 512MB로 묶입니다. **운영 사이징은 운영 compose가 갖습니다.**

### 12-3. 왜 명시해야 하는가

적지 않으면 힙은 `MaxRAMPercentage` 기본 **25%** 로 "컨테이너에 보이는 메모리"에 딸려 다닙니다.
지금 호스트가 1.9 GiB이므로 힙은 약 **476MB**이고, **인스턴스를 바꾸면 아무도 모르게 힙이 따라
움직입니다.** `mem_limit`만 넣고 `-Xmx`를 빼면 힙이 그 상한의 25%로 **더 줄어듭니다.**

명시의 목적은 힙을 **줄이는 것이 아니라 못 박는 것**입니다 — 512m은 명시 전 실효값(476MB)보다
크게 잡았습니다. XLSX가 500이 되던 문제는 힙이 아니라 **선계수**가 막습니다(E-111 — 힙 256m에서도 400).

`mem_limit`은 예약이 아니라 **천장**입니다. 세 컨테이너 천장의 합이 호스트를 넘어도 실제 사용량은
그대로입니다. 넘어서면 swap을 먼저 쓰고(Docker 기본 `memswap_limit` = 2 × `mem_limit`) 그 다음에야
**그 컨테이너만** 죽습니다. 상한이 없으면 대신 호스트 OOM killer가 고르는데, 그때 제일 큰 것은
대개 postgres입니다. `server`에는 `restart: unless-stopped`가 붙어 있습니다.

### 12-4. 값의 근거 — EC2 실측 (2026-09-09 · 명시 전 상태)

| 컨테이너 | 실사용 (`docker stats --no-stream`) |
|---|---|
| `server` | **497.7 MiB** |
| `ai` | 62.8 MiB |
| `db` | 39.7 MiB |
| 호스트 (`free -h`) | used 1.1 GiB / total 1.9 GiB · swap 52 MiB / 2 GiB |

`server` 상한은 실사용의 **약 두 배**로 둡니다. 상한을 실사용에 바짝 붙이면 업로드가 몰릴 때
컨테이너가 통째로 죽습니다 — 처음에 640m으로 잡았다가 여유가 143MB뿐이라 1g으로 올렸습니다.

**업로드가 이 힙에 드는지는 따로 실측했습니다** (작성자 로컬 · JDK 21 · `TransactionFileParser`):

| 파일 | 힙 256m | 힙 320m |
|---|---|---|
| 200,000행 xlsx (상한 초과) | 0.22초 **400** | 0.15초 **400** |
| 20,000행 xlsx (상한 경계) | 0.60초 정상 | 0.90초 정상 |
| 1,200행 xlsx (본선 데모 규모) | 0.09초 정상 | 0.28초 정상 |

운영 힙 512m은 이보다 넉넉합니다. 고치기 전에는 같은 200,000행 파일이 512m에서 5.9초 뒤
`OutOfMemoryError` → **500**이었습니다 (E-111).

### 12-5. 값을 바꿀 때

1. EC2에서 `docker stats --no-stream`과 `free -h`로 실제 사용량을 봅니다.
2. `deploy/docker-compose.yml`의 두 값을 함께 고칩니다.
3. **이 문서의 §12-1 · §12-4 표를 같은 PR에서 고칩니다.** 문서 값과 compose 값이 다르면 둘 다 못 믿습니다.
