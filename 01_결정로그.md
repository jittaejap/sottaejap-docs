# 소때잡 — 결정 로그

**팀:** 지때잡 | **프로젝트:** 소때잡 | **버전:** v1.10 | **기준일:** 2026-09-07

> 이 문서가 **단일 진실 공급원(Single Source of Truth)** 입니다.
> 다른 모든 문서(요구사항·IA·ERD·API)와 외부 산출물(구글 시트·피그마)의 값은 여기서 파생됩니다.
> 값이 충돌하면 이 문서가 우선합니다.

---

## 0. v1.10 변경 요약 (2026-09-07 — 카카오 로그인 흐름 · 소셜 계정 식별)

> v1.10은 **카카오 로그인을 어떤 흐름으로 붙이는가**를 정한 판입니다. E-15(카카오 1종 + 데모 계정 폴백)의 범위는 그대로이고, 흐름과 계정 식별 방식만 확정했습니다. `POST /auth/login` 본문이 바뀌므로 **계약 변경**입니다 (07 §6 절차 — 05를 같이 고쳤습니다).
> 별도 회원가입 API는 없습니다. 카카오 첫 로그인에서 서버가 사용자를 만듭니다.

| # | 항목 | v1.9 | **v1.10 확정** |
|---|---|---|---|
| E-51 | 카카오 로그인 흐름 | 07 E-15 표기 "Spring Security OAuth2 Client" — 서버 리다이렉트(`oauth2Login`)를 암묵 가정 | **클라이언트가 인가 코드를 받고, 서버가 카카오와 교환한다.** ① 클라이언트가 `kauth.kakao.com/oauth/authorize`로 이동 (`state` 난수는 클라이언트가 만들고 검증) ② 카카오가 **클라이언트의** `/auth/callback`으로 `code`를 돌려줌 ③ 클라이언트가 `POST /auth/login { provider: KAKAO, code, redirectUri }` ④ 서버가 `kauth.kakao.com/oauth/token`에서 토큰을 교환하고 `kapi.kakao.com/v2/user/me`로 프로필을 조회한 뒤 **우리 JWT만** 발급. Spring `oauth2Login`은 쓰지 않는다 — 세션이 `STATELESS`(05 §0)이고 클라이언트(Vercel)와 서버(EC2)가 다른 도메인이라 세션 기반 인가 요청 저장소가 깨진다. 카카오 액세스 토큰은 프로필 조회에만 쓰고 저장하지 않는다. 오류 코드 `OAUTH_CODE_INVALID`(400) · `OAUTH_PROVIDER_ERROR`(502) 신설 — 05 §0 |
| E-52 | 소셜 계정 식별 · 이메일 · 표시 이름 | `users.email NOT NULL UNIQUE` — 이메일이 항상 있다고 가정. 표시 이름 없음 | **`email`은 nullable. 계정 식별은 `(authProvider, providerUserId)` 유일 제약. `nickname` 신설.** 카카오 이메일은 선택 동의 항목이라 비어서 올 수 있다. 같은 이메일이라는 이유로 계정을 합치지 않는다 (V1 주석 유지). 데모 계정(`LOCAL`)은 `providerUserId`가 없으므로 유일 제약은 `providerUserId IS NOT NULL`인 행에만 건다 — **V1 `uq_users_provider`에 이미 있다** (9/7 확인, V3는 만들지 않는다). `nickname`은 카카오 `properties.nickname`(기본 동의 항목)을 저장하고 마이페이지 `1. 프로필`(03 S13)에 표시한다 — 이메일은 선택 동의라 프로필 표시를 기댈 수 없다. nullable이며 비면 클라이언트가 `사용자`로 대체. 데모 계정은 V3에서 `데모 사용자`로 채운다. 스키마는 server `V3` 마이그레이션 — 04 User |

> E-51로 `KAKAO_REDIRECT_URI`(서버 콜백 1개)는 **`KAKAO_REDIRECT_URIS`(허용할 클라이언트 콜백 목록)** 로 바뀝니다 — 07 §3. 서버는 클라이언트가 보낸 `redirectUri`를 이 목록으로 검사하고, 카카오도 콘솔에 등록된 URI와 대조합니다. `KAKAO_CLIENT_SECRET`은 서버에만 있습니다.
> 카카오 콘솔 준비(앱 생성 · Client Secret 사용 · 동의항목 **닉네임(필수) · 이메일(선택)** · 리다이렉트 URI 3개)는 06 §9/2 항목 3의 후속입니다. 닉네임을 나중에 추가하면 이미 로그인한 계정의 재동의가 필요하므로 처음부터 받습니다.
> server `build.gradle.kts`의 `spring-boot-starter-security-oauth2-client`는 E-51로 쓰이지 않으므로 V3 반영 때 제거합니다.

---

## 0-0. v1.9 변경 요약 (2026-09-03 — 회고 진입 경로 · 시간대 4구간)

> v1.9는 **회고를 어디서 몇 건 시작할 수 있는가**를 경로별로 나눈 판이고, 그 과정에서 **시간대 구간이 3개에서 4개로** 바뀌었습니다. 시간대는 세 레포의 공유 enum이므로 **계약 변경**입니다 (07 §6 절차 — 05를 먼저 고쳤습니다).
> 선별 규칙의 수치(이상치 기준선 기간 · 큰 금액 기준 비율 · 표본 최소 건수)는 여기서 정하지 않습니다. 근거 없는 임의값을 늘리지 않도록 06 §데이터 확보 후 **#20**으로 넘겼습니다.

| # | 항목 | v1.8 | **v1.9 확정** |
|---|---|---|---|
| E-48 | 회고 진입 경로와 노출 범위 | "1회 1건"(B-7) 하나로 뭉뚱그림 | **경로 3종으로 분리.** ① 인앱 알림 = **1일 1건** (FR-10-01) ② AI 채팅 회고 진입 = **오늘 포함 최근 3일**의 후보 전부 ③ 사용자가 채팅에서 날짜·기간을 지정하거나 거래내역에서 직접 고르면 **범위 제한 없음**. 회고 자격에 **상한(오래된 거래 제외)은 두지 않는다** |
| E-49 | 후보 제외(`skip`) | 제외 상태를 저장한다고 암묵 가정 | **상태를 저장하지 않는다.** 알림 읽음 처리로 갈음한다. 스킵은 "다시는 묻지 마"가 아니라 "지금은 말고"이고, 사용자는 같은 거래를 채팅·거래내역에서 다시 회고할 수 있어야 한다 — 별도 컬럼은 그 길을 막는다 |
| E-50 | 시간대 구간 | **3개** — `MORNING` 05~12 · `AFTERNOON` 12~22 · `NIGHT` 22~05 | **4개 — `MORNING` 05~11(6h) · `DAY` 11~17(6h) · `EVENING` 17~22(5h) · `NIGHT` 22~05(7h).** `AFTERNOON` 폐기 |

> ⚠️ **E-50은 브레이킹 체인지입니다.** `AFTERNOON`을 쓰는 코드가 세 레포에 있고, server는 `transactions.time_slot`의 `CHECK` 제약과 **이미 저장된 값**까지 바꿔야 합니다 (V2 마이그레이션). 반영은 06 §레포 조치 **R13**.
> 네 구간의 합은 6 + 6 + 5 + 7 = 24시간으로 맞습니다. `NIGHT` 22~05는 **7시간**입니다.
> E-48은 새 제약이 아니라 **제약을 걷어낸 결정**입니다. 선별은 "회고할 자격"이 아니라 "서비스가 먼저 말을 거는 시점과 개수"만 정합니다.

---

## 0-1. v1.8 변경 요약 (2026-09-03 — AI taskType 확장)

> v1.8은 FR-12(금융 지식 Q&A, `/chat/finance`) 처리를 위해 AI `taskType`을 6종으로 확장한 판입니다. 스택·규칙 엔진은 v1.7 그대로입니다.

| # | 항목 | v1.7 | **v1.8 확정** |
|---|---|---|---|
| E-47 | AI `taskType` | 5종(REFLECTION/ANALYSIS/ACTION_PLAN/CLUSTER_NAMING/ANALYSIS_NARRATE) | **6종으로 확장 — `FINANCE_QA` 신설.** `task_context.state`는 거의 빈 객체(`{}`) — 질문은 `message`, 맥락은 `recent_messages`. 출처는 `reply` 문장에 자연스럽게 언급하고 별도 필드로 분리하지 않는다(LLM이 정확한 문서명·연도를 재현하다 오탈자·환각을 낼 위험 방지). 05 §0·§3 반영 |

---

## 0-2. v1.7 변경 요약 (2026-09-03 — 와이어프레임 v1 검수 · UI 자산 확정)

> v1.7은 **와이어프레임 v1(피그마 `Z9isZcGBA6XWAUcmeOJzJp` 페이지 `19-2` · 21화면)** 을 이 문서·02·03과 대조한 결과와, 그 과정에서 확정한 UI 자산 2건을 기록한 판입니다. 스택·API·규칙 엔진은 v1.6 그대로입니다.
> 화면별 수정 지시는 **03 §5-2 (W-1~W-17 · D-1~D-7)** 에 있습니다. 피그마가 이 문서와 다르면 피그마를 고칩니다.

| # | 항목 | v1.6 | **v1.7 확정** |
|---|---|---|---|
| E-43 | 아이콘 세트 | 미명시 (와이어프레임은 Lucide + 이모지 혼용) | **Tabler Icons 단일 세트.** 클라이언트는 `@tabler/icons-vue` 3.46(설치 완료)만 쓴다. 이모지·Lucide·직접 그린 SVG를 아이콘 자리에 쓰지 않는다. 치환표는 03 §5-3 |
| E-44 | 서체 | 미명시 (와이어프레임은 Inter) | **Pretendard.** 피그마 텍스트 스타일과 클라이언트 전역 서체를 모두 Pretendard로 통일한다. 숫자·영문에 별도 서체를 두지 않는다. 적용 방법은 03 §5-3 |
| E-45 | 온보딩 단계 수 | 5단계 | **4단계 — 목표 자금 → 월 예산 + 분석 민감도 → 거래내역 업로드 → 표본 회고.** 예산과 임계값을 한 화면에 묶는다 (Q-1 채택, 9/3). `onboardingCompleted`는 4단계 완료 시 |
| E-46 | 이상치 임계값 입력 | 숫자 임계값 직접 입력 (02 FR-01-04) | **프리셋 3종 — 보수적 · 기본 · 민감하게.** 사용자는 프리셋만 고르고, 프리셋 → 숫자 매핑은 Spring `rules.sensitivity.{conservative,standard,sensitive}` 파라미터 세트로 둔다. 값은 06 §데이터 확보 후 #15~18과 함께 9/7 확정 (Q-2 채택, 9/3) |

> 검수에서 드러난 설계 위반 10건은 **새 결정이 아니라 기존 결정의 미반영**입니다 — 점 색상 2색(E-4·E-11) · 처방 문구 상세 패널 전용(E-4) · 사분면 비노출(§4) · 카카오 1종 로그인(E-15) · 목적 7종(§3) · 월 예산 필수(02 FR-01-03) · 전월 대비(02 FR-08-06) · 업로드 형식 CSV/XLSX(02 FR-02-01) · 탭명 `지도`(§9) · P0 화면 3장 누락(03 S5·S10·S11). 값은 바뀌지 않았고 피그마가 따라옵니다.
> Q-1·Q-2는 9/3 팀장 결정으로 **E-45·E-46**에 종결했습니다. §9 온보딩 행도 갱신했습니다.

---

## 0-3. v1.6 변경 요약 (2026-09-02 밤 — AI 레포 조치 R1~R6 · R10 완료)

> v1.6은 **`sottaejap-ai` 레포가 06 §레포 조치 R1~R6·R10을 구현한 결과**를 기록한 판입니다. 스택·의존성은 바꾸지 않았습니다. 다른 항목은 v1.5 그대로입니다.
> 세 레포 모두 스캐폴딩이 끝났습니다 — `sottaejap-ai` `f9d3c78` · `sottaejap-server` `f8e3067` · `sottaejap-client` `580f9ad`. AI 레포의 v1.6 변경은 `66d1e62`로 `main`에 푸시했습니다. server `AGENTS.md`도 `e7bc91e`로 맞췄습니다.

| # | 항목 | v1.5 | **v1.6 확정** |
|---|---|---|---|
| E-37 | AI `/chat` 인바운드 인증 | "두 방향 모두 헤더" (AI 쪽 거부 규칙 미명시) | **`X-Internal-Secret`이 없거나 다르면 401. `INTERNAL_SHARED_SECRET`이 비어 있으면 모든 `/chat`을 401로 거부** — Spring v1.5 규칙과 대칭. `/health`만 열림 |
| E-38 | LLM 미설정 시 동작 | 미정 | **`OPENAI_API_KEY`가 비어 있어도 `/chat`은 템플릿 응답 + `fallback: true` + 200.** P0 회고 경로가 LLM 없이 완주하고, 키 없이도 `ai-ping`이 200 — **9/2 밤 실측 확인** |
| E-39 | `SpringClient` 응답 처리 | "dict로 받으므로 변환하지 않는다" | **봉투 `{ success, data }`를 벗겨 `data`만 Tool에 넘김.** `success: false`는 `SpringApiError(code)`. 키는 camelCase 그대로 |
| E-40 | `requirements.lock` 생성 방식 | `pip freeze` | **pip 교차 해석 `--python-version 3.12`** — 로컬 파이썬이 3.14여도 3.12 기준으로 고정. Docker 불필요 (07 §11) |
| E-41 | 표준 태그 enum 값 | (미명시) | **한국어 문자열 그대로** (`"충동"` · `"혼자"`). `/chat`의 `tool_results[].data`와 Spring 저장값이 같은 표기 |
| E-42 | 레포명 | E-32 (rename 예정) | **GitHub `jittaejap/sottaejap-ai` rename 완료 (9/2 밤).** 구 URL은 GitHub가 리다이렉트. `/health`의 `service` = `sottaejap-ai-server` |

> 레포 조치 완료 현황 — **R1~R6 · R10 (ai) ✅ · R7~R9 (server) ✅** (server 스캐폴딩 `f8e3067`에 포함). 남은 것은 **R11(docs)** 뿐입니다 (06 §레포 조치).
> **9/2 성공 기준 `ai-ping` 200 — 실측 통과** (Spring `f8e3067` ↔ AI v1.6, 로컬 · OpenAI 키 없음). 06 §8 · 07 §4.
> 폴백 템플릿 문구(`app/ai/fallback.py`)는 초안입니다. 담당 오진호가 다듬습니다.

---

## 0-4. v1.5 변경 요약 (2026-09-02 저녁 — 서버 스택 재확인)

> v1.5는 **서버 스캐폴딩 직전에 Maven Central · start.spring.io · Adoptium · Docker Hub에서 스택을 재조회**한 결과를 기록한 판입니다. 다른 항목은 v1.4 그대로입니다.

| # | 항목 | v1.4 | **v1.5 확정** |
|---|---|---|---|
| E-34 | 서버 Java | Java 25 | **Java 21 (LTS, Temurin 21.0.12.1)** — 팀 결정. Boot 4.1은 17~26 지원. 팀원 로컬에 21이 설치되어 있고 25로 올릴 이유가 없음. 07 §1·§5-4 |
| E-35 | springdoc-openapi | 3.0.3 | **3.1.0** — 3.0.3은 Boot **4.0.5** 기준 빌드, 3.1.0이 Boot **4.1.0** 기준 빌드. Boot 4.1과 짝이 맞는 것은 3.1.0 |
| E-36 | Spring Boot · Gradle 고정값 | 4.1.x · wrapper 9.3.1 | **Boot 4.1.1** (4.2.0-M1은 마일스톤이라 제외) · **Gradle wrapper 9.7.1** |

> Boot 4.1.1이 관리하는 버전(그대로 따름): Spring Framework 7.0.9 · Security 7.1.1 · Data JPA 4.1.1 · Hibernate 7.4.5 · Flyway 12.4.0 · **Jackson 3.1.5 (`tools.jackson` 패키지)** · Tomcat 11.0.24 · JUnit 6.0.3 · PostgreSQL JDBC 42.7.13. Boot가 관리하지 않아 직접 고정: jjwt 0.13.0 · springdoc 3.1.0.

---

## 0-5. v1.4 변경 요약 (2026-09-02 오후 — 레포명 · 클라이언트 스택)

> v1.4는 **클라이언트 스캐폴딩 시점의 팀 결정 2건**을 기록한 판입니다. 다른 항목은 v1.3 그대로입니다.

| # | 항목 | v1.3 | **v1.4 확정** |
|---|---|---|---|
| E-32 | 레포명 표기 | `sottaejab-*` (E-28) | **`sottaejap-*`** — 팀 결정. GitHub `jittaejap/sottaejab-ai`는 **`sottaejap-ai`로 rename** (오진호 · 06 R10). Java 패키지 `kr.sottaejap.server`, DB명 `sottaejap`도 같이 통일 |
| E-33 | 클라이언트 언어·스타일 | 미확정 (07 트리는 `.js`) | **TypeScript `~6.0.3`** (7.x 금지 — `vue-tsc`·`typescript-eslint` 미지원) · **Tailwind 4** + `tokens.css` 토큰 · 07 §2 **평면 구조 유지**, 의존 방향 `views → components·composables·stores → api` |

> E-32는 E-28을 대체합니다. 문서 안의 `sottaejab-*` 표기는 v1.4에서 일괄 정정했습니다. AI 레포의 `/health` 문자열도 v1.6에서 `sottaejap-ai-server`로 정정했습니다 (E-42).

---

## 0-6. v1.3 변경 요약 (2026-09-02 정합성 감사 3차 — 레포 우선)

> v1.3은 **문서 v1.2 ↔ `sottaejab-ai` 레포(`f9d3c78`, 9/2 13:24)** 대조에서 드러난 불일치를 **레포 기준으로** 종결한 판입니다.
> 팀 결정: **코드가 앞서 간 곳은 문서가 따라간다.** 단, 설계 원칙이 걸린 4건(E-18 담당 · E-20 · E-22 · E-23)은 팀장이 직접 결정했습니다.

| # | 항목 | v1.2 | **v1.3 확정** |
|---|---|---|---|
| E-18 | 규칙 엔진 위치 | FastAPI(정민규) | **Spring(Java) — 정민규 구현 · 고현석 통합·리뷰.** FastAPI는 Agent·자연어·Tool·RAG·설명만 |
| E-19 | Spring ↔ FastAPI 호출 방향 | Spring → `/internal/*` 7종 (push) | **Spring → `POST /chat` 1종 + FastAPI → Spring 내부 AI API 6종 (pull)** — 05 §3 |
| E-20 | 목적·동행인 정규화 | 임베딩 코사인 최근접 (E-16) | **AI가 표준 태그 후보를 제안하고 사용자가 확인한 값만 저장.** 자유 문자열 저장 금지. E-16 폐기 |
| E-21 | pgvector | 드롭 (E-16) | **복원 — 금융 RAG 저장소 (P2 착수 시 설치).** A-2 = PostgreSQL + pgvector |
| E-22 | 금융 지식 RAG | 범위 외 | **범위 내 P2 — FR-12 신설.** NFR-05 규제 경계 유지 |
| E-23 | 만족도 enum 문자열 | `SATISFIED/UNSATISFIED/UNSURE` | **`HIGH/LOW/UNKNOWN`** — 3택 유지, 레포의 `MEDIUM`은 **삭제** |
| E-24 | AI 경계 명명 규칙 | camelCase · retrospect · `IN_PROGRESS` | **`/chat` 경계는 snake_case · reflection · `ACTIVE`.** Spring 외부 API는 camelCase 유지, 변환은 Spring `AiClient` 한 곳 |
| E-25 | LLM 제공자 | 미정 | **OpenAI `gpt-4o-mini`** (기획서와 동일). 임베딩 모델은 RAG(P2) 착수 시 확정 |
| E-26 | AI 레포 스택 | Python 3.13 · uv · pyproject | **Python 3.12+ · requirements.txt · venv** |
| E-27 | 브랜치 | `main/develop/feat/*` | **`main/feature/*`** — `develop` 없음 |
| E-28 | 레포명 표기 | `sottaejab-*` | **`sottaejab-*`** (GitHub 실제명 `jittaejap/sottaejab-ai`) |
| E-29 | NFR-01 증명 테스트 | `tests/test_rules_determinism.py` (Python) | **Spring `RuleEngineDeterminismTest` (JUnit)** |
| E-30 | "규칙 엔진 명시" README | AI 레포 첫 줄 | **Spring 레포 README 첫 줄** — "규칙 엔진(결정론적) 소유". AI 레포 첫 줄은 현행 유지 |
| E-31 | 절감액·목표 배분 계산 (⑦) | 오진호 (FastAPI) | **정민규 (Spring 규칙 엔진 일부)** — 오진호는 AI 레포 전담 |

> **양보하지 않은 것 (레포와 무관하게 유지):** FR-04-15 LLM 폴백 P0 · NFR-02(`reasonCode`는 Spring 산출, AI는 재구성만) · E-11·E-12·E-5 판정 층위와 `Out` 금지 · NFR-05 규제 경계 · E-17 OS 혼용 규칙.
> 이 항목들은 레포가 **추가로 구현**해야 합니다 — `06_액션시트.md` §레포 조치 R1~R9. **→ v1.6에서 R1~R10 완료.**

---

## 0-7. v1.2 변경 요약 (2026-09-01 정합성 감사 2차)

> v1.2는 **문서 ↔ 구글 시트 ↔ 피그마 3원 대조**에서 드러난 불일치를 종결한 판입니다.

| # | 항목 | v1.1 | **v1.2 확정** |
|---|---|---|---|
| E-1 | 만족도 지도 가로축 분자 | "묶음 금액"(01) / "묶음 평균 금액"(03) / `avgAmount`(04) — 3중 불일치 | **분석 기준월 1개월 묶음 합계 금액 ÷ 월 예산** |
| E-2 | `verdict` enum | `KEEP`/`CHANGE`/`PENDING` — `quadrant.KEEP`과 문자열 충돌 | **`SUSTAIN`/`ADJUST`** (E-11과 함께 2종으로) |
| E-3 | 처방 문구 종수 | §4 안에서 "5종"·"4종" 자체 모순 | **5종 (PENDING 포함)** |
| E-4 | 판정 표시 층위 | 피그마에서 판정과 5문구가 1:1 오결합 | **판정 2색 + 보류 상태 / 처방 5문구 — 노드 분리** (E-11 참조) |
| E-5 | `Out` 표기 | 시트 FR-07-04·피그마 2-4에 잔존 | **전면 삭제 → `PENDING`(더 알아볼게요)** |
| E-6 | "ai 둘러대기" 표기 | 시트 FR-03-05 | **삭제** — `reasonCode` 재구성으로 통일 (NFR-02) |
| E-7 | IA 번호 체계 | 문서 `3-1-x` / 피그마 `4-x` 충돌 | **탭 기준 재정비** — §9 |
| E-8 | 소비 분석 화면 | 피그마에만 존재, FR·API 없음 | **FR-11 신설 · P1 · 화면 S15 · `GET /analysis`** |
| E-9 | PWA · SNS 로그인 | 피그마에만 존재 | **범위 내 확정** — FR-01-01 확장 / NFR-08 신설 |
| E-10 | 심야 배달 단가 | 12,000원 vs 23,000원 미해소 | **12,000원(데모 데이터 기준)으로 통일** |
| E-11 | 판정 라벨 구조 | 3라벨 안에 **판정 2종 + 상태 1종**이 혼재 | **판정 `verdict` 2종 / 보류는 `evaluationStatus`로 분리** |
| E-12 | `PROTECT` 비대칭 | Ⅰ·Ⅱ 배지 동일한데 행동은 Ⅰ에만 존재 | **배지 동일 유지 / 상세 패널에만 '예산 확보' CTA** |
| E-13 | 프론트엔드 스택 | 미확정 | **Vue 3 + Vite + Pinia** (PWA — `vite-plugin-pwa`) |
| E-14 | 레포 구성 | 미확정 | **3개 레포 분리** — `sottaejap-client` / `sottaejap-server` / `sottaejap-ai` (표기는 E-28 → **E-32**로 `sottaejap-*` 확정) |
| E-15 | SNS 로그인 구현 수준 | 미확정 | **카카오 OAuth 1종 실구현** + 데모 계정 폴백 |
| E-16 | pgvector | A-2에 있으나 FR 70건 중 사용처 0 | ~~**드롭.** 목적·동행인 정규화는 **태그 임베딩 + 코사인 최근접**으로 결정론화~~ → ⚠️ **v1.3 E-20·E-21로 대체** |
| E-17 | 개발 환경 | 미확정 | **macOS + Windows 혼용** — 인코딩·줄바꿈·도구 규칙은 `07` §5 |

---

## 1. 아키텍처

| # | 항목 | 확정 |
|---|---|---|
| A-1 | 백엔드 | **Spring Boot + FastAPI 병행** |
| A-2 | DB | **PostgreSQL 단일 DB + pgvector 확장** (MySQL 제외 · pgvector는 금융 RAG용, P2 착수 시 설치 — **E-21**) |
| A-3 | 사전 개발 | **허용 — 최대한 진행** |
| A-4 | 배포 | Docker Compose · AWS EC2 · Vercel |
| A-5 | 마이그레이션 | **Flyway** |
| A-6 | 규칙 엔진 위치 | **Spring (Java) — 정민규 구현 · 고현석 통합·리뷰** (v1.3 — E-18). FastAPI는 AI 표현 계층만 |
| A-7 | 클라이언트 형태 | **PWA (웹 우선, 설치형 진입 지원)** — v1.2 확정, NFR-08 |
| A-8 | 프론트엔드 | **Vue 3 + Vite + Pinia** (E-13) — 상세는 `07_기술스택_레포구성.md` |
| A-9 | 레포 | **3개 분리** (E-14 · 표기 E-32) — `sottaejap-client`(Vue) / `sottaejap-server`(Spring + 규칙 엔진) / `sottaejap-ai`(FastAPI) |
| A-10 | 런타임 | **Java 21 (LTS)** (v1.5 — E-34) · Spring Boot **4.1.1** (E-36) · **Python 3.12+** (E-26) · **Node 24 (LTS)** · PostgreSQL 18.6 |
| A-12 | 개발 환경 | **macOS + Windows 혼용** (E-17) — `.gitattributes` LF 강제 · UTF-8 강제 · CSV 두 인코딩 테스트 |
| A-11 | 인증 | **카카오 OAuth 1종 실구현** + 데모 계정 폴백 (E-15) · 흐름 = 클라이언트 인가 코드 → 서버 교환 (v1.10 — E-51) · 계정 식별 `(authProvider, providerUserId)` · 이메일 nullable · `nickname` 저장 (E-52) |
| A-13 | AI 레포 역할 | **Single Agent · 자연어 이해 · Tool Calling · 금융 RAG · 설명** — 계산·판정 금지 (v1.3 — E-18) |
| A-14 | LLM 제공자 | **OpenAI `gpt-4o-mini`** (v1.3 — E-25) |

> A-6 부속 결정 (v1.3): 선별 쿼리와 규칙 엔진이 **같은 레포(Spring)** 에 있습니다.
> 회고 **후보 선별(D+1 필터·이상치 검출) 쿼리는 고현석**, 그 외 규칙 엔진(②③④⑦⑧)은 **정민규**가 Java로 구현합니다.

---

## 2. 역할 경계 — AI와 규칙 엔진

가장 중요한 구조적 결정입니다. **기획서의 "같은 데이터에 항상 같은 분석 결과" 주장을 지키는 방어선**입니다.

| 단계 | 담당 | 위치 | 성격 |
|---|---|---|---|
| ⓪ 회고 후보 선별 (D+1·이상치·임계값) | **규칙 엔진** (구현: 선별 쿼리·고현석) | Spring | 결정론적 |
| ① 목적·동행인 자유입력 → 표준 태그 | **AI 후보 제안 + 사용자 확인** (오진호 · 석정한) | FastAPI(후보) · 클라이언트(확인) | 표현 + 확인 (E-20) |
| ② 묶음 키 조합 (카테고리+시간대+목적+동행인) | **규칙 엔진** (정민규) | Spring | 결정론적 |
| ③ 롤업 판단 | **규칙 엔진** (정민규) | Spring | 결정론적 |
| ④ 축소 추정 보정 · 판정 | **규칙 엔진** (정민규) | Spring | 결정론적 |
| ⑤ 묶음 이름 붙이기 | **AI** (오진호) | FastAPI | 표현 |
| ⑥ 회고 대화 · 결과 설명 | **AI** (오진호) | FastAPI | 표현 |
| ⑦ 절감액 · 목표 배분 계산 | **규칙 엔진** (정민규 — 단순 산술, E-31) | Spring | 결정론적 |
| ⑧ 소비 분석 요약 수치 집계 | **규칙 엔진** (정민규) | Spring | 결정론적 |
| ⑨ 소비 분석 문장 재구성 ('나만의 특징') | **AI** (오진호) | FastAPI | 표현 |
| ⑩ 저장 · 조회 · API | **Spring** (고현석) | Spring | — |
| ⑪ 금융 지식 Q&A (P2) | **AI + RAG** (오진호) | FastAPI · pgvector | 표현 (근거 안에서만, E-22) |

> **AI는 입구(① 후보 제안)와 출구(⑤⑥⑨⑪)에만 있습니다. 판단·집계(②③④⑦⑧)는 전부 Spring 규칙 엔진의 결정론입니다.** (v1.3 — E-18·E-20)
> ①에서 AI가 제안한 값은 **사용자가 확인하기 전에는 저장되지 않습니다.**
> 심사위원의 "왜 이렇게 묶였습니까"에는 Spring 규칙 엔진이, "왜 이 태그입니까"에는 "사용자가 확인했습니다"가 답합니다.

### ①의 확인 원칙 (E-20 — v1.3, E-16 폐기)

목적 7종·동행인 6종은 **고정 선택지**입니다. P0 경로(FR-04-04·05)는 사용자가 선택지 버튼을 직접 고르므로 AI가 관여하지 않습니다.
P1 경로(FR-04-07)에서만 AI가 자연어에서 **표준 태그 후보 또는 미확정(`null`)** 을 제안하고, 사용자가 확인합니다.

```
AI 후보 ∈ {표준 태그 7/6종} ∪ {null}   → 사용자에게 보여주고 확인받는다 (레포 needs_clarification · uncertain_fields)
확인된 값만 저장                        → 자유 문자열 저장 금지 (Spring이 400으로 거부)
null                                   → 해당 항목만 되묻는다 (FR-04-08). 추측 저장 금지 (FR-04-09)
```

- 저장값은 **사용자가 확인한 표준 태그**이므로, 그 이후의 묶음·보정·판정은 전부 결정론입니다 — NFR-01의 범위는 "확정 입력 이후"입니다.
- LLM 왕복이 P1 경로에만 생기므로 NFR-04 폴백도 P1 경로와 표현 계층(⑤⑥⑨)에만 걸립니다. P0 경로는 LLM 없이 완주합니다.
- 임베딩 모델 · `TAG_MATCH_MIN_SIMILARITY` · numpy는 **불필요**합니다 (E-16 폐기).
>
> ⚠️ **금지 표현:** 어떤 문서·시트·발표자료에도 "AI가 둘러댄다 / 알아서 설명한다" 류의 서술을 쓰지 않습니다.
> AI의 모든 설명은 규칙 엔진 산출값(`reasonCode`·집계 수치)의 **재구성**입니다 (NFR-02).

---

## 3. 규칙 엔진 파라미터

| # | 항목 | 확정 | 비고 |
|---|---|---|---|
| B-1 | 회고 시점 | **D+1** | 사용자 조정 필드(`retrospectDelayDays`) 유지 |
| B-2 | 점수 체계 | 만족(`HIGH`) +1 / 별로(`LOW`) −1 / 잘 모르겠어요(`UNKNOWN`) **제외** — 3택, 중간값 없음 (E-23) | 행동 평균 범위 [−1, +1] |
| B-4 | 심야 경계 | 22시 ~ 05시 | 4구간 중 `NIGHT` (v1.9 — E-50) |
| B-5 | 시간대 구간 | **4개** (v1.9 — E-50) | `MORNING` 05~11 · `DAY` 11~17 · `EVENING` 17~22 · `NIGHT` 22~05. `AFTERNOON` 폐기 |
| B-7 | 1회 회고 건수 | **경로별** (v1.9 — E-48) | 알림 1일 1건 · 채팅 최근 3일 전부 · 날짜 지정·직접 선택은 제한 없음. 온보딩은 예외 (연속 회고) |
| B-8 | 선별 조건 | 시간대별 이상치 + **사용자 설정 임계값** | 온보딩 입력 항목 |
| B-9 | 온보딩 표본 | **20건 내외, 26.06~07에서 추출** | 26.08은 전월 비교용 보존 |
| B-10 | 묶음 키의 시간대 | **식사 카테고리에만 적용** | 그 외 카테고리는 시간대 자리 공백 |
| B-11 | 부담 산정 기간 | **분석 기준월 1개월** | v1.2 확정 — E-1 부속 |

**미결:** B-3 보정 강도 `k` / B-6 롤업 기준 건수 → 데이터 확보 후 (9/4 이후, 액션시트 #15·#16)

### 보정 만족도 산식

```
보정 만족도 = (행동 회고 수 × 행동 평균 + k × 사용자 전체 평균)
              ÷ (행동 회고 수 + k)
```

축소 추정(shrinkage estimation). 회고가 적을수록 전체 평균을, 쌓일수록 행동 평균을 반영합니다.

### 회고 입력 선택지

| 항목 | 선택지 |
|---|---|
| 목적 | 식사 / 만남·사교 / 휴식·취미 / 필수품 / 자기계발 / 충동 / 기타 |
| 동행인 | 혼자 / 친구 / 가족 / 연인 / 동료 / 기타 |

---

## 4. 만족도 지도

| # | 항목 | 확정 |
|---|---|---|
| C-1 | 가로축 (지출 부담) | **분석 기준월 1개월 묶음 합계 금액 ÷ 사용자 입력 월 예산** (v1.2 — E-1) |
| C-2 | 세로축 | 보정 만족도 (범위 [−1, +1]) |
| C-3 | 라벨 체계 | **사분면 4코드 / 판정 2라벨 + 보류 상태 / 처방 문구 5종** |

> **C-1 해설:** 평균 단가가 아니라 **한 달 동안 그 행동에 쓴 총액**이 예산의 몇 %인가로 봅니다.
> "심야 배달에 예산의 12%를 쓰고 있다"가 부담의 정의입니다.
> 평균 단가(`avgAmount`)는 **절감액 계산(⑦)에만** 쓰며, 상세 패널에 `평균 12,000원 × 8회` 형태로 병기합니다.

### 사분면 · 라벨 · 처방 문구 (확정)

| 사분면 | 부담 | 만족 | 사분면 코드 | 판정 (`verdict`) | 처방 문구 |
|---|---|---|---|---|---|
| Ⅰ | 높음 | 높음 | `PROTECT` | 지켜요 (`SUSTAIN`) | **Great!** 이건 지킬 가치가 있어요. 예산을 미리 확보해둘까요? |
| Ⅱ | 낮음 | 높음 | `KEEP` | 지켜요 (`SUSTAIN`) | **Awesome!!** 부담도 적고 만족도 높아요. 이대로 두셔도 좋아요. |
| Ⅲ | 낮음 | 낮음 | `MINOR` | 바꿔볼까요 (`ADJUST`) | **Umm...** 조금 아쉬웠던 소비네요. 여력이 될 때 조정해도 괜찮아요. |
| Ⅳ | 높음 | 낮음 | `PRIORITY` | 바꿔볼까요 (`ADJUST`) | **Hmm!** 여기부터 볼까요? 부담은 큰데 만족은 낮았던 소비예요. |
| — (보류) | — | — | `null` | `null` — 더 알아볼게요 | 아직 판단하기엔 이르네요. 조금 더 지켜볼게요. |

### E-11 — 왜 4가 3이 아니라 2가 되는가 (v1.2 핵심 정정)

사분면과 판정은 **다른 종류**입니다.

| 층위 | 정체 | 종수 | 결정 요인 |
|---|---|---|---|
| 사분면 `quadrant` | **좌표** — 지도 위 위치 | 4 | 가로축 × 세로축 (연속값) |
| 판정 `verdict` | **처방** — 사용자가 할 일 | **2** | **세로축(만족도) 부호만** |
| 우선순위 | 무엇부터 볼지 | (정렬) | **가로축(부담)** — 지도상 오른쪽일수록 먼저 |
| 평가 상태 `evaluationStatus` | 판정 가능 여부 | 2 | 회고 건수 ≥ 보류 임계값 |

v1.1의 '판정 3라벨'은 **판정 2종에 보류 상태 1종이 섞인** 것이었습니다.
`더 알아볼게요`는 판정이 아니라 "아직 판정할 수 없음"이므로, 별도 필드로 분리합니다.

```
evaluationStatus = PENDING  → quadrant = null, verdict = null   (회색 반투명 점)
evaluationStatus = RESOLVED → quadrant ∈ {PROTECT,KEEP,MINOR,PRIORITY}
                              verdict  = 만족도 ≥ 경계 ? SUSTAIN : ADJUST
```

> `verdict`는 세로축만으로 정해지고 가로축은 관여하지 않습니다. 이는 결함이 아니라 설계입니다 —
> **부담은 판정이 아니라 순서를 정하며, 그 순서는 이미 점의 가로 위치로 보입니다.** 색을 하나 더 쓸 이유가 없습니다.

**표시 구조 (E-4·E-11 — 층위 분리)**

| 층위 | 종수 | 표시 위치 |
|---|---|---|
| 사분면 `quadrant` | 4 | 화면 노출 안 함 (API·DB·정렬 전용) |
| 판정 `verdict` | **2** | 점 색상 · 배지 — 지켜요 / 바꿔볼까요 |
| 평가 상태 `evaluationStatus` | 2 | `PENDING`은 **회색 반투명 점** (색상 팔레트가 아닌 상태 표현) |
| 처방 문구 `prescription` | **5** | 상세 패널 전용 |
| 회고 건수 | — | 괄호 안 숫자 |

> 화면에서 사용자가 보는 구분은 여전히 셋(지켜요 / 바꿔볼까요 / 회색)이지만,
> **색상 팔레트는 2색**이고 세 번째는 상태 표현입니다. 심사에서 "왜 셋입니까"를 한 줄로 답할 수 있습니다.

### E-12 — `PROTECT`의 행동 비대칭

`PROTECT`(Ⅰ)는 "예산을 미리 확보"라는 **할 일이 있고**, `KEEP`(Ⅱ)는 없습니다.
둘 다 배지는 `지켜요`로 동일하게 두되, **상세 패널에서만 구분**합니다.

| 코드 | 배지 | 상세 패널 |
|---|---|---|
| `PROTECT` | 지켜요 | Great! 문구 + **`예산 확보하기` CTA** (목표 배분 화면으로 연결) |
| `KEEP` | 지켜요 | Awesome!! 문구 — CTA 없음 |

> 지도는 조용하게 두고 행동은 살립니다. 조정 제안 목록(FR-08-01)에는 넣지 않습니다 —
> 그 목록은 '줄일 것'의 목록이며, 여기에 '지킬 것'을 섞으면 목록의 의미가 흐려집니다.

> ⚠️ **Great/Awesome/Umm/Hmm을 점 색상이나 배지에 쓰지 않습니다.** 4색 배지가 되면 층위 설계가 무너집니다.
> ⚠️ **`Out`은 사용하지 않습니다** (§7 판결 어휘 금지).

**미결:** 축 경계선 / 보류 임계값 → 데이터 확보 후 (액션시트 #17·#18)

---

## 5. 데이터

| # | 항목 | 확정 |
|---|---|---|
| D-1 | 지원 카드사 | 국민 · 하나 · 신한 |
| D-2 | 필수 컬럼 | 거래일시 / 가맹점명 / 금액 / 카테고리 |
| D-3 | 날짜 포맷 | `2026.08.25 20:22(:30)` — 초 단위 선택적 |
| D-5 | 시드 데이터 | **가맹점·금액 치환한 익명화 데이터셋** |
| D-6 | 데모 기간 | 26.06 ~ 26.08 (3개월) |
| D-7 | 대표 사례 단가 | **심야 배달 12,000원** (v1.2 — E-10) |

> **D-7:** 제출 기획서의 23,000원은 데모 데이터셋과 맞지 않습니다.
> 모든 문서·API 예시·발표 슬라이드를 **12,000원**으로 통일하고,
> 발표 시 "데모 데이터 기준 재산출" 한 줄로 정리합니다.

### 익명화 처리 방침

| 항목 | 처리 |
|---|---|
| 가맹점명 | 실명 → 가명 (`○○치킨`, `A커피`) |
| 금액 | ±10~20% 랜덤 스케일 (분포 유지) |
| 거래일시 | **원본 유지** — 시간대 분포가 핵심 |
| 카테고리 | 원본 유지 |
| 민감 업종 | 병원·약국 등 제외 또는 일반 카테고리로 치환 |
| 사용 범위 | 실데이터 = 파서 검증용 / 화면 노출 = 익명화본만 |

---

## 6. 규제 경계

- **허용:** 저축 · 정책 상품 조건 매칭
- **제외:** 원금 손실 위험이 있는 투자 상품
- 근거: 유사투자자문 규제 경계 준수
- 발표 시 반드시 **의도적 준수**임을 명시 (기능 축소로 오독됨)

---

## 7. 서비스 톤 원칙

- '후회' 언어를 쓰지 않습니다
- 지킬 소비를 먼저 말합니다
- **판결 어휘(탈락 · 아웃 · `Out` 등)를 어떤 산출물에도 쓰지 않습니다** — 문서·시트·피그마·발표자료 전부
- AI의 설명은 반드시 **규칙 엔진이 반환한 근거(`reasonCode`·집계 수치)를 재구성**한 것이어야 합니다 — 근거 없는 임의 생성 금지 (NFR-02)

---

## 8. 담당 확정

| 역할 | 담당 |
|---|---|
| 팀장 | **오진호** |
| 통합 담당 (Integration Owner, Spring↔FastAPI) | **고현석** |
| 익명화 스크립트 | **정민규** |
| LLM 장애 대체 템플릿 문구 | **오진호** |
| 3사 CSV 헤더 확보 | **석정한** — ⚠️ 실질 기한 **9/2** (9/3 파서 착수) |
| 프론트엔드 | **석정한** (일부 문서의 '김정한'은 오기) |
| 화면 · IA | **석예림** |
| 규칙 엔진 (Spring · Java) | **정민규** — 구현 / **고현석** — 통합·리뷰 (v1.3 — E-18) |
| AI 레포 `sottaejap-ai` (Agent · 폴백 · RAG) | **오진호** (v1.3) |
| 금융 문서 적재 (FR-12 · P2) | **오진호** (⚠️ 잠정 — 문서 소스 확정 필요) |

### 발표·시연 담당 (v1.2 확정)

| 역할 | 확정 |
|---|---|
| 발표 자료 | **전원 공동 — 본선 현장에서 함께 제작** |
| 데모 시연 | **본선 현장에서 결정** |

> ⚠️ **리스크:** 심사표 필수 항목인 '사회적 파급효과'가 현재 0건입니다.
> 현장 30시간 안에서 처음 쓰기 시작하면 개발 시간을 그만큼 잃습니다.
> **최소한 사회적 파급효과 1장만은 9/7 이전에 초안을 만들어 두십시오** (액션시트 §발표 자료).

---

## 9. 화면 · 네비게이션 (v1.2 — 번호 체계 재정비, E-7)

| 항목 | 확정 |
|---|---|
| 하단 탭 | **4탭 — 홈 · 채팅 · 지도 · 거래내역** |
| 설정 진입 | 우측 상단 (마이페이지 · 알림) |
| 최초 진입 | 온보딩 미완료 → 온보딩 / 완료 → 홈 |
| 온보딩 완료 판정 | **4단계**(목표 자금 → 월 예산 + 분석 민감도 → 거래내역 업로드 → 표본 회고) 완료 시 `onboardingCompleted = true` (E-45 — v1.7) |
| 알림 | **인앱 알림 P1 포함** / 푸시(앱 외부)는 범위 외 유지 |
| 로그인 | **카카오 OAuth 1종 실구현** (E-9·E-15). 데모 단일 계정 폴백 유지. 흐름은 E-51 (v1.10) |
| 아이콘 | **Tabler Icons** 단일 세트 (E-43 — v1.7). 이모지·타 세트 혼용 금지 |
| 서체 | **Pretendard** (E-44 — v1.7) |

### 번호 체계 규칙 (v1.2 확정 — 이것이 유일한 정본)

```
1  초기 화면          1L. 로그인/회원가입
2  메인 페이지         2H. 홈  →  2-1. 온보딩(첫 접속 1회 팝업)
3  하단 탭 바          3-1 홈 · 3-2 채팅 · 3-3 지도 · 3-4 거래내역
4  상단 내비게이션      4MP. 마이페이지 · 4A. 알림
```

- **2-1은 온보딩 전용**입니다. 홈 결과 리포트는 `3-1`입니다 (구 2-1 중복 해소).
- **3-3은 만족도 지도**입니다 (구 결번 해소).
- **채팅 하위는 `3-2-x`** 입니다 (구 `4-0~4-3` 폐기 — depth 4는 상단 내비 전용).

| 구 번호(피그마 v1.1) | 신 번호(v1.2) | 화면 |
|---|---|---|
| 2-1 (홈) | **3-1** | 홈 결과 리포트 |
| 3-1 | **3-2** | AI 에이전트와 채팅 |
| 4-0 | **3-2-1** | 초기 메뉴 선택 |
| 4-1 | **3-2-2** | 회고 |
| 4-2 | **3-2-3** | 행동 제안 |
| 4-3 | **3-2-4** | 소비 분석 (FR-11 — E-8) |
| 3-2 | **3-3** | 만족도 지도 |
| 3-4 | **3-4** | 거래내역 (유지) |
| 2-1 (온보딩) | **2-1** | 온보딩 (유지) |

---

## 10. 비기능 잠정치 (⚠️ 잠정 — 9/7 튜닝 시 재확인)

| 항목 | 잠정치 |
|---|---|
| 업로드 파싱 | 3개월분(약 1,000건) 10초 이내 |
| LLM 폴백 | AI 서버 안에서 8초 초과 시 템플릿 전환, 재시도 1회 |
| Spring → AI `/chat` 타임아웃 | 15초 (AI 내부의 LLM 8초 + Spring 조회 왕복 포함 — v1.3, 05 §3) |
| 클라이언트 | PWA — Chrome 최신 기준, 설치형 진입 지원 (v1.2 — E-9) |
