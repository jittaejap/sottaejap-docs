# 소때잡 — 결정 로그

**팀:** 지때잡 | **프로젝트:** 소때잡 | **버전:** v2.35 | **기준일:** 2026-09-09


> 이 문서가 **단일 진실 공급원(Single Source of Truth)** 입니다.
> 다른 모든 문서(요구사항·IA·ERD·API)와 외부 산출물(구글 시트·피그마)의 값은 여기서 파생됩니다.
> 값이 충돌하면 이 문서가 우선합니다.

---

## 0. v2.35 변경 요약 (2026-09-09 — 회고 대화의 이번 발화에도 500자 상한)

| # | 항목 | v2.34 | **v2.35 확정** |
| --- | --- | --- | --- |
| E-112 | `POST /retrospects/chat`의 `message` 길이 | 규정 없음 — 05 §2 #11은 "`INTRO`에서는 생략 가능, 그 외 단계는 필수"만 적었다. 같은 프롬프트 예산 논리로 `POST /chat/finance`(#24) · `POST /chat/analysis`(#28)의 `message`는 1~500자이고, E-109 · E-110은 같은 요청의 `recentMessages` 항목까지 `user` 500 · `assistant` 2,000으로 막는다. **정작 이번 발화만 무제한**이라 붙여넣기 한 번에 프롬프트 예산이 통째로 무너진다 (server PR #59 리뷰) | **`message`는 최대 500자다 — #24 · #28과 같은 값, 같은 이유(같은 요청에 최근 대화 6건이 함께 실린다).** 넘으면 **400 `INVALID_INPUT`**이고 AI를 부르지 않는다 — 새 오류 코드는 없다. `INTRO` 생략 규칙은 그대로라 `@NotBlank`는 붙이지 않는다(빈 값은 서버가 고정 문구로 대체). 단위는 **UTF-16**(`@Size`)이다 — E-110의 코드 포인트 계수는 AI와 맺은 `recent_messages` 계약에 적용되고, `message`는 서버 자체 입력 상한이라 #24 · #28과 같은 방식을 쓴다. 05 §2 #11 · server #67 |

> 계약의 모양은 바뀌지 않습니다 — 열려 있던 한 자리를 #24 · #28과 같은 값으로 닫는 것뿐입니다.
> 화면 입력창의 상한은 이 결정의 범위 밖입니다. client가 더 긴 값을 보내면 서버가 400으로 막습니다.

## 0-0. v2.34 변경 요약 (2026-09-09 — XLSX 업로드가 상한을 넘겨도 500이 되지 않게 · 운영 JVM 힙 확정)

| # | 항목 | v2.33 | **v2.34 확정** |
| --- | --- | --- | --- |
| E-111 | 20,000행 상한(E-105)이 XLSX에서 지켜지는 조건 | 06 v2.44가 **"운영 JVM 힙 1GB 이상"** 을 조건으로 달았다. `TransactionFileParser.readSheet`가 `XSSFWorkbook`으로 시트를 통째로 올린 **뒤에** 행을 세므로, 200,000행 · 3.2MB xlsx가 `-Xmx1g`에서는 1.85초에 400이지만 `-Xmx512m`에서는 세기 전에 `OutOfMemoryError`가 나고 그것은 `Error`라 `catch (IOException \| RuntimeException)`에 걸리지 않아 **500**이 된다 | **조건을 없앤다 — 힙과 무관하게 400이다.** ① **XLSX는 시트를 열기 전에 센다.** POI `XSSFReader`로 워크시트 XML을 흘려보내며 `<row>` 여는 태그만 세고, 물리적 행이 `20,000 + 머리글 탐색 30줄`을 넘으면 `XSSFWorkbook`을 부르지 않는다. **워크북 전체 장을 더한다** — `XSSFWorkbook`은 첫 장만 쓰더라도 모든 장을 객체 모델로 올리므로 메모리를 정하는 것은 전체 행수다. ② **운영 JVM 힙은 `-Xmx512m`, 컨테이너 상한은 `mem_limit: 1g`** 이고 자리는 `sottaejap-server/deploy/docker-compose.yml`이다 (07 §12). `Dockerfile`에 넣지 않는 이유는 같은 이미지를 로컬에서도 쓰기 때문이다. **명시의 목적은 힙을 줄이는 것이 아니라 호스트 메모리에 딸려 다니지 않게 못 박는 것이다** — 적지 않으면 `MaxRAMPercentage` 기본 25%라 인스턴스를 바꾸면 아무도 모르게 힙이 따라 움직인다. 명시 전 실효 힙은 호스트 1.9 GiB의 25%인 약 476MB였고 512m은 그보다 크다. 상한은 실사용의 두 배로 둔다 — `docker stats` 실측이 `server` **497.7MB** · `ai` 62.8MB · `db` 39.7MB, 호스트는 **1.9 GiB · swap 2 GiB**다(640m은 여유가 143MB뿐이라 되돌렸다). **문서에 적혀 있던 "t3.micro · 1 GiB"는 실측과 달랐다 — 사이징 값은 문서가 아니라 EC2에서 재고 적는다.** ③ 계약은 그대로다 — 상한 20,000행 · 400 `TOO_MANY_ROWS` · 한 건도 저장하지 않음(05 §2). 세는 **시점**만 앞당겼다. 실측(작성자 로컬 · JDK 21): 200,000행 xlsx가 힙 256m · 320m · 512m 모두 **0.2초 안에 400**(고치기 전 512m는 5.9초 OOM), 상한 경계인 20,000행 xlsx는 힙 256m에서 0.6초에 정상 파싱. 06 **R28** · server 이슈 **#58** |

> 결정은 둘입니다 — 세는 시점을 앞당긴 것과, "힙 1GB 이상"이라는 지킬 수 없는 조건을 실측한 값으로 바꾼 것.
> 운영 메모리 배분은 07 §12에 적혀 있고 그 값이 곧 compose의 값입니다.

## 0-1. v2.33 변경 요약 (2026-09-09 — `recentMessages` 항목 상한을 `role`별로 — `assistant` 2,000자)

| # | 항목 | v2.32 | **v2.33 확정** |
| --- | --- | --- | --- |
| E-110 | `recentMessages` 항목 `content` 상한 — `role`별 분리 (E-109 개정) | E-109가 `role`과 무관하게 500자. `user`는 `message` 상한(v2.10)이 이미 막지만 `assistant`(AI `reply`를 client가 되돌려 보낸 것)는 길이 보장이 없다 — ai에 `max_tokens`가 없고 공통 프롬프트의 "짧게"는 Task 지시문에 밀린다(ai DEVELOPMENT §12). server 자신도 `FINANCE_QA`에서는 `chat_messages.content`(TEXT)를 그대로 싣는다. **server가 만든 길이로 client가 400을 맞는** 모양(server PR #59 리뷰) | **`user` 500자 유지 · `assistant` 2,000자.** 무제한은 예산 방어가 사라져 택하지 않는다. 상한은 생산하는 쪽이 지킨다 — ai는 `reply`를 2,000자(코드 포인트) 이내로 만든다(**ai #55**, `max_tokens` + 문장 경계 절단). `role` 값 규칙 · `null` 항목 400 · 6건 절단(E-87)은 그대로. 05 §2 #11 · #28 · §3 v2.34 · server #61 |

> 값은 리뷰어 제안(2,000)을 그대로 확정했다. ai 보장 전까지 2,000자를 넘는 `reply`는 client 쪽에서 잘라 보내는 수밖에 없다 — client #22 코멘트.

---

## 0-2. v2.32 변경 요약 (2026-09-09 — `recentMessages` 항목 규칙 · 규격 밖 값은 503이 아니라 400)

| # | 항목 | v2.31 | **v2.32 확정** |
| --- | --- | --- | --- |
| E-109 | `POST /retrospects/chat`(05 #11) · `POST /chat/analysis`(05 #28)의 `recentMessages` **항목** 규칙 — `role` 값과 `content` 길이 | 규정 없음 — E-87은 정렬(오름차순)과 건수(최근 6건)만 정했다. 서버는 항목을 검증하지 않고 AI `POST /chat`의 `recent_messages`로 그대로 넘기고, AI 스키마(`role`은 `user \| assistant`, `content`는 1자 이상)가 거절한 422를 `AiClient`가 **503 `LLM_UNAVAILABLE`**로 바꾼다. 클라이언트 입력 오류가 AI 장애로 보이고 클라이언트는 템플릿 모드(S11)로 넘어간다(server PR #55 리뷰 실측 — `role: "system"` · `content: ""` 둘 다 503, 두 채널 공통). `message`의 500자 상한(v2.10)도 항목에는 없어 서버가 6건을 함께 싣는 프롬프트 예산의 절반만 지켰다 | **항목마다 `role`은 `user` 또는 `assistant`, `content`는 공백 아닌 1~500자다. 어기면 400 `INVALID_INPUT`이고 AI를 부르지 않는다.** 두 채널이 같은 규칙이다. 규격 밖 항목을 버리고 진행하는 안(되물음 맥락이 조용히 사라진다)은 택하지 않았다. 500자는 `message`와 같은 값, 같은 이유(프롬프트 예산). 정렬 · 6건 절단(E-87) · 규격 안 입력의 응답은 그대로. 새 오류 코드 없음. 05 §2 #11 · #28 · §3 v2.33 · server #56 |

> 결정은 두 갈래 중 400을 택한 것 하나입니다. 서버 코드는 server #56이 이 문서를 따라갑니다.

---

## 0-3. v2.31 변경 요약 (2026-09-09 — `POST /chat/analysis` 안내문 확정 · 숫자 가드 거절의 fallback 값)

| # | 항목 | v2.30 | **v2.31 확정** |
| --- | --- | --- | --- |
| E-107 | `POST /chat/analysis`의 "byCategory 빈 경우" 안내문 — 유효 묶음 0개와 기준월 합계 0을 나누는 최종 문구 | v2.30이 조건 분리를 **ai #50**에 넘기고 "정해지면 이 줄을 그 결과로 바꾼다"고 남겨 둠 | **ai #50이 A안(조건 분리)으로 확정.** 유효 묶음 수(`byVerdict[].clusterCount` 합 + `pending.clusterCount`, server `HighlightTemplate.effectiveClusterCount`와 같은 식)가 0이면 `"아직 돌아본 소비가 없어요. 몇 건만 회고하면 분석을 보여드릴 수 있어요."`, 1 이상이면 `"이번 달 거래는 아직 회고한 게 없어요. 이번 달 거래를 몇 건 회고하면 분석을 보여드릴 수 있어요."`다. 후자는 server `NO_MONTH_ACTIVITY`(E-89)처럼 "이번 달 거래를"이 회고 대상을 가리켜 시점 모호성이 없다. server 확정 문구를 그대로 복사하지 않고 채팅 맥락에 맞게 다시 썼다(server PR #53 리뷰). 05 §2 반영 |
| E-108 | `ANALYSIS`의 숫자 가드(ai #48, E-79 · E-101 재사용)가 답을 버릴 때 `fallback` 값 | 규정 없음 — E-79·R19가 정한 `fallback: true` 용법은 `ANALYSIS_NARRATE`(Spring이 문장을 템플릿으로 갈음)뿐이고, `POST /chat/analysis`의 `fallback: true`는 05 §2가 이미 "`OPENAI_API_KEY` 미설정(E-38) · AI→OpenAI 초과·실패(E-88)"로만 정의해 뒀다 — 가드 거절은 이 정의 밖이다 | **`fallback: false`로 확정.** 숫자 가드가 근거 밖 수치를 걸러 답을 버린 것은 검증 실패이지 AI 장애가 아니다. `fallback: true`로 세우면 클라이언트가 템플릿 모드 배너(S11)를 띄워 "AI가 죽었다"고 오인시킨다(server #49가 이미 같은 혼동으로 비용을 냄 — `highlight` 캐시가 가드 폴백을 장애로 읽은 사례). 안내문(`ANALYSIS_UNAVAILABLE_REPLY`)만 돌려주고 플래그는 세우지 않는다. 05 §2 반영 |

> 오탐(사용자가 말한 숫자를 답이 되받았을 뿐인데 가드에 걸리는 경우)의 문구 톤은 아직 실측 전이다 — ai #48의 완료 조건으로 남겨 둔다.

---

## 0-4. v2.30 변경 요약 (2026-09-09 — 업로드 행수 상한 · E-89 문구 확정 · 가맹점명 정규화 본선 제외)

| # | 항목 | v2.29 | **v2.30 확정** |
| --- | --- | --- | --- |
| E-105 | `POST /transactions/upload` 행수 상한 (06 R28) | 규정 없음 — `multipart.max-file-size` 10MB만. client는 모든 요청에 `timeout: 20_000`을 공통으로 걸고 업로드만 늘리지 않았다. 실측 약 3,500행/초라 10MB에 가까운 파일은 20초를 넘기고, 타임아웃이 먼저 끊기면 **서버는 저장했는데 화면만 실패**로 보인다(PR #31 리뷰 5번) | **머리글 아래 행이 20,000행을 넘으면 400 `TOO_MANY_ROWS`이고 한 건도 저장하지 않는다.** 파서가 머리글을 찾은 직후 레코드 수(합계 · 여백 행 포함)를 세어 행 해석 전에 멈춘다 — CSV · XLSX 같은 지점 하나. 20,000행 ≈ 6초로 NFR-03(3개월분 약 1,000건 10초)의 20배이고 본선 데모 CSV는 1,200행이다. R28의 다른 대응(client 업로드 타임아웃 분리)은 하지 않는다 — server 한 곳에서 닫힌다. 파일 크기 상한 10MB는 그대로. 05 §0 · §2 v2.29 · server #52 · PR #54 |
| E-89 확정 | `highlight` 템플릿 4종째 문구 | **잠정** "이번 달에 돌아본 소비가 아직 없어요. …" — v2.14 메모가 후보 2를 달았다: `이번 달에`가 회고 시점인지 소비 시점인지 두 갈래로 읽히고, 이 경로의 사용자는 회고를 이번 달에 했다 | **후보 2로 확정 — "이번 달 거래 중 돌아본 것이 아직 없어요. 이번 달 거래를 몇 건 회고하면 특징이 보이기 시작해요."** 소비 시점에 붙여 모호성이 없다. 판별 조건 · 나머지 3종은 그대로. server #51 · PR #53. ai `app/agent/handlers/analysis.py`의 "유효 묶음 없음" 안내문이 옛 문구를 같은 뜻으로 들고 있다 — ai 몫으로 맞춘다(06 v2.39 메모) |
| E-106 | FR-02-05 가맹점명 정규화 (P1) | 02에 P1으로 있고 04 §1 `merchantNormalized`가 "고유명사 유지 / 편의점 지점 표기 제거" 한 줄뿐. server는 컬럼만 있고 채우지 않는다(`Transaction` 주석) | **본선 제외.** `merchantNormalized`를 읽는 코드가 server에 하나도 없다 — 묶음 키(E-58)는 카테고리 · 시간대, 중복 판별(04 §4)은 원본 `merchant`, 화면 어디에도 안 쓰인다. 구현해도 데모에서 보이는 차이가 0이다. 컬럼은 남기고 규칙은 후속으로 미룬다. 02 v2.14 FR-02-05 비고 |

> 셋 다 결정 자체는 오늘 것입니다. E-105는 server PR #54, E-89는 PR #53이 코드이고, E-106은 코드가 없습니다. 남은 P0 갭은 R29(카테고리 매핑표)뿐입니다.

---

## 0-5. v2.29 변경 요약 (2026-09-09 — 채팅 소비 분석 채널의 자유 질문 · `ANALYSIS` 서버 경로)

| # | 항목 | v2.28 | **v2.29 확정** |
| --- | --- | --- | --- |
| E-104 | 채팅 소비 분석 채널의 자유 질문 — `ANALYSIS` 작업을 누가 부르는가 | 규정 없음 — 05 §3에 `ANALYSIS` 작업 정의("사용자 질문에 대한 설명")만 남아 있고 부르는 경로가 없다(v1.2 `/internal/*` 시절의 유물, E-19). ai는 핸들러를 만들었고(ai #38) client는 소비 분석 채널의 입력을 고정 문장 "소비 분석에 대한 질문을 확인했어요"로 받는다(`ChatView.vue:413`) — 세 저장소가 서로 다른 전제 위에 있다 | **`POST /chat/analysis`(05 #28)를 신설한다 — 상태 없는 프록시.** ① 클라이언트가 `message`와 `recentMessages`를 들고 다니고 서버는 저장하지 않는다(E-63 `/retrospects/chat`과 같은 모양). `chat_messages`는 `transaction_id IS NULL`을 금융 Q&A의 맥락으로 쓰고 있어(E-67) 여기 섞으면 두 채널의 되물음이 서로 오염되고, 채널 컬럼을 더하면 본선 하루 전에 마이그레이션이 든다 — 클라이언트는 이미 채널별 이력을 들고 있다. ② `task_context.state`는 `{ analysis_year_month }` 하나(05 §3) — 집계는 AI가 `ANALYSIS` 도구로 내부 `/analysis`를 pull한다. ③ 응답은 `{ reply, fallback }`으로 #24와 같다. ④ 유효 묶음이 없으면 ai 핸들러가 LLM 없이 안내문을 돌려준다 — Spring은 그 문장을 다른 문장으로 바꾸지 않는다(#24와 같은 원칙). ⑤ 이 경로는 `highlight` 캐시(E-102)와 무관하다 — `ANALYSIS_NARRATE`가 아니라 다른 Task다. **위험:** `ANALYSIS` 핸들러에는 `ANALYSIS_NARRATE`의 숫자 가드(E-79 · E-101)가 없어 집계 밖 수치가 새어 나갈 수 있다(NFR-02) — 06 R34 ai 몫. FR-11-05 **P2** |

> **왜 P2인가.** 본선 하루 전이고 FR-11의 P1 네 항목은 이미 끝났습니다. 이 경로는 클라이언트의 막힌 입력창을 살리는 것이지 심사 항목이 아닙니다.
> 계약 변경이라 05 §2를 먼저 고치고 팀 채널에 `[계약변경]`을 올립니다(07 §6). 클라이언트는 고정 문장 자리(`ChatView.vue:413`)를 이 호출로 바꾸면 됩니다 — 그 전까지는 지금처럼 동작합니다.
>
> **되물음의 맥락은 얕습니다.** ai `build_system_prompt`는 `recent_messages`에서 **마지막 assistant 발화 하나**만 프롬프트에 싣습니다(`state.last_question`). "그럼 배달은요?"처럼 직전 답에 기대는 되물음은 이어지지만, 세 턴 전을 가리키는 질문은 못 잇습니다. #24도 같은 제약이고, 넓히려면 ai 프롬프트 조립을 바꿔야 합니다 — 이번 범위 밖입니다.

---

## 0-6. v2.28 변경 요약 (2026-09-09 — `highlight` 캐시 적중률과 가드레일 폴백)

| # | 항목 | v2.27 | **v2.28 확정** |
| --- | --- | --- | --- |
| E-102 보강 | `fallback: true`를 캐시하지 않는 결정의 비용 | "AI가 돌아오면 다시 부른다"(E-38) — 폴백을 일시 장애로만 본다 | **폴백에는 두 번째 원인이 있다.** ai PR #43의 숫자 가드(E-79 · E-101)가 근거 밖 수치를 잡으면 LLM이 정상이어도 200 · `fallback: true`다. 같은 집계에서 다시 뽑아도 또 걸릴 수 있으므로 그 사용자는 `GET /analysis`마다 정상 LLM 왕복 한 번을 치른 뒤 템플릿을 본다. **캐시 적중률은 가드레일 통과율만큼 깎인다** — 추가 비용은 정상 왕복 1회 × 가드레일 거절 비율이고, `AI_TIMEOUT_MS`(15초)는 이 경로에 걸리지 않는다. 결정은 미룬다 — 리허설에서 비율을 먼저 센다(06 **R33** · server #49 · server PR #48 리뷰) |

> 결정 내용은 바꾸지 않습니다. 폴백도 캐시할지(A) · 원인을 구분해 가드레일 폴백만 캐시할지(B, 05 §3 계약 변경이 먼저) · 그대로 둘지(C)는 실사용 폴백 비율을 본 뒤 E-번호로 확정합니다. 지금 확정하는 것은 **비용이 E-102에 빠져 있었다**는 사실 하나입니다.

---

## 0-7. v2.27 변경 요약 (2026-09-09 — 제안 이유 AI 문장(E-103) 병합 기록)

| # | 항목 | v2.26 | **v2.27 확정** |
| --- | --- | --- | --- |
| E-103 병합 기록 | 제안 `reason`을 AI 문장으로 — server 반영 상태 | server 이슈 #43 · PR #46 리뷰 중 | **PR #46(`0f507da`) 병합 · CI · Deploy success (9/9).** 리뷰 3라운드에서 롤업 리프 추적 · 숫자 가드 · `@Async` 거절 정책(기본 `AbortPolicy`면 커밋 뒤 `POST /retrospects`가 500)을 닫았다. 값 · 계약 변경 없음 |

> 결정 내용은 E-103 그대로입니다. 남는 것은 06 **R32**(ai `ACTION_PLAN` 톤 · "제안 없음" 안내문 `fallback: true` · ai#40 배포 뒤 `reason = NULL` 1회)뿐이고 ai 몫입니다. 06 v2.36.

---

## 0-8. v2.26 변경 요약 (2026-09-09 — `ANALYSIS_NARRATE` 전용 핸들러 · E-102 전제 정정 · R19 종결)

| # | 항목 | v2.25 | **v2.26 확정** |
| --- | --- | --- | --- |
| E-102 전제 정정 | `highlight` 캐시 키가 기대는 전제 | "지금 `ANALYSIS_NARRATE`는 **전용 핸들러 없이** 도구 호출 없는 생성 경로다" — 정본 ai PR #43이 병합되며 사실과 달라졌다 | **`ANALYSIS_NARRATE`에는 전용 핸들러가 있다**(ai PR #43 `be82917` · `app/agent/handlers/analysis_narrate.py`). **그 핸들러는 도구를 부르지 않는다** — Registry를 건드리지 않고 `ctx.generate()`만으로 `state`에서 한 문장을 만든다. 따라서 결론은 그대로다: AI 입력은 `state`가 싣는 것이 전부이고, 키 `(analysisYearMonth, AnalysisSummary)`가 그것을 빠짐없이 덮는다. 바뀌는 것은 전제 문장뿐이다. 이 핸들러에 `ANALYSIS` 도구(내부 `/analysis`, 묶음 단위 `points`)를 붙이는 사람이 캐시 키를 함께 보도록 server `HighlightServiceImpl.Key` 주석도 같은 문장으로 고친다(server 이슈 #47). 값 · 계약 변경 없음 |

> **R19(06)는 이 라운드에서 종결합니다** — ai PR #43 `be82917` 병합 · CI · Deploy success · 전체 `pytest` 167건 통과.
> 리뷰에서 걸린 오탐 둘(정상 연월 거절 · 한 자릿수 `%` 미검사)은 E-101대로 반영됐고, 퍼센트는 `share × 100` 전용 집합과만
> 대조합니다 — 허용 목록에 새로 들어간 월(`8`)이 `8%`의 근거가 되지 않습니다. `%` 앞 공백도 건너뛰고 판정합니다.
> `TaskType` 6종의 핸들러가 전부 채워졌고, Spring이 실제로 부르는 것은 `REFLECTION` · `CLUSTER_NAMING` · `FINANCE_QA` ·
> `ANALYSIS_NARRATE` · `ACTION_PLAN`(E-103) 다섯입니다. `ANALYSIS`(ai #38)는 서버 호출 경로가 없습니다 — 붙일지는 미결입니다.

---

## 0-9. v2.25 변경 요약 (2026-09-09 — 제안 이유를 AI 문장으로)

| # | 항목 | v2.24 | **v2.25 확정** |
| --- | --- | --- | --- |
| E-103 | 제안 `reason`을 누가 쓰는가 (E-84 갱신) | **Spring 템플릿 3종**이고 AI를 부르지 않는다 | **AI가 쓴 문장을 저장하고, 없으면 템플릿으로 갈음한다.** ① 회고 저장이 **커밋된 뒤** `ACTION_PLAN`(05 §3)을 부른다 — AI가 `/internal/ai/…/suggestions`로 제안을 되읽으므로 커밋 전이면 방금 만든 제안이 보이지 않는다. ② **응답 경로 밖(`@Async`)이다** — 이유는 `RetrospectSaveResponse`에 실리지 않고, 동기로 두면 저장 응답이 명명 15초 + 이유 15초로 최악 30초가 되어 클라이언트 공통 타임아웃 20초가 먼저 끊는다. **응답 시간에 드는 AI 왕복은 명명 1회뿐이다**(E-64 유지). ③ 대상은 방금 회고한 **리프의 제안**이고, 리프가 롤업돼 있으면 `parentId`를 따라간 **상위 묶음**의 제안이다 (E-59 · E-72). ④ **숫자가 바뀌면 문장을 버린다** — `adjustCount` · `expectedSaving`이 달라지면 그 문장은 옛 값을 인용한 것이라 `null`로 되돌리고 다음 저장 때 다시 짓는다. ⑤ **폴백 · 빈 문장 · AI의 조회 실패는 저장하지 않는다** — `ACTION_PLAN` 폴백은 "지금은 설명드릴 수 없어요"라는 안내라 이유 자리에 사과문이 남는다. ⑥ **내부 AI 목록에는 싣지 않는다** (E-75와 같은 이유 — AI가 자기 출력을 근거로 삼는다). 04 `Suggestion.reason` · 05 §2 · 06 **R32** · server 이슈 #43 · **PR #46(`0f507da`) 병합 · CI · Deploy success(9/9, v2.27)** |

> **E-84의 "AI를 부르지 않는다"를 폐기합니다.** 그 판단의 근거는 "`ai` 컨테이너가 내려가도 제안 목록이 뜬다"였고,
> 그것은 지금도 그대로입니다 — `reason`이 `null`이면 화면이 템플릿 3종으로 채웁니다(E-38). 바뀐 것은 **AI가 살아 있을 때
> 더 나은 문장을 쓸 수 있다**는 것뿐이고, 목록 조회는 여전히 AI를 부르지 않습니다.
>
> **왜 저장하는가.** 이유는 제안이 생기는 순간 한 번 지으면 되고, 숫자가 바뀌기 전까지 같은 문장이 맞습니다.
> 조회 때마다 부르면 `GET /suggestions`가 AI 왕복을 타게 되어 E-38의 전제가 깨집니다.
>
> **응답 밖으로 뺀 대가.** 저장 응답이 돌아온 직후에 목록을 열면 이유가 아직 비어 템플릿 문장이 보일 수 있습니다.
> 다음 조회에서 AI 문장으로 바뀝니다 — 화면이 두 문장 사이를 오가는 것이 30초를 기다리다 타임아웃을 보는 것보다 낫다고 봅니다.

---

## 0-10. v2.24 변경 요약 (2026-09-09 — `highlight` 캐시)

| # | 항목 | v2.23 | **v2.24 확정** |
| --- | --- | --- | --- |
| E-102 | `highlight` 캐시 — 집계가 같으면 `ANALYSIS_NARRATE`를 다시 부르지 않는다 (E-75 보강) | 규정 없음 — `GET /analysis`가 진입할 때마다 `ANALYSIS_NARRATE`를 부른다. 집계는 결정론이고(E-18) 문장은 집계만 보고 만드는데(E-75 · NFR-02) 같은 입력에 같은 왕복을 새로고침 · 탭 전환마다 최대 15초씩 한다(server PR #16 리뷰 #6 · 이슈 #19) | **`HighlightServiceImpl`이 사용자별로 마지막 문장을 한 칸 캐시한다. 키는 `(analysisYearMonth, AnalysisSummary)` — 집계 그 자체다.** 집계가 바뀌면 키가 바뀌어 저절로 miss이므로 무효화 훅(회고 저장 · 설정 변경 · 업로드 재계산)을 두지 않는다 — "재계산 없이 거래만 업로드되면 캐시가 남는다"는 위험도 같은 장치로 닫힌다. 캐시하는 것은 AI가 집계를 보고 만든 문장뿐이다 — 503 · 빈 문장 · `fallback: true`는 저장하지 않는다(AI가 돌아오면 다시 부른다, E-38 — **다만 숫자 가드레일 폴백은 일시 장애가 아니라 같은 집계에서 되풀이될 수 있어 적중률이 가드레일 통과율만큼 깎인다.** v2.28 · 06 R33 · server #49). 인메모리 · 인스턴스 하나 전제(데모 규모 — `NotificationServiceImpl`과 같은 전제)라 항목 수는 사용자 수를 넘지 않고, 배포하면 빈다 — 프롬프트를 고치면 옛 문장이 남지 않는다. **키는 AI가 `task_context.state`만 보고 쓴다는 전제에 기댄다**(05 §3 · `ANALYSIS_NARRATE`에는 전용 핸들러가 있으나 — ai PR #43 `analysis_narrate.py` — 도구를 부르지 않고 `state`만으로 생성한다, v2.26 전제 정정) — ai가 이 작업에 `ANALYSIS` 도구(내부 `/analysis`, 묶음 단위 `points`)를 붙이면 집계가 같아도 문장 재료가 달라지므로 그때 키를 다시 본다(server PR #45 리뷰 · `HighlightServiceImpl.Key` 주석). 응답 본문 · 오류 계약은 그대로 — 언제나 200, `highlight`는 비지 않는다. 05 §2 |

> 이 결정은 응답 본문 · 오류 계약을 바꾸지 않습니다. 05 §2의 "AI 왕복(최대 15초)이 응답 시간에 포함됩니다"가
> **집계가 바뀐 뒤 첫 진입에만** 참이 됩니다. `OPENAI_API_KEY`가 없는 환경에서는 늘 `fallback: true`라 캐시가 채워지지 않습니다 —
> 리허설 환경에 키가 있어야 체감됩니다 (server PR #45 `61c777e`).

---

## 0-11. v2.23 변경 요약 (2026-09-09 — `ANALYSIS_NARRATE` 숫자 가드 예외 범위)

| # | 항목 | v2.22 | **v2.23 확정** |
| --- | --- | --- | --- |
| E-101 | `ANALYSIS_NARRATE` 숫자 가드가 한 자릿수와 기준 연월을 다루는 방법 (E-79 보강) | **모든 한 자릿수 정수**를 서수 표현으로 보고 검사하지 않는다. `analysis_year_month`는 프롬프트에 실리지만 허용 숫자에는 넣지 않았다 | **한 자릿수 예외는 `%`가 바로 뒤에 붙지 않은 정수에만 적용한다.** 따라서 `1번째`처럼 숫자 자체가 집계 수치를 뜻하지 않는 표현은 종전대로 허용하지만, `8%`는 한 자릿수여도 반드시 `share × 100` 등 `state`의 근거와 견준다. **`analysis_year_month`의 연도와 월도 허용 숫자에 넣는다** — `2026-08`을 받은 모델이 `2026년 8월 기준`이라고 재구성한 정상 문장이 가드에 걸리면 안 된다. 형식이 올바르지 않으면 연월 숫자는 허용하지 않는다. 가드가 거절하면 E-79대로 `fallback: true`다. `sottaejap-ai` PR #43 리뷰 |

> 이 결정은 새로운 수치를 허용하는 것이 아니라, `state`에 실제로 있는 기준 연월과 비율만 통과시키고
> 근거 없는 한 자릿수 비율이 서수 예외를 타는 구멍을 닫습니다. DTO · enum · 엔드포인트는 바뀌지 않습니다.

---

## 0-12. v2.22 변경 요약 (2026-09-09 — 바뀐 컬럼만 쓰는 엔티티의 일반 규칙)

| # | 항목 | v2.21 | **v2.22 확정** |
| --- | --- | --- | --- |
| E-100 보강 | 바뀐 컬럼만 쓰는 엔티티의 범위 | **`Goal` 하나** — `PUT /goals/{id}`와 실적 배분의 경합만 다뤘다 | **서로 다른 요청이 겹치지 않는 컬럼을 각자 읽어 고치는 엔티티는 모두 바뀐 컬럼만 쓴다(`@DynamicUpdate`).** 전체 행을 쓰면 나중에 커밋한 쪽이 상대의 컬럼을 옛 값으로 덮는다 — 조건은 `Goal`만의 것이 아니었다(server PR #42 리뷰, 실제 PostgreSQL로 셋 다 재현). 대상 4종: ① `Goal` — 수정 ↔ 실적 배분(`currentAmount`). ② **`Suggestion`** — 재계산의 `refresh`(수치) ↔ 사용자의 `adopt`(`status` · `goalId`). `SuggestionSyncService.sync`가 트랜잭션 시작 때 읽은 `PROPOSED`를 되쓰므로 그 사이 커밋된 **채택이 목표 연결까지 풀렸다** — 셋 중 가장 눈에 띈다. ③ **`User`** — 설정 변경(`monthlyBudget` · `outlierThreshold` · `retrospectDelayDays`) ↔ 재계산(`avgSatisfaction`) ↔ 온보딩 완료. 예산을 바꾸지 않는 설정 변경은 재계산 없이 커밋하므로 옛 `avgSatisfaction`을 되써서 **지도의 만족도 축(E-61 축소 추정)이 옛 값으로 돌아갔다.** ④ **`BehaviorCluster`** — 재계산의 `apply` · `markEmpty`(지표) ↔ 명명 `rename`(`displayName`). 명명 → 재계산 방향은 다시 읽어 막혀 있었지만 반대 방향이 열려 **AI가 지은 이름이 `NULL`로 지워졌다.** 새 엔티티를 둘 이상의 요청이 각자 읽어 고치게 된다면 같은 기준으로 붙인다. E-100 ②(명시값은 마지막 값이 이긴다)는 그대로다 — `SELECT … FOR UPDATE`는 `addCurrentAmount`의 행 잠금과 순서가 얽혀 교착 가능성을 새로 만들고, client가 부르지 않는 경로에 치를 값이 아니다. 04 §1 · server PR #42 |

> **client 메모.** `service.ts`의 목표 입력 타입에 `currentAmount?`가 있고 온보딩이 `currentAmount: 0`을 보낸다(생성이라 문제 없다).
> 같은 타입으로 `PUT /goals/{id}`를 배선하면 `0`이 실려 E-100 ②에 걸린다 — **목표 수정 화면은 `PUT`에 `currentAmount`를 싣지 않는다**(client 이슈 #17에 적어 둠).

---

## 0-13. v2.21 변경 요약 (2026-09-09 — 목표 수정과 실적 배분의 경합)

| # | 항목 | v2.20 | **v2.21 확정** |
| --- | --- | --- | --- |
| E-100 | `PUT /goals/{id}`가 월간 리포트 확정의 실적 배분과 겹칠 때 (E-94 ④ 보강) | 규정 없음 — E-94 ④는 배분끼리의 경합만 `GoalRepository.addCurrentAmount`(DB에서 원자적으로 더함)로 막았다. `PUT`은 엔티티를 읽어 바꾸고 Hibernate가 **전체 행**을 UPDATE하므로, `currentAmount`를 생략한 이름 변경이 **자기가 읽은 옛 실적을 다시 써서** 그 사이에 더해진 배분이 증발했다(server PR #32 리뷰) | **`Goal`은 바뀐 컬럼만 쓴다(`@DynamicUpdate`).** ① `currentAmount`를 생략한 `PUT`은 SQL에 `current_amount`를 싣지 않는다 — 05 §2의 "생략하면 그대로 둔다"가 **메모리의 옛 값을 되쓰는 것이 아니라 DB 값을 건드리지 않는 것**이 된다. 어느 쪽이 먼저 커밋하든 배분은 남는다. ② **명시한 `currentAmount`는 종전대로 마지막 값이 이긴다** — 사용자가 실적을 직접 적은 것이고, client는 아직 `PUT /goals`를 부르지 않는다(`POST`만). ③ `@Version`(409)은 쓰지 않는다 — 05 §0에 없는 오류이고 client 대응이 따라야 하는데, 본선 전날 오류 코드를 늘릴 이유가 없다. 이 저장소에는 L2 캐시 · JDBC 배치 설정이 없어 `@DynamicUpdate`의 부작용 범위도 없다. ④ 계약 · 스키마 변경 없음. 04 §1 · server 이슈 #36 |

> **겹치는 모양.** Tx1(8월 확정)이 `update goals set current_amount = current_amount + 20000 where id = 1`을 실행하고,
> Tx2(`PUT` 이름 변경)가 그 전에 읽어 둔 `current_amount = 0`을 들고 `update goals set name = …, target_amount = …, current_amount = 0`을
> 쓰면 20,000이 사라진다. 바뀐 컬럼만 쓰면 Tx2의 UPDATE에 `current_amount`가 없어 Tx1의 결과 위에 이름만 얹힌다.

---

## 0-14. v2.20 변경 요약 (2026-09-09 — 표준 태그 표기 정규화)

| # | 항목 | v2.19 | **v2.20 확정** |
| --- | --- | --- | --- |
| E-99 | 표준 태그를 **받을 때** 표기가 다르면 어떻게 하는가 | 규정 없음 — E-41은 저장값과 AI 값이 같은 표기라는 것만 정했고, 받는 자리는 문자열을 통째로 비교했다 | **공백을 지워 대조하고 정본 표기로 되돌려 저장한다.** ① 대조 전에 **공백 · 줄바꿈 · 비분리 공백(U+00A0)을 지운다.** 화면 문구 `"휴식 · 취미"`와 정본 `"휴식·취미"`는 가운뎃점(U+00B7)이 같고 공백만 다르다. ② 공백을 지워도 표준 태그 7/6종이 아니면 **그대로 400 `INVALID_TAG`** 다(`"야식"`) — 정규화가 새 태그를 만들지 않는다. ③ **저장값과 응답값은 언제나 정본 한 표기다.** `purpose`는 `clusterKey`의 한 자리라, 표기가 갈라지면 같은 태그가 두 묶음이 된다. ④ 적용 지점은 `POST /retrospects`(내부 AI `save_reflection` 포함) · `POST /retrospects/chat`의 사용자 확정값 · 같은 API의 AI 후보값 **셋이고, 셋 다 `StandardTags` 한 곳을 지난다.** **E-20 · E-41은 그대로 유효하다** — 저장 규칙이 아니라 받는 규칙이 넓어졌다. 05 §2 · server 이슈 #38 · PR #41 |

> **2026-09-09 원인.** 온보딩 4단계에서 회고 대화는 끝까지 가는데 **마지막 저장만 400**이었다.
> client 화면 문구가 `휴식 · 취미`이고 서버 정본이 `휴식·취미`라, 목적 7종 중
> `만남 · 사교` · `휴식 · 취미` **2종**이 막혔다. 동행인 6종은 한 단어라 무사했고, 사용자에게는
> "될 때도 있고 안 될 때도 있는 저장"으로 보였다. 세 레포의 가운뎃점은 모두 U+00B7이고
> AI `PurposeTag` enum도 서버 정본과 같다 — 어긋난 곳은 **client 한 군데**였다.

> **받는 자리를 고른 이유.** client만 고치면 표기를 값으로 쓰는 구조가 남아 다음 배선에서 다시 걸린다.
> 그렇다고 공백을 그대로 통과시키면 `clusterKey`가 갈라진다. 근본 원인인 **client의 라벨 · 값 분리는
> 06 R31**로 따로 세웠다(client 이슈 #16).

> **보정 마이그레이션은 없다.** 공백이 낀 값은 애초에 400으로 막혀 저장된 적이 없다.
> `retrospects.purpose`에 이미 들어 있는 값은 전부 정본 표기다.

---

## 0-15. v2.19 변경 요약 (2026-09-09 — Flyway 체크섬 불일치 대응 절차)

| # | 항목 | v2.18 | **v2.19 확정** |
| --- | --- | --- | --- |
| E-98 | 로컬에서 Flyway 체크섬이 어긋날 때(`Migration checksum mismatch`) 무엇을 하는가 | 규정 없음 — 검색이 가장 먼저 알려주는 `flyway repair`를 부르게 된다 | **`repair`를 쓰지 않는다. 판별을 먼저 하고, 로컬은 볼륨을 버린다.** ① **판별** — `docker compose exec db psql -U sottaejap -d sottaejap -c "\d <그 마이그레이션이 만드는 테이블>"`로 실제 컬럼을 본다. 마이그레이션 파일이 적는 컬럼이 출력에 없으면 체크섬만 어긋난 것이 아니라 **스키마가 다르다.** ② **`repair`가 오답인 이유** — 기록된 체크섬을 지금 파일 값으로 덮어쓸 뿐 **빠진 컬럼을 만들지 않는다.** 기동은 지나가고 나중에 그 컬럼을 읽는 코드가 깨진다. ③ **해결은 `docker compose down -v` 후 재기동**이고 **로컬 개발 DB에서만** 쓴다. EC2 배포 DB(`deploy/docker-compose.yml`)에서는 쓰지 않는다 — 실제 사용자 데이터가 사라진다. ④ **`down -v`의 사정권은 명령을 실행한 폴더의 compose 프로젝트 볼륨뿐이다.** `sottaejap-server`에서 실행하면 `sottaejap-server_sottaejap_db_data` 하나이고 `sottaejap-demo_db_data`는 닿지 않는다. 07 §4 **C** · server README(이슈 #39 · PR #40) |

> **2026-09-09 실측.** `docker compose up -d --build` 뒤 `db` · `ai`는 `(healthy)`인데 `server`만 `Exited (1)`이었다.
> 로그는 `Migration checksum mismatch for migration version 8` · `Applied to database : 928890964` ·
> `Resolved locally : -541474916`이다. 원인은 demo 저장소 오염이 아니라 **볼륨이 파일보다 오래된 것**이었다 —
> server PR #22가 리뷰 중 `V8__financial_chunks.sql`에 `created_at`을 더한 뒤 squash로 병합됐고,
> `sottaejap-server_sottaejap_db_data`는 그 추가 이전 브랜치로 만든 DB였다. `\d financial_chunks`에
> `created_at`이 없다는 것이 근거다. 볼륨에 남아 있던 것은 로그인으로 생긴 `users` 몇 행뿐이라 버리는 비용이 없었다.

> **계약값이 아니라 실행 절차다.** 다른 문서가 인용하는 수치 · enum · 경로가 아니므로 이 번호를 가리키는 곳은
> 07 §4 C와 server README 둘뿐이다. 06 레포 조치는 세우지 않는다 — 반영이 이미 끝나 대기 중인 작업이 없다.

---

## 0-16. v2.18 변경 요약 (2026-09-09 — 온보딩 표본 추출 규칙 정정 · 읽는 범위 상한)

| # | 항목 | v2.17 | **v2.18 확정** |
| --- | --- | --- | --- |
| E-92 ③ 교체 | 기간에 표본을 펼치는 방법 | **최신순으로 늘어놓고 같은 간격으로 건너뛴다** — 간격 = 전체 ÷ `sampleSize`(최소 1) | **간격을 먼저 정하지 않는다. `i`번째 표본은 목록의 `i × 전체 건수 ÷ 뽑을 개수`번째다**(`뽑을 개수 = min(sampleSize, 전체 건수)`, `i`는 0부터, 소수점은 버린다). 종전 규칙은 정수 나눗셈의 나머지를 통째로 버려 **기간에서 가장 오래된 구간을 아예 보지 않았다** — 59건에서 20건을 고르면 간격이 2가 되어 39번째에서 멈추고 뒤 20건(34%)이 빠진다. 99건에서 20건이면 22건(22%), 150건에서 100건이면 50건(33%)이다. **전체가 `sampleSize`의 두 배 미만이면 간격이 1이 되어, 이 결정이 막으려던 "최신순 앞에서 자르기"와 완전히 같아진다**(25건에서 20건 → 최신 20건 그대로). 새 규칙은 비율과 무관하게 목록의 처음과 끝을 함께 덮고, 난수를 쓰지 않아 같은 요청이면 같은 표본이라는 성질(E-18)은 그대로다(server PR #35 리뷰). 05 §2 · 06 **R25** |
| E-92 ① 보강 | 표본을 고르기 전에 읽는 거래 수 | 규정 없음 — 기간 전체를 한 번에 읽었다 | **최신 2,000건까지만 읽고 그 안에서 표본을 고른다.** 업로드 1회의 행 수 상한이 server에 없고(방벽은 `multipart.max-file-size` **10MB** 하나뿐이며 여러 번 올리면 그것도 누적을 막지 못한다), 상한이 없으면 기간을 넓게 실은 요청 하나가 기간 안의 거래를 전부 메모리에 올린다. 2,000건을 넘는 기간에서는 **최신 2,000건 안에서** 표본을 펼친다 — 개인의 두 달치 CSV는 여기에 닿지 않는다. 수년치를 한 번에 다루게 되면 이 자리에서 페이지를 나눈다. 05 §2 |
| E-92 ① 명시 | 온보딩 표본과 D+1(E-62 ①) | 05 §2가 대상 조건을 "E-62 ②와 같은 조건"으로만 적어 ①의 적용 여부가 열려 있었다 | **온보딩 표본은 D+1을 보지 않는다.** 대상 조건은 기간과 "아직 회고하지 않았다" 둘뿐이다. 후보 API(#8)는 `retrospectDelayDays`가 지난 거래만 주지만, 온보딩은 방금 올린 기간을 그대로 돌아보는 자리다 — CSV에 오늘 거래가 섞이면 후보 API가 주지 않는 거래를 온보딩이 준다. `POST /retrospects`는 D+1을 검사하지 않으므로 그 표본도 저장이 막히지 않는다(server PR #35 리뷰에서 코드로 확인). 05 §2 |

> **새 규칙이 회귀를 잡는지 확인했다.** server `OnboardingServiceImplTest`에 첫·마지막 표본 id를 단언하는 테스트 3건을
> 넣고, 구현만 종전 규칙으로 되돌려 돌리면 정확히 그 3건이 실패한다(`12 tests completed, 3 failed`). 종전 테스트는
> 개수만 세어 편향이 있어도 통과했다.

---

## 0-17. v2.17 변경 요약 (2026-09-09 — 월간 리포트 실적 배분 시점 · 확정된 달의 전월 값)

| # | 항목 | v2.16 | **v2.17 확정** |
| --- | --- | --- | --- |
| E-94 ④ 명확화 | 목표 실적을 배분하는 확정 | ④는 "**지난달**이 확정되는 순간", 04 §3은 "**M** 확정 시 1회"로 두 문장이 다르게 읽혔다 — server PR #32는 04 쪽(어느 과거 달이든)이었다 | **직전 달(현재 KST 연월 − 1)이 확정되는 요청에서만 배분한다.** 그 이전 달은 확정만 하고 실적을 올리지 않는다. 어느 과거 달이든 배분하면 감소한 달은 더하고 더 쓴 달은 빼지 않아, 달성률이 "얼마나 아꼈나"가 아니라 "과거 달을 몇 개 열어봤나"에 좌우된다(server PR #32 리뷰). 04 §3 · 05 §2 동기화 |
| E-94 ③ 보강 | 확정된 달의 전월 값 | 규정 없음 — `previousTotalSpending` · `previousRepeatCount`를 지금 거래로 매번 다시 셌다. 그래서 확정된 달의 응답에서 굳은 값과 살아 있는 값이 섞이고, 전월 CSV를 나중에 올리면 `savedAmount ≠ previousTotalSpending − totalSpending`이 됐다 | **확정된 달은 전월 값도 굳는다.** `previousTotalSpending`은 저장된 `savedAmount`에서 역산(`totalSpending + savedAmount`, null이면 null) — 응답 안에서 04 §3의 식이 항상 성립한다. `previousRepeatCount`는 **전월 스냅샷이 있으면 그 값, 없으면 null** — 전월이 나중에 확정되면 한 번 채워지고 그 뒤로 움직이지 않는다. **단, `previousTotalSpending`이 null(전월 없음으로 확정)이면 같이 null** — 두 값은 같은 전월을 말해야 하고 "지난달 없음 · 지난달 반복 3회"가 한 응답에 나오면 안 된다(server PR #32 리뷰 2라운드). 스키마 변경 없음. 확정하지 않은 달(이번 달)은 종전대로 매번 계산 |
| E-94 ⑤ 보강 | 확정의 하한 | 없음 — `?yearMonth=1900-01`도 0으로 채운 스냅샷 행을 만들었다 | **첫 거래월 이전 달은 저장하지 않는다**(계산만, `finalized: false`). 거래가 하나도 없는 사용자도 같다. 데이터 없는 달 200은 그대로 |

> `Goal.currentAmount` 갱신은 **DB에서 원자적으로 더한다**(`update … set current_amount = current_amount + :amount`). 서로 다른
> 달을 동시에 확정하는 두 트랜잭션이 같은 목표 행을 각자 읽고 각자 쓰면 앞의 배분이 증발한다 — 구현 규칙이라 07이 아니라
> server `GoalRepository` 주석에 둔다.

---

## 0-18. v2.16 변경 요약 (2026-09-09 — 업로드 뒤 재계산의 건너뜀 조건 · 제안 목록 영향)

| # | 항목 | v2.15 | **v2.16 확정** |
| --- | --- | --- | --- |
| E-96 | 업로드 뒤 재계산을 건너뛰는 조건 (E-95 ⑤ 재정의) | **`importedCount`가 0이면 건너뛴다** — 전량 중복 재업로드는 기준월도 금액도 바꾸지 않는다 | **`importedCount`로 판단하지 않는다. 조건은 "회고가 0건"이고, 판단은 `ClusterRecomputeServiceImpl`이 회고를 읽은 자리에서 한다.** 종전 조건은 E-95 ④의 복구 경로를 정확히 막았다 — 재계산이 실패해 어긋난 값이 남으면 사용자가 가장 먼저 하는 행동이 **같은 CSV 재업로드**이고, 그때 `importedCount`는 0이라 재계산을 건너뛰어 회고를 새로 저장할 때까지 같은 화면에 갇힌다(server PR #31 리뷰). 새 조건은 증명 가능한 no-op이다 — 회고를 지우는 경로가 server에 없으므로(삭제 API는 `goals` · `push-subscriptions` 둘뿐) "회고 0건"은 곧 "묶음 0건"이고 지울 기존 묶음도 있을 수 없다. **막는 것이 하나 더 있다** — 회고 0건으로 재계산이 돌면 `ShrinkageRule.userAverage`가 빈 입력에 `0.0`을 내고 그 값이 `User.avgSatisfaction`에 박힌다. 회고를 한 번도 하지 않은 사용자에게 평균 만족도 0이 남는다. 조기 반환이 이것도 막는다. **걸리는 경로가 둘이고 둘 다 실재한다** — 온보딩 3단계(업로드 — 표본 회고 전)와 **`POST /onboarding/complete`(E-92 · server #35)** 다. 후자는 회고 0건으로도 온다 — `complete`가 "회고가 0건이어도 플래그는 저장한다"이고, client `OnboardingView.vue`의 4단계 버튼 둘(**"나중에 회고하기"** · 후보가 없을 때의 **"홈으로 가기"**)이 회고를 하나도 저장하지 않고 `finishOnboarding`으로 간다(server PR #31 2차 리뷰 실측). **"온보딩은 표본 회고 뒤라 회고 ≥ 1"이라는 근거로 이 가드를 지우지 않는다** — 온보딩에서 가장 흔한 이탈 경로가 곧 이 가드가 막는 경로다. 05 §2 · 06 **R24** |
| E-97 | 업로드 뒤 `GET /suggestions`의 상태 (E-81 · E-95 보강) | 05 §2가 `GET /analysis` · `GET /satisfaction-map`만 적는다 | **`GET /suggestions`의 `PROPOSED`도 새 기준월 기준으로 다시 계산되고, 대상이 없으면 빈다.** 재계산은 끝에서 `SuggestionSyncService.sync`를 부르고(E-81), 새 달 CSV 직후에는 기준월 거래가 있는 묶음이 없어 `avgAmount`가 전부 `null`이 된다 → `SuggestionTargetRule.isTarget`이 전부 탈락 → `removeStale`이 `PROPOSED`를 지운다. `ADOPTED` · `REJECTED`는 그대로다. **이것은 새 동작이 아니다** — 고치기 전에도 그 달 첫 회고를 저장하는 순간 같은 삭제가 일어났고, E-95는 그 시점을 업로드로 당길 뿐이다. 지도 · 금액과 제안이 같은 순간에 새 달 상태가 되므로 앞뒤가 맞는다. 다만 05가 적지 않아 클라이언트가 "업로드했더니 제안이 사라졌다"를 버그로 볼 여지가 있었다(server PR #31 리뷰). 05 §2 |

> **측정.** 업로드 응답에 붙는 재계산 비용을 실측했습니다(신규 PostgreSQL · 회고 30 · 100 · 300건). `recomputeAll`은
> **9~12ms**로 회고 수에 거의 반응하지 않고, CSV 1,200행에서 **12ms**, 17,000행(613KB)에서 **93ms**입니다. 같은 조건의
> `upload`(파싱 + 저장)는 각각 341~702ms · 4,785ms입니다. **E-95가 더한 비용은 전체의 2~3%입니다.**
>
> client가 모든 요청에 공통으로 거는 20초 타임아웃(`httpClient.ts`)을 위협하는 것은 재계산이 아니라 **파싱과 저장**입니다.
> 613KB에 4.8초이므로 `multipart.max-file-size` 상한 10MB에 가까운 파일은 20초를 넘깁니다 — E-95 전에도 같았던 위험이고,
> 대응은 업로드 행수 상한이나 client 타임아웃 분리입니다. 06 **R28**로 뺍니다.

---

## 0-19. v2.15 변경 요약 (2026-09-09 — 온보딩 표본 추출 계약)

| # | 항목 | v2.14 | **v2.15 확정** |
| --- | --- | --- | --- |
| E-92 | 온보딩 표본(FR-09-01)을 만드는 자리와 고르는 방법 | `ONBOARDING_SAMPLE` 사유만 05 §2 표에 있고 **만드는 경로가 없다** (v2.13에서 이 번호를 비워 두었다) | **`POST /onboarding/start`가 만든다.** 요청 기간(`periodFrom`~`periodTo`) 안에서 아직 회고하지 않은 거래를 **최신순으로 늘어놓고 같은 간격으로 건너뛰며** `sampleSize`건 고른다(간격 = 전체 ÷ `sampleSize`, 최소 1). 응답은 `GET /retrospects/candidates`와 **같은 `candidates` 배열**이고 `reasonCode`는 전부 `ONBOARDING_SAMPLE`이다. `sampleSize` 상한은 후보 API와 같은 **100**. 05 §2 · 06 **R25** |

> **왜 후보 API(#8)로 갈음하지 않는가.** 후보 규칙 ③④⑤ 중 ⑤(낮은 만족도 반복)는 이전 회고가 있어야 걸리는데
> 온보딩 시점의 회고는 0건입니다. 규칙은 **최신 100건 안에서만** 봅니다(E-62 · 05 §2 v2.9). 두 달치 CSV를 올려도
> 표본 20건(FR-09-01)이 모이지 않습니다 — 로컬 실측에서 `GET /retrospects/candidates?limit=20`이 **0건**이었습니다.
> 그래서 온보딩만 별도 경로를 둡니다.
>
> **왜 최신 20건이 아니라 균등 간격인가.** 최신순으로 앞에서 20건을 자르면 마지막 한 주만 돌아보게 되어
> "26.06~07 표본"이 아니게 됩니다. 첫 만족도 지도의 묶음이 한 주에 쏠립니다.
>
> **표본 목록은 저장하지 않습니다.** 후보에 상태를 두지 않는다는 E-49 · E-65를 그대로 잇습니다. 같은 요청이면
> 같은 표본이 나오고(난수 없음 — E-18), 회고를 저장하면 그 거래가 다음 호출에서 자연히 빠집니다.
>
> **`POST /onboarding/complete`의 `clusterCount`는 지도에 찍히는 점의 수입니다** — `retrospectCount > 0`이고
> 상위가 없는 묶음(E-72 유효 묶음)의 개수입니다. 지도·분석이 쓰는 조회 하나를 그대로 씁니다.

---

## 0-20. v2.14 변경 요약 (2026-09-09 — CSV 업로드 뒤 묶음 재계산)

| # | 항목 | v2.13 | **v2.14 확정** |
| --- | --- | --- | --- |
| E-95 | CSV 업로드 뒤 묶음 재계산 · 배치 자리 (06 R24 · server 이슈 #27) | 거래를 넣는 유일한 경로 `POST /transactions/upload`가 재계산을 부르지 않는다. 기준월은 실시간으로 계산하고(E-78) 금액 · 부담 · 좌표는 묶음 행에 저장된 값이라(E-61), 새 달 CSV를 올리면 **새 기준월 이름표에 지난달 금액**이 붙는다 | **① 돌린다.** 업로드가 기준월을 바꾸는 순간이므로 그 자리에서 다시 계산한다. **② 배치는 파사드 빈이다** — `transaction/service/TransactionUploadFacade`가 `TransactionService.upload`와 `ClusterRecomputeService.recomputeAll`을 순서대로 부른다. `upload`에 `ClusterRecomputeService`를 직접 주입하면 `ClusterRecomputeServiceImpl`이 이미 `TransactionService`를 물고 있어(E-78) `BeanCurrentlyInCreationException`으로 기동이 죽는다(실측 · `spring.main.allow-circular-references` 기본값 false). **③ 재계산은 업로드 커밋 뒤 별도 트랜잭션이다.** 파사드에 `@Transactional`을 붙이지 않는다. 업로드 응답 시간에 전체 재계산과 `SuggestionSyncService.sync`(E-81)를 넣지 않고, 재계산이 실패해도 파싱에 성공한 CSV를 되돌리지 않는다. **④ 재계산이 실패해도 업로드는 200으로 응답한다.** 파사드가 `RuntimeException`을 WARN 로그로만 남긴다. 업로드는 사용자가 파일을 고르고 기다리는 행위라 성공률이 응답 속도보다 앞서고, 어긋난 값은 다음 회고 저장이나 예산 변경의 재계산이 덮는다. **그 사이 사용자가 새 기준월 이름표에 지난달 금액을 볼 수 있다 — 이것이 이 선택의 비용이고 버그가 아니다.** **⑤ 새로 저장된 거래가 0건이면 재계산을 건너뛴다** — 전량 중복 재업로드는 기준월도 금액도 바꾸지 않는다. 05 §2 · 06 **R24** |

> **`가`(컨트롤러) · `다`(이벤트)를 고르지 않은 이유.** 트랜잭션 경계는 세 안이 같습니다 — 셋 다 업로드를 커밋한
> 뒤에 돕니다. 갈린 것은 배치 자리입니다. 컨트롤러에 두면 예외를 삼키는 `try/catch`가 `@RestController`에
> 들어가고, 완료 조건의 통합 테스트가 MockMvc와 인증 주체 스텁을 새로 들여야 합니다. 파사드는 기존
> `AnalysisIntegrationTest`처럼 빈을 직접 주입해 끝납니다. 이벤트(`@TransactionalEventListener(AFTER_COMMIT)`)는
> 이 저장소에 이벤트가 하나도 없어 기계장치가 늘고, `AFTER_COMMIT` 리스너의 예외는 커밋 호출자에게 전파돼
> **거래는 남고 응답만 500**이 됩니다. 그것을 막으려면 리스너 안에 같은 `try/catch`를 둡니다 — 얻는 것이 없습니다.
> **`라`(재계산이 `TransactionRepository`를 직접 보는 안)는 반대합니다.** E-78의 "기준월 산출지는 하나"를 깹니다.
>
> **창은 좁아지고 닫히지는 않습니다.** 재계산이 실패하면(DB 순단 · `RuleParamMissingException`) 사용자는 다음 회고
> 저장이나 예산 변경 전까지 이 이슈의 화면을 다시 봅니다. 창을 닫는 안은 업로드와 재계산을 한 트랜잭션에 넣는
> `나`뿐이고, 그 대가는 정상적으로 파싱된 CSV까지 되돌리는 것입니다. 본선 데모 경로(온보딩 — 업로드 → 표본 회고)는
> 순서가 반대라 이 창을 밟지 않습니다.

---

## 0-21. v2.13 변경 요약 (2026-09-09 — 거래 목록 · 월간 리포트 계약)

> E-92는 온보딩 표본 API(server 이슈 #28, softcatch)가 채번 예정이라 비워 둔다.

| # | 항목 | v2.12 | **v2.13 확정** |
| --- | --- | --- | --- |
| E-93 | `GET /transactions` 계약 (05 #7 · FR-02-02 · 회고 이력 겸용) | 05 §1 목록 표에만 있고 상세 없음 | **쿼리 `from` · `to`(KST 날짜, 양끝 포함) · `category` · `hasRetrospect` · `page`(0부터) · `size`(기본 20 · 상한 100, 넘으면 100으로 자름 · 1 미만은 400).** 정렬은 내부 AI 조회와 같은 `occurredAt desc, id desc`. 항목에 **회고 요약 `retrospectId` · `satisfaction`(없으면 null)** 을 실어 거래 화면과 회고 이력 탭이 한 API를 쓴다. `hasRetrospect=true`가 회고 이력 탭이다. 총건수는 `totalElements` · `totalPages`. 내부 AI `TransactionAiView`와 DTO를 공유하지 않는다(05 §3 계약 고정). 남의 거래는 보이지 않고 빈 결과는 200. server 이슈 #29 |
| E-94 | 월간 리포트 `GET /reports/monthly` · `MonthlySnapshot` (05 #17 · FR-08-06 P0 · FR-08-07) | 테이블만 있고 산식 · 생성 시점 · 목표 실적 규칙 없음 (E-80 "다음 판") | **① `savedAmount` = 전월 `totalSpending` − 당월 `totalSpending`.** 전체 지출 차이고 음수를 그대로 낸다(더 쓴 달). 예산 · 판정과 무관하므로 예산 null 사용자도 나온다. 전월 값이 없으면 null. **② `repeatCount`** = 유효 묶음 ∧ RESOLVED ∧ ADJUST 묶음의 그 달 거래 건수 합(E-72 · E-73과 정합). **`unsatisfiedCount`** = 그 달 거래에 달린 회고 중 `LOW` 건수. **③ 스냅샷 시점 — 지난달은 첫 조회 때 확정, 이번 달은 매번 계산.** `yearMonth` < 현재 KST 연월이면 스냅샷이 없을 때 계산해 upsert하고 이후 고정한다(확정된 달은 다시 계산하지 않는다). `yearMonth` = 현재 연월이면 계산만 하고 저장하지 않는다. 미래 달은 400. 스케줄러 없음. **④ `Goal.currentAmount` 실적** — **직전 달(현재 연월 − 1)** 이 확정되는 요청에서만(그 이전 달은 확정만, v2.17) `savedAmount > 0`이면 그 금액을 `ADOPTED` 제안이 붙은 목표들에 `expectedSaving` 비율로 정수 배분해 `currentAmount`에 더한다(나머지는 배분액이 가장 큰 목표에). 붙은 목표가 없으면 배분하지 않는다. 채택 시점 규칙(E-82)은 그대로다 — 실적은 "실제 감소액"만이다. 확정과 배분은 한 트랜잭션이라 두 번 더해지지 않는다. **⑤ 데이터 없는 달은 200**(`totalSpending` 0, 전월 없으면 `savedAmount` null). 404 없음. 기준월 기본값은 `TransactionService.analysisYearMonth`(E-60 · E-78). `rules/`에 순수 함수(`MonthlyDeltaRule`), 현재 연월은 서비스가 `Clock`으로 넘긴다. server 이슈 #30 |

---

## 0-22. v2.12 변경 요약 (2026-09-08 — `highlight` AI 미호출 조건 확대)

| # | 항목 | v2.11 | **v2.12 확정** |
| --- | --- | --- | --- |
| E-91 | `highlight` AI 미호출 조건 (E-75 · E-89 보강) | **유효 묶음 0개일 때만** AI를 부르지 않는다 | **`byCategory`가 비어 있을 때도 부르지 않는다.** 즉 `유효 묶음 0개 ∨ byCategory 비어 있음`이면 Spring 템플릿으로 바로 간다. E-89의 4번째 문장은 `유효 묶음 ≥ 1 ∧ byCategory 비어 있음`에서만 나오는데, 그 조건이 곧 AI를 부르는 조건이라 **`OPENAI_API_KEY`가 있는 환경에서는 E-89 문장이 한 번도 나가지 않았다**(server PR #25 리뷰). 05 §2 · 06 **R22** |

> 이 경우 AI에 보내는 `state`는 `by_category []` · `by_verdict`의 금액이 전부 0입니다. 재구성할 수치가 하나도
> 없어 LLM이 무엇을 쓰든 집계 밖 서술이 되고(NFR-02), 그것을 걸러야 할 `analysis_narrate` 숫자 가드(E-79 · 06 R19)는
> `sottaejap-ai`에 아직 없습니다. E-75가 AI를 건너뛰는 근거로 든 "할 말이 정해져 있는데 15초를 쓸 이유가 없다"가
> 그대로 적용됩니다 — 이 분기에서 템플릿은 무엇이 들어와도 한 문장만 돌려줍니다.
>
> **E-89 잠정 문구에 후보를 하나 더 답니다.** "이번 달에 돌아본 소비가 아직 없어요"는 `이번 달에`가 회고한 시점에
> 붙는지 소비한 시점에 붙는지 두 갈래로 읽히고, 이 사용자는 회고를 이번 달에 했습니다. **"이번 달 거래 중 돌아본
> 것이 아직 없어요"**를 함께 올려 두고 문구 담당이 고릅니다.

---

## 0-23. v2.11 변경 요약 (2026-09-08 — 서비스 응답 톤·형식 확정)

| # | 항목 | v2.10 | **v2.11 확정** |
| --- | --- | --- | --- |
| E-90 | AI 자연어 응답 톤·형식 | 후회 언어·판결 어휘 금지, 지킬 소비 우선, 규칙 엔진 근거 재구성 | 기존 원칙을 유지하고 **해요체**로 답한다. 반말·격식체·이모지는 쓰지 않고, **한두 문장의 한 문단**으로 짧게 답한다. 지킬 만한 소비가 있으면 그것부터 먼저 언급한다. `sottaejap-ai` 공통 `SYSTEM_PROMPT`와 Few-shot 예시는 이 원칙을 따른다 |

> 새로운 판정·계산 정책이 아니라 기존 §7의 표현 원칙을 실제 응답 형식까지 구체화한 결정입니다.
> Task별 지시문과 LLM 호출 파라미터는 바꾸지 않습니다.

---

## 0-24. v2.10 변경 요약 (2026-09-08 — `highlight` 템플릿 4종화)

| # | 항목 | v2.9 | **v2.10 확정** |
| --- | --- | --- | --- |
| E-89 | `highlight` Spring 템플릿 (E-75 보강) | **3종** — 유효 묶음 0개 · ADJUST 카테고리 있음 · 없음 | **4종.** "유효 묶음은 있는데 기준월 합계가 0"(회고한 거래가 전부 기준월 밖 — 온보딩 표본을 지난달에 회고하면 바로 밟는 경로)을 따로 말한다. 지금은 이 경우에 "바꿔보고 싶은 소비가 눈에 띄지 않았어요"(NO_ADJUST)가 나가 사실과 어긋난다(server PR #16 리뷰). ~~**잠정 문구:** "이번 달에 돌아본 소비가 아직 없어요. …"~~ **v2.30 확정:** "이번 달 거래 중 돌아본 것이 아직 없어요. 이번 달 거래를 몇 건 회고하면 특징이 보이기 시작해요." (server PR #53). 판별 조건은 `유효 묶음 ≥ 1 ∧ byCategory 비어 있음`. 06 **R22** |

---

## 0-25. v2.9 변경 요약 (2026-09-08 — LLM 타임아웃 예산 조정)

| # | 항목 | v2.8 | **v2.9 확정** |
| --- | --- | --- | --- |
| E-88 | LLM 타임아웃 `LLM_TIMEOUT_SECONDS` | **8초** · 재시도 1회 | **6초** · 재시도 1회. 재시도 포함 최악 12초로 Spring `/chat` 15초 타임아웃 예산 안에 맞춘다 (`sottaejap-ai` PR #9 · `bc1d687`). **아직 잠정값 — 실측 전.** 실제 회고 추출 프롬프트로 응답 시간을 측정한 뒤 재확인한다 |

> LLM 단독 호출 예산만 조정했습니다. Tool Calling으로 Spring 내부 조회가 더해지는 경로는 07 §10 리스크 4에 따라
> 통합 환경에서 다시 계산해야 합니다.

---

## 0-26. v2.8 변경 요약 (2026-09-08 — 최근 대화 정렬 계약)

| # | 항목 | v2.7 | **v2.8 확정** |
| --- | --- | --- | --- |
| E-87 | `recentMessages` · `recent_messages` 정렬 | 최근 6건만 전달하고 배열 순서는 미정 | **오름차순(오래된 → 최신)**. 마지막 원소가 가장 최근 발화다. `POST /retrospects/chat`의 `recentMessages`와 Spring이 AI `POST /chat`에 보내는 `recent_messages`에 같은 규칙을 적용한다. 최근 6건을 DB에서 내림차순으로 조회하더라도 전달할 때는 오름차순으로 정렬한다. AI는 타임스탬프가 없는 메시지 배열의 순서를 추정하거나 다시 정렬하지 않는다 |

> **계약 변경입니다.** 클라이언트와 Spring은 최근 대화를 오래된 발화부터 담고, AI는 이 순서를 전제로
> 배열을 역순 탐색해 가장 최근 assistant 메시지를 찾습니다. 세 코드 저장소가 같은 규칙을 따라야 합니다.

---

## 0-27. v2.7 변경 요약 (2026-09-07 — 금융 RAG 개통)

> v2.7은 **P2로 미뤄 두었던 금융 RAG(FR-12)를 실제로 켜는** 판입니다.
> 검색·임베딩·적재 코드는 v1.3부터 있었고, 비어 있던 것은 저장소 스키마와 기동 배선 두 가지뿐입니다.

| # | 항목 | v2.6 | **v2.7 확정** |
| --- | --- | --- | --- |
| E-85 | `FinancialChunk` 저장소 스키마 (E-21) | "P2 착수 시 `V3`" · 차원 `vector(N)` 미정 | **`V8__financial_chunks.sql`** — `V3`는 `V3__drop_card_issuer.sql`(E-52)이 이미 썼고 `V7`까지 나갔다. 차원은 **1536** (`text-embedding-3-small` — `ai/app/rag/embedding.py`가 이미 고정한 값을 문서가 따라간다, "레포 우선"). `CREATE EXTENSION IF NOT EXISTS vector`를 같은 파일에서 실행하고, `chunk_id`에 UNIQUE를 건다 — 재적재가 `ON CONFLICT (chunk_id) DO UPDATE`로 돌기 때문이다. **벡터 인덱스(hnsw · ivfflat)는 두지 않는다** — 데모 규모(수백 청크)에서는 순차 스캔이 더 빠르고 `ivfflat`의 `lists`는 데이터가 쌓인 뒤에야 정할 수 있다. 스키마 소유자는 server이므로 `ai`는 만들지 않고 읽기만 한다 |
| E-86 | 금융 RAG를 언제 켜는가 · 저장소가 죽으면 | 규정 없음 (`FINANCIAL_RAG`를 등록하는 경로 자체가 없었다) | **`DATABASE_URL`이 있을 때만** `ai`가 기동 시 풀을 열고 `FINANCIAL_RAG`를 등록한다. 없으면 **기동은 정상이고** `FINANCE_QA`만 근거 없음 경로로 내려가 "확인할 수 없다"고 답한다 (FR-12-02) — 없다고 기동을 실패시키면 `ai` 없이도 흐름이 완주해야 한다는 E-38 방어선이 무너진다. 풀은 기동 시점에 접속하지 않는다(`min_size=0`) — compose에서 `ai`는 `db`의 healthcheck를 기다리지 않는다. **DB 장애·테이블 없음도 같은 답이다** — Retriever 경계가 `RetrieverUnavailableError`로 바꾸고 `call_tool`이 흡수해 `success=False`로 만든다. asyncpg 예외를 그대로 올리면 `/chat`이 500이 되어 E-38과 어긋난다 |

> **계약 변경이 아닙니다** — 테이블 하나가 새로 생길 뿐 경로 · DTO · enum · 오류 코드가 그대로이고,
> `POST /chat/finance`의 응답 형식도 같습니다. 근거가 생기면 `reply` 문장의 내용만 달라집니다.
> **06 R12가 종결됩니다.**
>
> **남은 것:** 적재할 공개 문서 선정(FR-12-04)과 임베딩 API 권한입니다. 원금 손실 위험 상품 문서를 넣으면
> FR-12-03이 막아야 할 답에 근거가 생겨 버리므로, 문서 선정은 이 판에서 하지 않았습니다.

---

## 0-28. v2.6 변경 요약 (2026-09-07 — 제안 · 목표 슬라이스 · 규칙 엔진 ⑦)

> v2.6은 **줄여볼 소비를 언제 제안하고, 채택이 무엇을 바꾸는가**를 정한 판입니다.
> 대상·정렬·산식은 이미 v2.5(E-72 · E-73 · E-76)에 걸려 있어, 새로 정한 것은 네 가지뿐입니다.

| # | 항목 | v2.5 | **v2.6 확정** |
| --- | --- | --- | --- |
| E-81 | 제안을 언제 만드는가 | 규정 없음 (`suggestions` 테이블만 있고 쓰는 경로 없음) | **재계산 파생 행이다.** `recomputeAll` 끝에서 동기화한다. 대상 = 유효 묶음(E-72) ∧ `RESOLVED` ∧ `ADJUST` ∧ `avgAmount != null` — **상위 묶음 · `MINOR` · `quadrant`가 null인 것도 포함**하고 `PROTECT`는 넣지 않는다(02 §FR-07 각주 "그 목록은 '줄일 것'의 목록"). 대상마다 `PROPOSED` 1행(`adjustCount = txCount` · `expectedSaving = avgAmount × adjustCount`), 재계산마다 제자리 갱신, 비대상이 된 `PROPOSED`는 삭제. **`ADOPTED`·`REJECTED`는 보존·불변**이고 그 묶음에 새 `PROPOSED`를 만들지 않는다(재제안은 P2). 정렬은 `PRIORITY` → `MINOR` → null, 각 `burdenRatio` 내림차순(null 마지막), `id` 오름차순. 스케줄러도 배치도 두지 않는다 — "지도에 보이는 것 = 제안 목록"이 항상 맞다 |
| E-82 | 채택 · 거절 계약 | 미정 | `adopt`는 `{ adjustCount(1..txCount), goalId(내 목표 · 미삭제) }` → `ADOPTED` · `expectedSaving` 재계산. `reject`는 본문 없음. 전이는 `PROPOSED → ADOPTED/REJECTED`, **`ADOPTED → ADOPTED`(횟수·목표 수정 = FR-08-05) · `ADOPTED → REJECTED`(철회)**, `REJECTED`는 종단이라 400 `INVALID_INPUT`. 없거나 남의 제안·목표는 404. **채택은 `Goal.currentAmount`를 바꾸지 않는다** — 채택은 '예상'이고 `currentAmount`는 '실적'이다. 섞으면 쓰지도 않은 돈으로 달성률이 오른다. 실적 반영 시점은 월간 리포트 판에서 정한다 |
| E-83 | Goal 계약 | 필드만 있고 검증·집계 미정 | `name` 1~50 · `targetAmount ≥ 1` · `currentAmount ≥ 0`(기본 0), 어기면 400. soft delete(`deletedAt`)이고 `suggestions.goal_id`는 그대로 둔다. 목록이 `adoptedSaving`(그 목표에 붙은 `ADOPTED`의 `expectedSaving` 합) · `achievementRate = current ÷ target` · `projectedRate = (current + adoptedSaving) ÷ target`을 raw double로 낸다 — 화면(S13 · FR-08-04)이 "지금"과 "채택대로 가면"을 나란히 보여야 한다. 반올림은 화면이 한다 |
| E-84 | 패키지 · 이번 범위 | — | server `suggestion/` · `goal/`. `reason`은 **Spring 템플릿 3종**이고 AI를 부르지 않는다 — 판정에서 바로 나오므로 `ai` 컨테이너가 내려가도 제안 목록이 뜬다(E-38 · `PrescriptionTemplate` 선례). **제안 발생 알림(FR-08-05 · FR-10-03)은 이번 범위 밖** — 재계산마다 제안이 생겼다 없어졌다 하므로 "언제 한 번만 보낼지"를 먼저 정해야 하고, 정하지 않고 붙이면 재계산마다 알림이 쏟아진다 |

> **계약 변경은 아닙니다** — 경로·enum이 새로 생길 뿐 기존 응답이 바뀌지 않습니다. 다만 05 §0에 `suggestionStatus`,
> §2에 본문 명세 6종이 신설되고 07 §7에 `V7__suggestion_indexes.sql`이 추가됩니다.
> **`suggestions`에 `user_id`가 없습니다** — 사용자 판별은 `behavior_clusters` 조인 하나뿐이므로, "남의 제안"을
> 404로 막는 지점이 그 조인 조건입니다.

---

## 0-29. v2.5 변경 요약 (2026-09-07 — 분석 슬라이스 · 규칙 엔진 ⑧)

> v2.5는 **묶음 집계를 무엇으로 보고 어떻게 문장으로 만드는가**를 정한 판입니다.
> 지도·분석이 처음으로 실제 수치를 냅니다. 제안·목표(⑦)는 다음 판입니다.

| # | 항목 | v2.4 | **v2.5 확정** |
| --- | --- | --- | --- |
| E-72 | 지도·분석의 대상 묶음 | 규정 없음 | **유효 묶음 = `retrospectCount > 0 && parentId == null`.** 롤업된 리프는 상위 묶음이 같은 금액을 이미 들고 있어 두 번 세게 된다. 필터는 리포지토리 조회 하나로 고정한다. 단 `GET /behaviors/{id}`는 **롤업된 리프도 열어 주고 `parentId`를 준다** — 회고 저장 응답이 리프 id라서, 막으면 저장 직후 화면이 막힌다. 회고 수 0인 묶음은 404 |
| E-73 | `GET /analysis` 집계 산식 (⑧) | 미정 | **`share = monthlyTotalAmount ÷ User.monthlyBudget`** (05 예시 역산과 일치, 예산 없으면 null, 반올림 없음). `byVerdict`는 항상 SUSTAIN·ADJUST 2행. `pending`은 별도. `byCategory`는 묶음 키 첫 자리로 묶고 `avgAmount = Σ합계 ÷ ΣtxCount`, `dominantTimeSlot`은 합계 최대 시간대(동률은 이른 쪽, 키에 시간대 없으면 null), `verdict`는 합계 최대 **RESOLVED** 묶음의 것(전부 보류면 null, **보류 금액은 합계에 포함**). 정렬은 합계 내림차순 → 카테고리 오름차순, 합계 0은 제외 |
| E-74 | `boundaries` 노출 | `null` — 클라이언트가 축을 안 그림 | **`rules.axis-x-boundary` · `axis-y-boundary` 잠정값을 그대로 내려준다** (0.1 / 0). 값이 잠정이어도 축이 있는 편이 낫다. 05 §2 · 07 §8의 "null이면 그리지 않는다"는 **폐기** — **[계약변경]** |
| E-75 | `highlight` 생성 경로 (⑨) | 미정 | **`GET /analysis`가 그 자리에서 `ANALYSIS_NARRATE`를 부른다.** state는 `analysis_year_month` · `by_verdict[]` · `by_category[]`뿐이고 **`pending`은 싣지 않는다**(판정 없는 금액을 문장 근거로 주지 않는다). 503 · 빈 문장 · `fallback: true` · 유효 묶음 0개(이때는 AI 미호출) → **Spring 템플릿 3종**. GET은 언제나 200. **내부 AI `analysis`에는 `highlight`가 없다** |
| E-76 | 처방 5종 · CTA 3종 | CTA는 `PROTECT`만 (E-12) | **PROTECT `{RESERVE_BUDGET, "예산 확보하기"}` · MINOR·PRIORITY `{ADJUST, "조정하기"}` · KEEP·보류 `null`** (03 §7 W-6 채택). 예산이 없어 `quadrant`가 null인 RESOLVED는 처방을 판정으로 고르고(SUSTAIN→KEEP 문장, ADJUST→MINOR 문장) CTA는 없다. 05 §0에 `ctaType` 신설 — **[계약변경]** |
| E-77 | 월 예산 입력 경로 | 화면에만 있고 저장 경로가 없었음 | **`PUT /users/me/settings`(05 #3)를 구현한다.** 세 값 모두 선택이고 null은 "그대로 두기", 셋이 전부 비면 400. **예산이 바뀌면 묶음을 다시 계산한다** — `burdenRatio`·`quadrant`는 재계산이 행에 써 둔 값이라 예산만 고치면 지도가 옛 기준으로 남는다 |
| E-78 | `analysisYearMonth` 산출지 | 서비스 두 곳에 중복 | **`TransactionService.analysisYearMonth` 하나로 고정** (E-60의 최근 거래월). 묶음 행에서 읽으면 비워진 묶음의 옛 달이 섞인다 |
| E-79 | AI 숫자 가드의 소수 처리 | 정수만 검사 | **소수를 한 수로 파싱해 `share × 100`과 견준다.** `3.6%`를 `3`·`6`으로 쪼개면 둘 다 한 자리라 서수 예외로 빠져, 지어낸 `9.9%`가 검사 없이 나갔다. **가드가 걸리면 `fallback: true`를 세운다** — 그래야 Spring이 자기 템플릿으로 갈음한다 (06 R19) |
| E-80 | 패키지 · 범위 | — | server `analysis/`(지도·분석·묶음 조회)와 `rules/aggregate/`(순수 집계). `GET /suggestions` · `goal/` · `GET /reports/monthly`는 **다음 판**이다 |

> ⚠️ **E-74 · E-76은 계약 변경입니다** (07 §6). 클라이언트는 `ctaType` 2종을 받아야 하고, 축 경계선을 그려야 합니다.
> 같은 라운드에 **클라이언트 `TIME_SLOT`을 4종으로** 맞춰야 합니다 (06 R13) — `byCategory[].dominantTimeSlot`이
> `DAY`·`EVENING`을 실제로 내보내기 시작하기 때문입니다.
> **예산 미설정 상태가 정상 경로입니다** — `burdenRatio` · `quadrant` · `cta` · `share` · `axisX.monthlyBudget`이 모두
> `null`일 수 있고, 거래가 없으면 `analysisYearMonth`도 `null`입니다. 클라이언트 타입이 이를 표현해야 합니다.

---

## 0-30. v2.4 변경 요약 (2026-09-07 — 회고 알림 시각)

> v2.4는 알림을 **언제** 보낼지 하나만 정한 판입니다. 무엇을 보낼지(E-62 후보 선별)와 어떻게 닿는지(E-68 Web Push)는 그대로입니다.
> **E-71은 E-68의 "기본 09:00 KST"를 대체합니다.** 경로·DTO·enum이 그대로이므로 계약 변경(07 §6)이 아닙니다.

| # | 항목 | v2.3 | **v2.4 확정** |
|---|---|---|---|
| E-71 | 회고 알림 시각 | 전 사용자 **매일 09:00 KST** 고정 (E-68) | **후보 거래의 결제 시각에 맞춘다.** 알림 시각 = `결제 시각 − 1시간`을 `07:00~21:00`으로 자르고 30분 격자에 내린 값. D+1 회고는 "어제 그 시간"을 되짚는 것이라, 그 시간대가 다가올 때 말을 거는 편이 09시 일괄보다 회고가 붙는다. 새벽 1시 결제는 그날 **07:00**, 22시 결제는 **21:00**에 간다 — 창 밖으로는 보내지 않는다. **날짜는 보지 않으므로** 며칠 전 거래가 후보로 잡혀도 규칙이 그대로 선다. 스케줄(`NOTIFICATION_DAILY_CRON`)은 **30분마다** 돌며 "지금이 이 사람의 시각인가"만 본다 (기본 `0 0,30 * * * *`) |

> **사용자가 시각을 고르는 설정은 두지 않습니다.** 거래에서 계산되므로 고를 것이 없고, 설정을 두면 컬럼 · 설정 API · 화면이 따라옵니다. 필요해지면 그때 덮어쓰기 값을 더합니다.
> **어느 거래로 말을 걸지는 여기서 정하지 않습니다** — 규칙 엔진 ⓪(E-62)이 고른 후보를 그대로 씁니다. 알림과 `GET /retrospects/candidates`가 다른 거래를 가리키면 안 됩니다.
> **목록 조회 시 생성(E-68의 그물)은 목표 시각이 지난 뒤에만 합니다.** 오전에 목록을 열었다고 저녁 알림을 미리 소진하면, 정작 그 시각에는 하루 1건 가드(FR-03-03)에 걸려 아무것도 오지 않습니다.

---

## 0-31. v2.3 변경 요약 (2026-09-07 — 알림 전달 경로 · 대화 이력 · 내부 API 봉투)

> v2.3은 **알림이 사용자에게 어떻게 닿는가**와 **대화 맥락을 누가 들고 있는가**를 정한 판입니다.
> 앞의 둘(E-67·E-68)은 04 ERD에 테이블 2개를 더하고, E-68은 02 §범위의 "앱 외부 푸시 = 범위 외"를 뒤집습니다.
> E-70은 이미 나 있던 구멍을 막은 것이라 계약 변경이 아니라 계약 명시입니다.

| # | 항목 | v2.2 | **v2.3 확정** |
|---|---|---|---|
| E-67 | 대화 맥락 소유 | 04에 채팅 이력 테이블이 없어 `recent_messages`를 누가 들고 있는지 미정 | **대화 종류마다 다르다 — `chat_messages` 테이블 신설.** 금융 Q&A(`transaction_id` NULL)는 **서버가 소유**한다: 회고와 달리 화면이 상태를 들고 다니지 않아 새로고침에 맥락이 사라진다. 회고 대화는 **클라이언트가 소유**한다 — `POST /retrospects/chat`이 상태 없는 프록시여서 `step`·확정값과 함께 `recentMessages`를 같이 보낸다 (E-63). AI에는 어느 쪽이든 **최근 6건만** 싣는다("전체 이력을 보내지 않는다" 05 §3 그대로) |
| E-68 | 앱 외부 푸시 알림 | **범위 외** (02 §범위 · FR-10 아래 주석) | **범위 내로 전환 — Web Push 채택.** `push_subscriptions` 테이블과 VAPID 키 3종 신설. 알림 생성은 **매일 스케줄이 본류**(`NOTIFICATION_DAILY_CRON`, 기본 09:00 KST)이고 목록 조회 시 생성은 그 사이를 메우는 그물이다 — 앱을 열어야만 알림이 생기면 보낼 푸시가 없다. 키가 비면 **Web Push만 꺼지고 인앱 알림은 그대로** 동작한다 |
| E-69 | 회고 대화 단계 | 05 §3 `state.step` 6종이 문자열로만 존재 | **공유 enum `reflectionStep`으로 승격** — `INTRO` · `SATISFACTION` · `PURPOSE` · `COMPANION` · `REPEAT` · `CONFIRM`. **화면이 소유한다** — 지금 어떤 질문을 띄웠는지는 클라이언트가 안다. `INTRO`는 사용자 입력이 없는 턴이다 |
| E-70 | 내부 AI API 성공 응답 | 봉투 `{ success, data }`만 규정 | **`data`는 항상 JSON object다.** 돌려줄 것이 없어도 `null`이 아니라 빈 객체를 싣는다. AI `SpringClient._request`가 object가 아닌 `data`를 예외로 처리하므로, `data` 없는 200은 AI에서 500이 되고 Spring은 그것을 503 `LLM_UNAVAILABLE`로 보여준다 — **LLM은 멀쩡한데 저장만 실패하는** 증상이 된다 |

> ~~⚠️ **E-68은 02와 충돌합니다.** 02 §범위의 "푸시 알림(앱 외부)"과 FR-10 아래 "앱 외부 푸시 알림은 범위 외입니다"를 같은 라운드에 고쳐야 합니다.~~
> **(9/8) 해소했습니다** — 02 v2.10에서 두 문장을 고치고, **v2.11에서 `FR-10-04`(Web Push 발송, P1)를 신설**했습니다. **FR 76 → 77건**(NFR 9 그대로)이고 `요구사항 정의서.xlsx`도 다시 만들었습니다. FR-10 절은 "알림 (인앱 · Web Push)"이고 담당은 석정한(구독·화면) · 고현석(서버 발송)입니다.
> **E-67·E-68은 04에 테이블 2개**(`chat_messages` · `push_subscriptions`)를, **E-68은 07 §3에 환경 변수 4종**(`VAPID_PUBLIC_KEY` · `VAPID_PRIVATE_KEY` · `VAPID_SUBJECT` · `NOTIFICATION_DAILY_CRON`)을 더해야 합니다.
> **(9/8) 07 §3 · 04 테이블 2개 모두 반영했습니다** — 07 v2.10 · 04 v2.8. 02 충돌도 해소했습니다 — 02 v2.10. `NOTIFICATION_DAILY_CRON`의 기본값은 E-71을 따라 `0 0,30 * * * *`이고 **30분 격자 전용**입니다. `chat_messages`는 server `V6`, `push_subscriptions`는 `V5`입니다.
> E-69의 `INTRO` 턴은 AI `ChatRequest.message`가 공백을 거부해(레포 `app/schemas/chat.py`) Spring이 대화를 여는 신호를 대신 싣습니다. **그 문장은 사용자의 말이 아니므로 이력에 남기지 않습니다.**

> ⚠️ **판 번호 주의:** 이 라운드는 E-51~E-54로 초안됐으나 CSV 판(v2.0)이 그 번호를 먼저 받아 E-57~E-60이 됐고,
> 회고 슬라이스 판(§0-29 · v2.2)이 다시 그 번호를 먼저 받아 **v2.3 · E-67~E-70**으로 옮겼습니다 (9/7 밤).

---

## 0-32. v2.2 변경 요약 (2026-09-07 — 회고 슬라이스 · 규칙 파라미터 잠정값)

> v2.2는 **회고를 서버에 실제로 붙이기 위해 비어 있던 자리를 채운 판**입니다. 데이터 확보 후로 미뤄 둔 규칙 파라미터(06 #15~#18 · #20)에 **잠정값**을 넣고, 문서에 없던 롤업 상위 키 · `analysisYearMonth` 산출 규칙 · `POST /retrospects/chat` 본문을 정했습니다. 잠정값은 `application.yml` 기본값이므로 데이터 분포를 본 뒤 **값만 바꾸면** 됩니다 (07 §7). 후보 `reason` 문장의 생성 주체가 바뀌므로 05 §2 문구가 **계약 변경**입니다 (07 §6 절차 — 05를 같이 고쳤습니다).
> 회고 슬라이스의 레포 조치는 **R16**(06)입니다. server `V5` 마이그레이션은 없습니다 — `retrospects` · `behavior_clusters`는 V1에 이미 있습니다.

| # | 항목 | v2.1 | **v2.2 확정** |
|---|---|---|---|
| E-57 | 규칙 파라미터 잠정값 (#15~#18 · #20) | 전부 미결 — `rules.*`가 비어 있어 규칙 엔진이 계산을 거부 | **9/7 잠정값을 `application.yml` 기본값으로 주입한다.** `shrinkage-k=3` · `rollup-min-count=3` · `pending-min-count=3` · `axis-x-boundary=0.1`(월 합계가 예산의 10%) · `axis-y-boundary=0`(중립) · `chat-window-days=3`(E-48 그대로) · `sensitivity.{conservative=3.0, standard=2.0, sensitive=1.5}`(중앙값 배수, E-46 프리셋) · `candidate.outlier-baseline-days=90` · `candidate.outlier-min-samples=5` · `candidate.big-amount-budget-ratio=0.1` · `candidate.repeated-low-min-count=2` · `cluster.meal-categories=식사,식비,음식점,외식,배달,한식,중식,일식,양식,분식,패스트푸드,카페`. 환경변수 `RULES_*`로 덮어쓴다. **잠정이며 데이터 확보 후 값만 바꾼다** — 코드 상수 금지는 그대로(E-18) |
| E-58 | 묶음 키의 시간대 자리 (B-10 보강) | 시간대는 식사 카테고리에만 | **`rules.cluster.meal-categories`에 든 카테고리이거나 `기타`(미분류)이면 시간대를 포함한다.** 받은 CSV 3종이 모두 카테고리 미기재라 지금 `category`는 전부 `기타`(06 #11)다. 이 규칙이 없으면 키가 `기타\|\|목적\|동행인`으로 퇴화해 심야 배달과 낮 카페가 한 묶음이 된다. 카테고리가 정보를 못 주는 동안 시간대라도 묶음을 가른다. 매핑표(#11)가 오면 재검토하고, 키가 바뀌므로 그때 전체 재계산을 1회 돌린다 |
| E-59 | 롤업 상위 키 · `parentId` · 응답 대상 | 롤업(③)은 "회고 수가 기준 미만이면 상위로"만 있고 상위 키 정의 없음 | **상위 키 = `카테고리\|시간대\|\|`(목적·동행인 비움).** 리프 묶음의 `retrospectCount < rollup-min-count`이면 `parentId`를 상위 묶음에 건다. 상위 묶음의 집계는 **자식 전체 회고의 합집합**. `parentId`는 저장할 때마다 재계산하므로 건수가 차면 자동으로 분리된다 — FR-05-04(P2)는 별도 구현이 없다. `transactions.behavior_id`와 `POST /retrospects` 응답은 항상 **리프** 묶음이다 |
| E-60 | `analysisYearMonth` 산출 (06 R11 종결) | 04 `User`에 필드 없음 · 산출 규칙 미정 — `GET /users/me`는 `null` | **사용자의 최근 거래월**(`max(Transaction.occurredAt)`의 Asia/Seoul 년월). 사용자의 모든 묶음이 같은 달을 쓴다(지도 가로축 정합). `GET /users/me`도 같은 값을 돌려준다. 사용자 설정은 두지 않는다. 그 달 거래가 없는 묶음은 `monthlyTotalAmount=0` · `txCount=0` · `avgAmount=null` |
| E-61 | 재계산 범위 · null 처리 | 04 §3 산식에 분모·null 정의 없음 | **회고 저장 시 사용자 전체 묶음과 `User.avgSatisfaction` 캐시를 재계산한다.** `avgSatisfaction`이 바뀌면 모든 묶음의 보정 만족도가 바뀌고 상위 묶음은 자식의 합집합이라 부분 재계산이 더 어렵다. `rawAverage`는 `UNKNOWN` 제외 표본이 0이면 `null`. 축소 추정의 n은 **`UNKNOWN` 제외 건수**(rawAverage와 같은 분모) → 전부 `UNKNOWN`이면 `adjusted = avgSatisfaction`. `evaluationStatus`의 건수는 `UNKNOWN` 포함 `retrospectCount`. 첫 회고(사용자 평균 없음)는 0(척도 중립값). `monthlyBudget`이 없거나 0이면 `burdenRatio=null` · `quadrant=null`이고 `verdict`는 세로축만으로 계산한다(V1 CHECK 허용). 이번 재계산 결과에 없는 기존 묶음(자식이 롤업을 벗어난 상위 묶음 등)은 **지우지 않고 값만 비운다**(회고 수 0 · PENDING · null) — 행을 참조하는 곳이 있고, 옛 값이 남으면 지도에 유령 점이 생긴다 (9/7 실측) |
| E-62 | 후보 선별 세부 (⓪ · #20 잠정) | B-8 "시간대별 이상치 + 사용자 임계값"까지만 | ① D+1: KST 날짜 ≤ 오늘 − `retrospectDelayDays`. ② 이미 회고된 거래 제외. ③ `THRESHOLD_EXCEEDED`: `amount ≥ monthlyBudget × big-amount-budget-ratio`(예산 없으면 미적용). ④ `TIMESLOT_OUTLIER`: 같은 (카테고리, 시간대) 최근 `outlier-baseline-days` 거래의 **중앙값 × 배수**(`User.outlierThreshold`, 없으면 `sensitivity.standard`) 초과. 표본 < `outlier-min-samples`면 시간대를 버리고 카테고리 전체로, 그래도 부족하면 미적용. ⑤ `REPEATED_LOW_SATISFACTION`: 같은 상위 키(`카테고리\|시간대`) 아래 `LOW` 회고 ≥ `repeated-low-min-count`. 우선순위 ③ > ④ > ⑤. 정렬 최신순, `limit` 기본 1 · 최대 100. **`reason` 문장은 Spring 템플릿**(reasonCode당 1문장, AI `fallback.py`와 같은 문장) — 후보 N건마다 LLM 왕복을 만들지 않는다. AI 재구성 문장은 `POST /retrospects/chat`의 `INTRO`에서 받는다(FR-04-10·11). **05 §2 문구 변경 → `[계약변경]` 공지** |
| E-63 | `POST /retrospects/chat` 계약 (P1 자연어 경로) | 05 §1 표에만 있고 본문 없음 | **상태 없는 프록시.** 요청 `{ transactionId, message?, step(기본 INTRO), reflection{satisfaction, purpose, companion, repeatIntent}, recentMessages[] ≤ 6 }`. 서버가 거래 소유를 검사하고 ⓪ 규칙으로 `reason_code`(미매칭이면 `MANUAL_PICK`)를 구해 `task_context(REFLECTION · ACTIVE · state)`를 만들어 AI `/chat`에 위임한다. 응답 `{ reply, step(다음 단계 = 응답 reflection에서 아직 미확정인 첫 항목, 전부 확정이면 CONFIRM — 폴백이라 추출이 없어도 남은 항목을 계속 묻는다), reflection(AI 후보 — 표준 태그 밖이면 `null`, E-20 · data에 키가 없는 항목은 요청 확정값 유지), needsClarification, uncertainFields[](AI 목록 + 서버가 버린 항목), fallback }`. **회고 행을 만들지 않는다** — 저장은 항상 `POST /retrospects`. `status`는 항상 `ACTIVE`(PAUSED 재개는 P2). 남의 거래 404 · 이미 회고한 거래 409 · AI 미응답 503 `LLM_UNAVAILABLE` |
| E-64 | 묶음 이름(⑤) 붙이는 시점 | "이름 없는 새 묶음은 `CLUSTER_NAMING`으로" | 저장 트랜잭션 **커밋 후** `displayName`이 없는 묶음만 `CLUSTER_NAMING`을 부른다(보통 1~2건). 503 · 빈 응답이면 템플릿 `"{시간대 라벨} {카테고리}"`(시간대 없으면 `"{카테고리} {목적}"`, 12자). 이름은 한 번 정하면 재계산하지 않는다. `POST /retrospects` 지연에 AI 타임아웃(15초)이 더해질 수 있음을 허용한다 — DB 트랜잭션은 AI 호출 동안 열어 두지 않는다 |
| E-65 | `skip`(05 #9) | E-49 상태 미저장. 엔드포인트는 표에 남아 있음 | E-49 그대로. **회고 슬라이스에 엔드포인트를 두지 않는다** — 효과가 "알림 읽음"뿐인데 no-op 엔드포인트가 있으면 클라이언트가 저장된다고 오해한다. 구현은 인앱 알림 조치(`POST /notifications/{id}/read`, 05 #20)에서 |
| E-66 | 내부 `save_reflection` | "외부 `POST /retrospects`와 같은 검증·응답" | `source`가 없으면 `CANDIDATE`. 응답은 외부와 같은 객체(`data`가 object여야 AI `SpringClient`가 봉투를 벗긴다). 05 §3의 "`@JsonNaming(SnakeCaseStrategy)`" 문구는 **필드 `@JsonProperty`**(E-24와 같은 방식)로 정정 |

> E-57의 값은 **근거 있는 확정값이 아닙니다.** 데모가 돌게 하는 최소값이고, 06 §데이터 확보 후 #15~#18 · #20은 "잠정값 주입 · 튜닝 대기"로 남습니다.
> E-62의 `reason` 템플릿화로 05 §2 `GET /retrospects/candidates`의 "AI 재구성" 문구가 바뀝니다. 응답 모양은 그대로입니다.

---

## 0-33. v2.1 변경 요약 (2026-09-07 — 카카오 로그인 흐름 · 소셜 계정 식별)

> v2.1은 **카카오 로그인을 어떤 흐름으로 붙이는가**를 정한 판입니다. E-15(카카오 1종 + 데모 계정 폴백)의 범위는 그대로이고, 흐름과 계정 식별 방식만 확정했습니다. `POST /auth/login` 본문이 바뀌므로 **계약 변경**입니다 (07 §6 절차 — 05를 같이 고쳤습니다).
> 별도 회원가입 API는 없습니다. 카카오 첫 로그인에서 서버가 사용자를 만듭니다.

| # | 항목 | v1.9 | **v2.1 확정** |
|---|---|---|---|
| E-55 | 카카오 로그인 흐름 | 07 E-15 표기 "Spring Security OAuth2 Client" — 서버 리다이렉트(`oauth2Login`)를 암묵 가정 | **클라이언트가 인가 코드를 받고, 서버가 카카오와 교환한다.** ① 클라이언트가 `kauth.kakao.com/oauth/authorize`로 이동 (`state` 난수는 클라이언트가 만들고 검증) ② 카카오가 **클라이언트의** `/auth/callback`으로 `code`를 돌려줌 ③ 클라이언트가 `POST /auth/login { provider: KAKAO, code, redirectUri }` ④ 서버가 `kauth.kakao.com/oauth/token`에서 토큰을 교환하고 `kapi.kakao.com/v2/user/me`로 프로필을 조회한 뒤 **우리 JWT만** 발급. Spring `oauth2Login`은 쓰지 않는다 — 세션이 `STATELESS`(05 §0)이고 클라이언트(Vercel)와 서버(EC2)가 다른 도메인이라 세션 기반 인가 요청 저장소가 깨진다. 카카오 액세스 토큰은 프로필 조회에만 쓰고 저장하지 않는다. 오류 코드 `OAUTH_CODE_INVALID`(400) · `OAUTH_PROVIDER_ERROR`(502) 신설 — 05 §0 |
| E-56 | 소셜 계정 식별 · 이메일 · 표시 이름 | `users.email NOT NULL UNIQUE` — 이메일이 항상 있다고 가정. 표시 이름 없음 | **`email`은 nullable. 계정 식별은 `(authProvider, providerUserId)` 유일 제약. `nickname` 신설.** 카카오 이메일은 선택 동의 항목이라 비어서 올 수 있다. 같은 이메일이라는 이유로 계정을 합치지도 막지도 않는다 — V1의 `uq_users_email` 유일 제약은 V4에서 **삭제** (9/7 추가 결정). 데모 계정(`LOCAL`)은 `providerUserId`가 없으므로 유일 제약은 `providerUserId IS NOT NULL`인 행에만 건다 — **V1 `uq_users_provider`에 이미 있다** (9/7 확인, V4는 만들지 않는다). `nickname`은 카카오 `properties.nickname`(기본 동의 항목)을 저장하고 마이페이지 `1. 프로필`(03 S13)에 표시한다 — 이메일은 선택 동의라 프로필 표시를 기댈 수 없다. nullable이며 비면 클라이언트가 `사용자`로 대체. 데모 계정은 V4에서 `데모 사용자`로 채운다. 스키마는 server `V4` 마이그레이션 — 04 User |

> E-55로 `KAKAO_REDIRECT_URI`(서버 콜백 1개)는 **`KAKAO_REDIRECT_URIS`(허용할 클라이언트 콜백 목록)** 로 바뀝니다 — 07 §3. 서버는 클라이언트가 보낸 `redirectUri`를 이 목록으로 검사하고, 카카오도 콘솔에 등록된 URI와 대조합니다. `KAKAO_CLIENT_SECRET`은 서버에만 있습니다.
> 카카오 콘솔 준비(앱 생성 · Client Secret 사용 · 동의항목 **닉네임(필수) · 이메일(선택)** · 리다이렉트 URI ~~3개~~ **2개**)는 06 §9/2 항목 3의 후속입니다. 닉네임을 나중에 추가하면 이미 로그인한 계정의 재동의가 필요하므로 처음부터 받습니다.
> **9/8 정정:** 리다이렉트 URI는 **클라이언트 콜백 2개**입니다 — 로컬 `http://localhost:5173/auth/callback`과 Vercel 운영 도메인의 `/auth/callback`. "3개"는 서버 콜백(EC2 · `localhost:8080`)을 함께 세던 E-55 이전 표기입니다 (07 §10 리스크 3 · 06 §9/2 항목 3).
> server `build.gradle.kts`의 `spring-boot-starter-security-oauth2-client`는 E-55로 쓰이지 않으므로 V3 반영 때 제거합니다.

> ⚠️ **판 번호 주의:** v2.0(CSV · PR #8)은 v2.1보다 **뒤에** 병합됐지만 먼저 번호를 받았습니다. 카카오 결정은 E-51·E-52로 초안됐다가 충돌로 **E-55·E-56**, 레포 조치는 R14 → **R15**, 마이그레이션은 V3 → **V4**로 옮겼습니다 (9/7 밤).

---

## 0-34. v2.0 변경 요약 (2026-09-07 — CSV 파싱을 카드사 무관 규칙으로)

> v2.0은 **거래내역 CSV를 카드사별로 다루지 않기로 한 판**입니다. 실제 파일 3종(카드 이용내역서 2 · 통장 거래내역 1)을
> 파서에 통과시켜 보고 정했습니다. `cardIssuer`는 세 레포의 공유 enum이므로 **계약 변경**입니다 (07 §6 절차 — 05를 먼저 고쳤습니다).

| # | 항목 | v1.9 | **v2.0 확정** |
|---|---|---|---|
| E-51 | CSV 컬럼 매핑 | 카드사별 매핑표를 채운다 (04 §4, 담당 석정한 · 기한 9/2) | **매핑표 폐기.** 카드사가 아니라 **머리글 키워드**로 컬럼을 찾고, `출금액` 계열 컬럼 유무로 카드 내역서/통장을 가른다. 같은 은행이라도 카드와 통장의 머리글이 전혀 다르고, 사용자가 고른 카드사는 파일과 어긋날 수 있다 |
| E-52 | `cardIssuer` | `Transaction`의 필수 필드 · 업로드 API의 필수 파라미터 | **삭제.** 어디에서도 읽지 않는 값이었다. server `V3` 마이그레이션으로 `transactions.card_issuer` 컬럼을 떨어뜨린다 |
| E-53 | 통장 거래내역의 범위 | 출금은 모두 소비 | **`적요` 화이트리스트 — 카드 결제만 적재.** `체크카드`·`신용카드`가 든 행만 남기고 오픈뱅킹출금·전자금융·FBS출금·`현금IC`(ATM 인출)는 건너뛴다. 통장의 상대방 이름은 가맹점처럼 생겨서, 거르지 않으면 송금이 가맹점 소비로 남는다. 블랙리스트는 새 수단 이름 하나에 뚫린다 |
| E-54 | 건너뛴 행 보고 | 읽지 못한 행은 모두 사유와 함께 보고 | **합계·요약·여백 행은 조용히 무시한다.** 사용자가 보는 "건너뛴 N건"은 진짜 거래만 세야 뜻이 있다 |

> **E-52는 브레이킹 체인지입니다.** `cardIssuer`를 쓰는 코드가 세 레포에 있습니다 — 05 §0 공유 enum · 07 §6 목록에서 삭제했습니다.
> 실측 결과: 카드 이용내역서 A는 0건 → **110건**(날짜·시각 분리 칸 결합, 머리글의 따옴표 안 줄바꿈), 카드 이용내역서 B는 175건 유지,
> 통장은 485건 → **361건**(비카드 190건 제외 · 출금 0인 카드 환불 3건은 입금 행으로 별도 보고).
> 가맹점명 정규화와 가맹점명 기반 카테고리 추정은 여기서 정하지 않습니다 — 06 #11 통합 매핑표 뒤로 미룹니다.

---

## 0-35. v1.9 변경 요약 (2026-09-03 — 회고 진입 경로 · 시간대 4구간)

> v1.9는 **회고를 어디서 몇 건 시작할 수 있는가**를 경로별로 나눈 판이고, 그 과정에서 **시간대 구간이 3개에서 4개로** 바뀌었습니다. 시간대는 세 레포의 공유 enum이므로 **계약 변경**입니다 (07 §6 절차 — 05를 먼저 고쳤습니다).
> 선별 규칙의 수치(이상치 기준선 기간 · 큰 금액 기준 비율 · 표본 최소 건수)는 여기서 정하지 않습니다. 근거 없는 임의값을 늘리지 않도록 06 §데이터 확보 후 **#20**으로 넘겼습니다.

| # | 항목 | v1.8 | **v1.9 확정** |
|---|---|---|---|
| E-48 | 회고 진입 경로와 노출 범위 | "1회 1건"(B-7) 하나로 뭉뚱그림 | **경로 3종으로 분리.** ① 인앱 알림 = **1일 1건** (FR-10-01) ② AI 채팅 회고 진입 = **오늘 포함 최근 3일**의 후보 전부 ③ 사용자가 채팅에서 날짜·기간을 지정하거나 거래내역에서 직접 고르면 **범위 제한 없음**. 회고 자격에 **상한(오래된 거래 제외)은 두지 않는다** |
| E-49 | 후보 제외(`skip`) | 제외 상태를 저장한다고 암묵 가정 | **상태를 저장하지 않는다.** 알림 읽음 처리로 갈음한다. 스킵은 "다시는 묻지 마"가 아니라 "지금은 말고"이고, 사용자는 같은 거래를 채팅·거래내역에서 다시 회고할 수 있어야 한다 — 별도 컬럼은 그 길을 막는다 |
| E-50 | 시간대 구간 | **3개** — `MORNING` 05~12 · `AFTERNOON` 12~22 · `NIGHT` 22~05 | **4개 — `MORNING` 05~11(6h) · `DAY` 11~17(6h) · `EVENING` 17~22(5h) · `NIGHT` 22~05(7h).** `AFTERNOON` 폐기 |

> ⚠️ **E-50은 브레이킹 체인지입니다.** `AFTERNOON`을 쓰는 코드가 세 레포에 있고, server는 `transactions.time_slot`의 `CHECK` 제약과 **이미 저장된 값**까지 바꿔야 합니다 (V2 마이그레이션). 반영은 06 §레포 조치 **R13**.
> 네 구간의 합은 6 + 6 + 5 + 7 = 24시간으로 맞습니다. `NIGHT` 22~05는 **7시간**입니다.
> E-48은 새 제약이 아니라 **제약을 걷어낸 결정**입니다. 선별은 "회고할 자격"이 아니라 "서비스가 먼저 말을 거는 시점과 개수"만 정합니다.

---

## 0-36. v1.8 변경 요약 (2026-09-03 — AI taskType 확장)

> v1.8은 FR-12(금융 지식 Q&A, `/chat/finance`) 처리를 위해 AI `taskType`을 6종으로 확장한 판입니다. 스택·규칙 엔진은 v1.7 그대로입니다.

| # | 항목 | v1.7 | **v1.8 확정** |
|---|---|---|---|
| E-47 | AI `taskType` | 5종(REFLECTION/ANALYSIS/ACTION_PLAN/CLUSTER_NAMING/ANALYSIS_NARRATE) | **6종으로 확장 — `FINANCE_QA` 신설.** `task_context.state`는 거의 빈 객체(`{}`) — 질문은 `message`, 맥락은 `recent_messages`. 출처는 `reply` 문장에 자연스럽게 언급하고 별도 필드로 분리하지 않는다(LLM이 정확한 문서명·연도를 재현하다 오탈자·환각을 낼 위험 방지). 05 §0·§3 반영 |

---

## 0-37. v1.7 변경 요약 (2026-09-03 — 와이어프레임 v1 검수 · UI 자산 확정)

> v1.7은 **와이어프레임 v1(피그마 `Z9isZcGBA6XWAUcmeOJzJp` 페이지 `19-2` · 21화면)** 을 이 문서·02·03과 대조한 결과와, 그 과정에서 확정한 UI 자산 2건을 기록한 판입니다. 스택·API·규칙 엔진은 v1.6 그대로입니다.
> 화면별 수정 지시는 **03 §5-2 (W-1~W-17 · D-1~D-7)** 에 있습니다. 피그마가 이 문서와 다르면 피그마를 고칩니다.

| # | 항목 | v1.6 | **v1.7 확정** |
|---|---|---|---|
| E-43 | 아이콘 세트 | 미명시 (와이어프레임은 Lucide + 이모지 혼용) | **Tabler Icons 단일 세트.** 클라이언트는 `@tabler/icons-vue` 3.46(설치 완료)만 쓴다. 이모지·Lucide·직접 그린 SVG를 아이콘 자리에 쓰지 않는다. 치환표는 03 §5-3 |
| E-44 | 서체 | 미명시 (와이어프레임은 Inter) | **Pretendard.** 피그마 텍스트 스타일과 클라이언트 전역 서체를 모두 Pretendard로 통일한다. 숫자·영문에 별도 서체를 두지 않는다. 적용 방법은 03 §5-3 |
| E-45 | 온보딩 단계 수 | 5단계 | **4단계 — 목표 자금 → 월 예산 + 분석 민감도 → 거래내역 업로드 → 표본 회고.** 예산과 임계값을 한 화면에 묶는다 (Q-1 채택, 9/3). `onboardingCompleted`는 4단계 완료 시 |
| E-46 | 이상치 임계값 입력 | 숫자 임계값 직접 입력 (02 FR-01-04) | **프리셋 3종 — 보수적 · 기본 · 민감하게.** 사용자는 프리셋만 고르고, 프리셋 → 숫자 매핑은 Spring `rules.sensitivity.{conservative,standard,sensitive}` 파라미터 세트로 둔다. 값은 06 §데이터 확보 후 #15~18과 함께 9/7 확정 (Q-2 채택, 9/3) |

> 검수에서 드러난 설계 위반 10건은 **새 결정이 아니라 기존 결정의 미반영**입니다 — 점 색상 2색(E-4·E-11) · 처방 문구 상세 패널 전용(E-4) · 사분면 비노출(§4) · 카카오 1종 로그인(E-15) · 목적 7종(§3) · 월 예산 필수(02 FR-01-03) · 전월 대비(02 FR-08-06) · 업로드 형식 CSV/XLSX(02 FR-02-01) · 탭명 `지도`(§9) · P0 화면 3장 누락(03 S5·S10·S11). 값은 바뀌지 않았고 피그마가 따라옵니다.
> Q-1·Q-2는 9/3 팀장 결정으로 **E-45·E-46**에 종결했습니다. §9 온보딩 행도 갱신했습니다.

---

## 0-38. v1.6 변경 요약 (2026-09-02 밤 — AI 레포 조치 R1~R6 · R10 완료)

> v1.6은 **`sottaejap-ai` 레포가 06 §레포 조치 R1~R6·R10을 구현한 결과**를 기록한 판입니다. 스택·의존성은 바꾸지 않았습니다. 다른 항목은 v1.5 그대로입니다.
> 세 레포 모두 스캐폴딩이 끝났습니다 — `sottaejap-ai` `f9d3c78` · `sottaejap-server` `f8e3067` · `sottaejap-client` `580f9ad`. AI 레포의 v1.6 변경은 `66d1e62`로 `main`에 푸시했습니다. server `AGENTS.md`도 `e7bc91e`로 맞췄습니다.

| # | 항목 | v1.5 | **v1.6 확정** |
|---|---|---|---|
| E-37 | AI `/chat` 인바운드 인증 | "두 방향 모두 헤더" (AI 쪽 거부 규칙 미명시) | **`X-Internal-Secret`이 없거나 다르면 401. `INTERNAL_SHARED_SECRET`이 비어 있으면 모든 `/chat`을 401로 거부** — Spring v1.5 규칙과 대칭. `/health`만 열림 |
| E-38 | LLM 미설정 시 동작 | 미정 | **`OPENAI_API_KEY`가 비어 있어도 `/chat`은 템플릿 응답 + `fallback: true` + 200.** P0 회고 경로가 LLM 없이 완주하고, 키 없이도 `ai-ping`이 200 — **9/2 밤 실측 확인** |
| E-39 | `SpringClient` 응답 처리 | "dict로 받으므로 변환하지 않는다" | **봉투 `{ success, data }`를 벗겨 `data`만 Tool에 넘김.** `success: false`는 `SpringApiError(code)`. 키는 camelCase 그대로 |
| E-40 | `requirements.lock` 생성 방식 | `pip freeze` | **pip 교차 해석 `--python-version 3.12`** — 로컬 파이썬이 3.14여도 3.12 기준으로 고정. Docker 불필요 (07 §11) |
| E-41 | 표준 태그 enum 값 | (미명시) | **한국어 문자열 그대로** (`"충동"` · `"혼자"`). `/chat`의 `tool_results[].data`와 Spring 저장값이 같은 표기. **v2.20 부기:** 저장값 · 응답값이 이 한 표기라는 것은 그대로다. 받는 표기만 넓어졌다 — 공백만 다른 값은 정본으로 되돌려 받는다 (E-99) |
| E-42 | 레포명 | E-32 (rename 예정) | **GitHub `jittaejap/sottaejap-ai` rename 완료 (9/2 밤).** 구 URL은 GitHub가 리다이렉트. `/health`의 `service` = `sottaejap-ai-server` |

> 레포 조치 완료 현황 — **R1~R6 · R10 (ai) ✅ · R7~R9 (server) ✅** (server 스캐폴딩 `f8e3067`에 포함). 남은 것은 **R11(docs)** 뿐입니다 (06 §레포 조치).
> **9/2 성공 기준 `ai-ping` 200 — 실측 통과** (Spring `f8e3067` ↔ AI v1.6, 로컬 · OpenAI 키 없음). 06 §8 · 07 §4.
> 폴백 템플릿 문구(`app/ai/fallback.py`)는 초안입니다. 담당 오진호가 다듬습니다.

---

## 0-39. v1.5 변경 요약 (2026-09-02 저녁 — 서버 스택 재확인)

> v1.5는 **서버 스캐폴딩 직전에 Maven Central · start.spring.io · Adoptium · Docker Hub에서 스택을 재조회**한 결과를 기록한 판입니다. 다른 항목은 v1.4 그대로입니다.

| # | 항목 | v1.4 | **v1.5 확정** |
|---|---|---|---|
| E-34 | 서버 Java | Java 25 | **Java 21 (LTS, Temurin 21.0.12.1)** — 팀 결정. Boot 4.1은 17~26 지원. 팀원 로컬에 21이 설치되어 있고 25로 올릴 이유가 없음. 07 §1·§5-4 |
| E-35 | springdoc-openapi | 3.0.3 | **3.1.0** — 3.0.3은 Boot **4.0.5** 기준 빌드, 3.1.0이 Boot **4.1.0** 기준 빌드. Boot 4.1과 짝이 맞는 것은 3.1.0 |
| E-36 | Spring Boot · Gradle 고정값 | 4.1.x · wrapper 9.3.1 | **Boot 4.1.1** (4.2.0-M1은 마일스톤이라 제외) · **Gradle wrapper 9.7.1** |

> Boot 4.1.1이 관리하는 버전(그대로 따름): Spring Framework 7.0.9 · Security 7.1.1 · Data JPA 4.1.1 · Hibernate 7.4.5 · Flyway 12.4.0 · **Jackson 3.1.5 (`tools.jackson` 패키지)** · Tomcat 11.0.24 · JUnit 6.0.3 · PostgreSQL JDBC 42.7.13. Boot가 관리하지 않아 직접 고정: jjwt 0.13.0 · springdoc 3.1.0.

---

## 0-40. v1.4 변경 요약 (2026-09-02 오후 — 레포명 · 클라이언트 스택)

> v1.4는 **클라이언트 스캐폴딩 시점의 팀 결정 2건**을 기록한 판입니다. 다른 항목은 v1.3 그대로입니다.

| # | 항목 | v1.3 | **v1.4 확정** |
|---|---|---|---|
| E-32 | 레포명 표기 | `sottaejab-*` (E-28) | **`sottaejap-*`** — 팀 결정. GitHub `jittaejap/sottaejab-ai`는 **`sottaejap-ai`로 rename** (오진호 · 06 R10). Java 패키지 `kr.sottaejap.server`, DB명 `sottaejap`도 같이 통일 |
| E-33 | 클라이언트 언어·스타일 | 미확정 (07 트리는 `.js`) | **TypeScript `~6.0.3`** (7.x 금지 — `vue-tsc`·`typescript-eslint` 미지원) · **Tailwind 4** + `tokens.css` 토큰 · 07 §2 **평면 구조 유지**, 의존 방향 `views → components·composables·stores → api` |

> E-32는 E-28을 대체합니다. 문서 안의 `sottaejab-*` 표기는 v1.4에서 일괄 정정했습니다. AI 레포의 `/health` 문자열도 v1.6에서 `sottaejap-ai-server`로 정정했습니다 (E-42).

---

## 0-41. v1.3 변경 요약 (2026-09-02 정합성 감사 3차 — 레포 우선)

> v1.3은 **문서 v1.2 ↔ `sottaejab-ai` 레포(`f9d3c78`, 9/2 13:24)** 대조에서 드러난 불일치를 **레포 기준으로** 종결한 판입니다.
> 팀 결정: **코드가 앞서 간 곳은 문서가 따라간다.** 단, 설계 원칙이 걸린 4건(E-18 담당 · E-20 · E-22 · E-23)은 팀장이 직접 결정했습니다.

| # | 항목 | v1.2 | **v1.3 확정** |
|---|---|---|---|
| E-18 | 규칙 엔진 위치 | FastAPI(정민규) | **Spring(Java) — 정민규 구현 · 고현석 통합·리뷰.** FastAPI는 Agent·자연어·Tool·RAG·설명만 |
| E-19 | Spring ↔ FastAPI 호출 방향 | Spring → `/internal/*` 7종 (push) | **Spring → `POST /chat` 1종 + FastAPI → Spring 내부 AI API 6종 (pull)** — 05 §3 |
| E-20 | 목적·동행인 정규화 | 임베딩 코사인 최근접 (E-16) | **AI가 표준 태그 후보를 제안하고 사용자가 확인한 값만 저장.** 자유 문자열 저장 금지. E-16 폐기 |
| E-21 | pgvector | 드롭 (E-16) | **복원 — 금융 RAG 저장소 (P2 착수 시 설치).** A-2 = PostgreSQL + pgvector |
| E-22 | 금융 지식 RAG | 범위 외 | **범위 내 P2 — FR-12 신설.** NFR-05 규제 경계 유지 |
| E-23 | 만족도 enum 문자열 | `SATISFIED/UNSATISFIED/UNSURE` | **`HIGH/LOW/UNKNOWN`** — 3택 유지, 레포의 `MEDIUM`은 **삭제** |
| E-24 | AI 경계 명명 규칙 | camelCase · retrospect · `IN_PROGRESS` | **`/chat` 경계는 snake_case · reflection · `ACTIVE`.** Spring 외부 API는 camelCase 유지, 변환은 Spring `AiClient` 한 곳 |
| E-25 | LLM 제공자 | 미정 | **OpenAI `gpt-4o-mini`** (기획서와 동일). 임베딩 모델은 RAG(P2) 착수 시 확정 |
| E-26 | AI 레포 스택 | Python 3.13 · uv · pyproject | **Python 3.12+ · requirements.txt · venv** |
| E-27 | 브랜치 | `main/develop/feat/*` | **`main/feature/*`** — `develop` 없음 |
| E-28 | 레포명 표기 | `sottaejab-*` | **`sottaejab-*`** (GitHub 실제명 `jittaejap/sottaejab-ai`) |
| E-29 | NFR-01 증명 테스트 | `tests/test_rules_determinism.py` (Python) | **Spring `RuleEngineDeterminismTest` (JUnit)** |
| E-30 | "규칙 엔진 명시" README | AI 레포 첫 줄 | **Spring 레포 README 첫 줄** — "규칙 엔진(결정론적) 소유". AI 레포 첫 줄은 현행 유지 |
| E-31 | 절감액·목표 배분 계산 (⑦) | 오진호 (FastAPI) | **정민규 (Spring 규칙 엔진 일부)** — 오진호는 AI 레포 전담 |

> **양보하지 않은 것 (레포와 무관하게 유지):** FR-04-15 LLM 폴백 P0 · NFR-02(`reasonCode`는 Spring 산출, AI는 재구성만) · E-11·E-12·E-5 판정 층위와 `Out` 금지 · NFR-05 규제 경계 · E-17 OS 혼용 규칙.
> 이 항목들은 레포가 **추가로 구현**해야 합니다 — `06_액션시트.md` §레포 조치 R1~R9. **→ v1.6에서 R1~R10 완료.**

---

## 0-42. v1.2 변경 요약 (2026-09-01 정합성 감사 2차)

> v1.2는 **문서 ↔ 구글 시트 ↔ 피그마 3원 대조**에서 드러난 불일치를 종결한 판입니다.

| # | 항목 | v1.1 | **v1.2 확정** |
|---|---|---|---|
| E-1 | 만족도 지도 가로축 분자 | "묶음 금액"(01) / "묶음 평균 금액"(03) / `avgAmount`(04) — 3중 불일치 | **분석 기준월 1개월 묶음 합계 금액 ÷ 월 예산** |
| E-2 | `verdict` enum | `KEEP`/`CHANGE`/`PENDING` — `quadrant.KEEP`과 문자열 충돌 | **`SUSTAIN`/`ADJUST`** (E-11과 함께 2종으로) |
| E-3 | 처방 문구 종수 | §4 안에서 "5종"·"4종" 자체 모순 | **5종 (PENDING 포함)** |
| E-4 | 판정 표시 층위 | 피그마에서 판정과 5문구가 1:1 오결합 | **판정 2색 + 보류 상태 / 처방 5문구 — 노드 분리** (E-11 참조) |
| E-5 | `Out` 표기 | 시트 FR-07-04·피그마 2-4에 잔존 | **전면 삭제 → `PENDING`(더 알아볼게요)** |
| E-6 | "ai 둘러대기" 표기 | 시트 FR-03-05 | **삭제** — `reasonCode` 재구성으로 통일 (NFR-02) |
| E-7 | IA 번호 체계 | 문서 `3-1-x` / 피그마 `4-x` 충돌 | **탭 기준 재정비** — §9 |
| E-8 | 소비 분석 화면 | 피그마에만 존재, FR·API 없음 | **FR-11 신설 · P1 · 화면 S15 · `GET /analysis`** |
| E-9 | PWA · SNS 로그인 | 피그마에만 존재 | **범위 내 확정** — FR-01-01 확장 / NFR-08 신설 |
| E-10 | 심야 배달 단가 | 12,000원 vs 23,000원 미해소 | **12,000원(데모 데이터 기준)으로 통일** |
| E-11 | 판정 라벨 구조 | 3라벨 안에 **판정 2종 + 상태 1종**이 혼재 | **판정 `verdict` 2종 / 보류는 `evaluationStatus`로 분리** |
| E-12 | `PROTECT` 비대칭 | Ⅰ·Ⅱ 배지 동일한데 행동은 Ⅰ에만 존재 | **배지 동일 유지 / 상세 패널에만 '예산 확보' CTA** |
| E-13 | 프론트엔드 스택 | 미확정 | **Vue 3 + Vite + Pinia** (PWA — `vite-plugin-pwa`) |
| E-14 | 레포 구성 | 미확정 | **3개 레포 분리** — `sottaejap-client` / `sottaejap-server` / `sottaejap-ai` (표기는 E-28 → **E-32**로 `sottaejap-*` 확정) |
| E-15 | SNS 로그인 구현 수준 | 미확정 | **카카오 OAuth 1종 실구현** + 데모 계정 폴백 |
| E-16 | pgvector | A-2에 있으나 FR 70건 중 사용처 0 | ~~**드롭.** 목적·동행인 정규화는 **태그 임베딩 + 코사인 최근접**으로 결정론화~~ → ⚠️ **v1.3 E-20·E-21로 대체** |
| E-17 | 개발 환경 | 미확정 | **macOS + Windows 혼용** — 인코딩·줄바꿈·도구 규칙은 `07` §5 |

---

## 1. 아키텍처

| # | 항목 | 확정 |
|---|---|---|
| A-1 | 백엔드 | **Spring Boot + FastAPI 병행** |
| A-2 | DB | **PostgreSQL 단일 DB + pgvector 확장** (MySQL 제외 · pgvector는 금융 RAG용, P2 착수 시 설치 — **E-21**) |
| A-3 | 사전 개발 | **허용 — 최대한 진행** |
| A-4 | 배포 | Docker Compose · AWS EC2 · Vercel |
| A-5 | 마이그레이션 | **Flyway** |
| A-6 | 규칙 엔진 위치 | **Spring (Java) — 정민규 구현 · 고현석 통합·리뷰** (v1.3 — E-18). FastAPI는 AI 표현 계층만 |
| A-7 | 클라이언트 형태 | **PWA (웹 우선, 설치형 진입 지원)** — v1.2 확정, NFR-08 |
| A-8 | 프론트엔드 | **Vue 3 + Vite + Pinia** (E-13) — 상세는 `07_기술스택_레포구성.md` |
| A-9 | 레포 | **3개 분리** (E-14 · 표기 E-32) — `sottaejap-client`(Vue) / `sottaejap-server`(Spring + 규칙 엔진) / `sottaejap-ai`(FastAPI) |
| A-10 | 런타임 | **Java 21 (LTS)** (v1.5 — E-34) · Spring Boot **4.1.1** (E-36) · **Python 3.12+** (E-26) · **Node 24 (LTS)** · PostgreSQL 18.6 |
| A-12 | 개발 환경 | **macOS + Windows 혼용** (E-17) — `.gitattributes` LF 강제 · UTF-8 강제 · CSV 두 인코딩 테스트 |
| A-11 | 인증 | **카카오 OAuth 1종 실구현** + 데모 계정 폴백 (E-15) · 흐름 = 클라이언트 인가 코드 → 서버 교환 (v2.1 — E-55) · 계정 식별 `(authProvider, providerUserId)` · 이메일 nullable · `nickname` 저장 (E-56) |
| A-13 | AI 레포 역할 | **Single Agent · 자연어 이해 · Tool Calling · 금융 RAG · 설명** — 계산·판정 금지 (v1.3 — E-18) |
| A-14 | LLM 제공자 | **OpenAI `gpt-4o-mini`** (v1.3 — E-25) |

> A-6 부속 결정 (v1.3): 선별 쿼리와 규칙 엔진이 **같은 레포(Spring)** 에 있습니다.
> 회고 **후보 선별(D+1 필터·이상치 검출) 쿼리는 고현석**, 그 외 규칙 엔진(②③④⑦⑧)은 **정민규**가 Java로 구현합니다.

---

## 2. 역할 경계 — AI와 규칙 엔진

가장 중요한 구조적 결정입니다. **기획서의 "같은 데이터에 항상 같은 분석 결과" 주장을 지키는 방어선**입니다.

| 단계 | 담당 | 위치 | 성격 |
|---|---|---|---|
| ⓪ 회고 후보 선별 (D+1·이상치·임계값) | **규칙 엔진** (구현: 선별 쿼리·고현석) | Spring | 결정론적 |
| ① 목적·동행인 자유입력 → 표준 태그 | **AI 후보 제안 + 사용자 확인** (오진호 · 석정한) | FastAPI(후보) · 클라이언트(확인) | 표현 + 확인 (E-20) |
| ② 묶음 키 조합 (카테고리+시간대+목적+동행인) | **규칙 엔진** (정민규) | Spring | 결정론적 |
| ③ 롤업 판단 | **규칙 엔진** (정민규) | Spring | 결정론적 |
| ④ 축소 추정 보정 · 판정 | **규칙 엔진** (정민규) | Spring | 결정론적 |
| ⑤ 묶음 이름 붙이기 | **AI** (오진호) | FastAPI | 표현 |
| ⑥ 회고 대화 · 결과 설명 | **AI** (오진호) | FastAPI | 표현 |
| ⑦ 절감액 · 목표 배분 계산 | **규칙 엔진** (정민규 — 단순 산술, E-31) | Spring | 결정론적 |
| ⑧ 소비 분석 요약 수치 집계 | **규칙 엔진** (정민규) | Spring | 결정론적 |
| ⑨ 소비 분석 문장 재구성 ('나만의 특징') | **AI** (오진호) | FastAPI | 표현 |
| ⑩ 저장 · 조회 · API | **Spring** (고현석) | Spring | — |
| ⑪ 금융 지식 Q&A (P2) | **AI + RAG** (오진호) | FastAPI · pgvector | 표현 (근거 안에서만, E-22) |

> **AI는 입구(① 후보 제안)와 출구(⑤⑥⑨⑪)에만 있습니다. 판단·집계(②③④⑦⑧)는 전부 Spring 규칙 엔진의 결정론입니다.** (v1.3 — E-18·E-20)
> ①에서 AI가 제안한 값은 **사용자가 확인하기 전에는 저장되지 않습니다.**
> 심사위원의 "왜 이렇게 묶였습니까"에는 Spring 규칙 엔진이, "왜 이 태그입니까"에는 "사용자가 확인했습니다"가 답합니다.

### ①의 확인 원칙 (E-20 — v1.3, E-16 폐기)

목적 7종·동행인 6종은 **고정 선택지**입니다. P0 경로(FR-04-04·05)는 사용자가 선택지 버튼을 직접 고르므로 AI가 관여하지 않습니다.
P1 경로(FR-04-07)에서만 AI가 자연어에서 **표준 태그 후보 또는 미확정(`null`)** 을 제안하고, 사용자가 확인합니다.

```
AI 후보 ∈ {표준 태그 7/6종} ∪ {null}   → 사용자에게 보여주고 확인받는다 (레포 needs_clarification · uncertain_fields)
확인된 값만 저장                        → 자유 문자열 저장 금지 (Spring이 400으로 거부)
null                                   → 해당 항목만 되묻는다 (FR-04-08). 추측 저장 금지 (FR-04-09)
```

- 저장값은 **사용자가 확인한 표준 태그**이므로, 그 이후의 묶음·보정·판정은 전부 결정론입니다 — NFR-01의 범위는 "확정 입력 이후"입니다.
- LLM 왕복이 P1 경로에만 생기므로 NFR-04 폴백도 P1 경로와 표현 계층(⑤⑥⑨)에만 걸립니다. P0 경로는 LLM 없이 완주합니다.
- 임베딩 모델 · `TAG_MATCH_MIN_SIMILARITY` · numpy는 **불필요**합니다 (E-16 폐기).
>
> ⚠️ **금지 표현:** 어떤 문서·시트·발표자료에도 "AI가 둘러댄다 / 알아서 설명한다" 류의 서술을 쓰지 않습니다.
> AI의 모든 설명은 규칙 엔진 산출값(`reasonCode`·집계 수치)의 **재구성**입니다 (NFR-02).

---

## 3. 규칙 엔진 파라미터

| # | 항목 | 확정 | 비고 |
|---|---|---|---|
| B-1 | 회고 시점 | **D+1** | 사용자 조정 필드(`retrospectDelayDays`) 유지 |
| B-2 | 점수 체계 | 만족(`HIGH`) +1 / 별로(`LOW`) −1 / 잘 모르겠어요(`UNKNOWN`) **제외** — 3택, 중간값 없음 (E-23) | 행동 평균 범위 [−1, +1] |
| B-4 | 심야 경계 | 22시 ~ 05시 | 4구간 중 `NIGHT` (v1.9 — E-50) |
| B-5 | 시간대 구간 | **4개** (v1.9 — E-50) | `MORNING` 05~11 · `DAY` 11~17 · `EVENING` 17~22 · `NIGHT` 22~05. `AFTERNOON` 폐기 |
| B-7 | 1회 회고 건수 | **경로별** (v1.9 — E-48) | 알림 1일 1건 · 채팅 최근 3일 전부 · 날짜 지정·직접 선택은 제한 없음. 온보딩은 예외 (연속 회고) |
| B-8 | 선별 조건 | 시간대별 이상치 + **사용자 설정 임계값** | 온보딩 입력 항목 |
| B-9 | 온보딩 표본 | **20건 내외, 26.06~07에서 추출** | 26.08은 전월 비교용 보존 |
| B-10 | 묶음 키의 시간대 | **식사 카테고리에만 적용** | 그 외 카테고리는 시간대 자리 공백. **v2.2 보강(E-58):** 식사 목록은 `rules.cluster.meal-categories`, `기타`(미분류)도 시간대 포함 |
| B-11 | 부담 산정 기간 | **분석 기준월 1개월** | v1.2 확정 — E-1 부속 |

**미결:** B-3 보정 강도 `k` / B-6 롤업 기준 건수 → 데이터 확보 후 (9/4 이후, 액션시트 #15·#16). **v2.2:** 잠정값 `k=3` · 롤업 3 · 보류 3 · `Bx=0.1` · `By=0`을 yml 기본값으로 주입 (E-57). 상위 키는 `카테고리|시간대||` (E-59)

### 보정 만족도 산식

```
보정 만족도 = (행동 회고 수 × 행동 평균 + k × 사용자 전체 평균)
              ÷ (행동 회고 수 + k)
```

축소 추정(shrinkage estimation). 회고가 적을수록 전체 평균을, 쌓일수록 행동 평균을 반영합니다.

### 회고 입력 선택지

| 항목 | 선택지 |
|---|---|
| 목적 | 식사 / 만남·사교 / 휴식·취미 / 필수품 / 자기계발 / 충동 / 기타 |
| 동행인 | 혼자 / 친구 / 가족 / 연인 / 동료 / 기타 |

---

## 4. 만족도 지도

| # | 항목 | 확정 |
|---|---|---|
| C-1 | 가로축 (지출 부담) | **분석 기준월 1개월 묶음 합계 금액 ÷ 사용자 입력 월 예산** (v1.2 — E-1) |
| C-2 | 세로축 | 보정 만족도 (범위 [−1, +1]) |
| C-3 | 라벨 체계 | **사분면 4코드 / 판정 2라벨 + 보류 상태 / 처방 문구 5종** |

> **C-1 해설:** 평균 단가가 아니라 **한 달 동안 그 행동에 쓴 총액**이 예산의 몇 %인가로 봅니다.
> "심야 배달에 예산의 12%를 쓰고 있다"가 부담의 정의입니다.
> 평균 단가(`avgAmount`)는 **절감액 계산(⑦)에만** 쓰며, 상세 패널에 `평균 12,000원 × 8회` 형태로 병기합니다.

### 사분면 · 라벨 · 처방 문구 (확정)

| 사분면 | 부담 | 만족 | 사분면 코드 | 판정 (`verdict`) | 처방 문구 |
|---|---|---|---|---|---|
| Ⅰ | 높음 | 높음 | `PROTECT` | 지켜요 (`SUSTAIN`) | **Great!** 이건 지킬 가치가 있어요. 예산을 미리 확보해둘까요? |
| Ⅱ | 낮음 | 높음 | `KEEP` | 지켜요 (`SUSTAIN`) | **Awesome!!** 부담도 적고 만족도 높아요. 이대로 두셔도 좋아요. |
| Ⅲ | 낮음 | 낮음 | `MINOR` | 바꿔볼까요 (`ADJUST`) | **Umm...** 조금 아쉬웠던 소비네요. 여력이 될 때 조정해도 괜찮아요. |
| Ⅳ | 높음 | 낮음 | `PRIORITY` | 바꿔볼까요 (`ADJUST`) | **Hmm!** 여기부터 볼까요? 부담은 큰데 만족은 낮았던 소비예요. |
| — (보류) | — | — | `null` | `null` — 더 알아볼게요 | 아직 판단하기엔 이르네요. 조금 더 지켜볼게요. |

### E-11 — 왜 4가 3이 아니라 2가 되는가 (v1.2 핵심 정정)

사분면과 판정은 **다른 종류**입니다.

| 층위 | 정체 | 종수 | 결정 요인 |
|---|---|---|---|
| 사분면 `quadrant` | **좌표** — 지도 위 위치 | 4 | 가로축 × 세로축 (연속값) |
| 판정 `verdict` | **처방** — 사용자가 할 일 | **2** | **세로축(만족도) 부호만** |
| 우선순위 | 무엇부터 볼지 | (정렬) | **가로축(부담)** — 지도상 오른쪽일수록 먼저 |
| 평가 상태 `evaluationStatus` | 판정 가능 여부 | 2 | 회고 건수 ≥ 보류 임계값 |

v1.1의 '판정 3라벨'은 **판정 2종에 보류 상태 1종이 섞인** 것이었습니다.
`더 알아볼게요`는 판정이 아니라 "아직 판정할 수 없음"이므로, 별도 필드로 분리합니다.

```
evaluationStatus = PENDING  → quadrant = null, verdict = null   (회색 반투명 점)
evaluationStatus = RESOLVED → quadrant ∈ {PROTECT,KEEP,MINOR,PRIORITY}
                              verdict  = 만족도 ≥ 경계 ? SUSTAIN : ADJUST
```

> `verdict`는 세로축만으로 정해지고 가로축은 관여하지 않습니다. 이는 결함이 아니라 설계입니다 —
> **부담은 판정이 아니라 순서를 정하며, 그 순서는 이미 점의 가로 위치로 보입니다.** 색을 하나 더 쓸 이유가 없습니다.

**표시 구조 (E-4·E-11 — 층위 분리)**

| 층위 | 종수 | 표시 위치 |
|---|---|---|
| 사분면 `quadrant` | 4 | 화면 노출 안 함 (API·DB·정렬 전용) |
| 판정 `verdict` | **2** | 점 색상 · 배지 — 지켜요 / 바꿔볼까요 |
| 평가 상태 `evaluationStatus` | 2 | `PENDING`은 **회색 반투명 점** (색상 팔레트가 아닌 상태 표현) |
| 처방 문구 `prescription` | **5** | 상세 패널 전용 |
| 회고 건수 | — | 괄호 안 숫자 |

> 화면에서 사용자가 보는 구분은 여전히 셋(지켜요 / 바꿔볼까요 / 회색)이지만,
> **색상 팔레트는 2색**이고 세 번째는 상태 표현입니다. 심사에서 "왜 셋입니까"를 한 줄로 답할 수 있습니다.

### E-12 — `PROTECT`의 행동 비대칭

`PROTECT`(Ⅰ)는 "예산을 미리 확보"라는 **할 일이 있고**, `KEEP`(Ⅱ)는 없습니다.
둘 다 배지는 `지켜요`로 동일하게 두되, **상세 패널에서만 구분**합니다.

| 코드 | 배지 | 상세 패널 |
|---|---|---|
| `PROTECT` | 지켜요 | Great! 문구 + **`예산 확보하기` CTA** (목표 배분 화면으로 연결) |
| `KEEP` | 지켜요 | Awesome!! 문구 — CTA 없음 |

> 지도는 조용하게 두고 행동은 살립니다. 조정 제안 목록(FR-08-01)에는 넣지 않습니다 —
> 그 목록은 '줄일 것'의 목록이며, 여기에 '지킬 것'을 섞으면 목록의 의미가 흐려집니다.

> ⚠️ **Great/Awesome/Umm/Hmm을 점 색상이나 배지에 쓰지 않습니다.** 4색 배지가 되면 층위 설계가 무너집니다.
> ⚠️ **`Out`은 사용하지 않습니다** (§7 판결 어휘 금지).

**미결:** 축 경계선 / 보류 임계값 → 데이터 확보 후 (액션시트 #17·#18)

---

## 5. 데이터

| # | 항목 | 확정 |
|---|---|---|
| D-1 | 지원 카드사 | 국민 · 하나 · 신한 |
| D-2 | 필수 컬럼 | 거래일시 / 가맹점명 / 금액 / 카테고리 |
| D-3 | 날짜 포맷 | `2026.08.25 20:22(:30)` — 초 단위 선택적 |
| D-5 | 시드 데이터 | **가맹점·금액 치환한 익명화 데이터셋** |
| D-6 | 데모 기간 | 26.06 ~ 26.08 (3개월) |
| D-7 | 대표 사례 단가 | **심야 배달 12,000원** (v1.2 — E-10) |

> **D-7:** 제출 기획서의 23,000원은 데모 데이터셋과 맞지 않습니다.
> 모든 문서·API 예시·발표 슬라이드를 **12,000원**으로 통일하고,
> 발표 시 "데모 데이터 기준 재산출" 한 줄로 정리합니다.

### 익명화 처리 방침

| 항목 | 처리 |
|---|---|
| 가맹점명 | 실명 → 가명 (`○○치킨`, `A커피`) |
| 금액 | ±10~20% 랜덤 스케일 (분포 유지) |
| 거래일시 | **원본 유지** — 시간대 분포가 핵심 |
| 카테고리 | 원본 유지 |
| 민감 업종 | 병원·약국 등 제외 또는 일반 카테고리로 치환 |
| 사용 범위 | 실데이터 = 파서 검증용 / 화면 노출 = 익명화본만 |

---

## 6. 규제 경계

- **허용:** 저축 · 정책 상품 조건 매칭
- **제외:** 원금 손실 위험이 있는 투자 상품
- 근거: 유사투자자문 규제 경계 준수
- 발표 시 반드시 **의도적 준수**임을 명시 (기능 축소로 오독됨)

---

## 7. 서비스 톤 원칙

- AI 자연어 응답은 **해요체**를 사용합니다. 반말·격식체·이모지는 쓰지 않습니다 (E-90)
- 답변은 **한두 문장의 한 문단**으로 짧게 작성하며 여러 문단으로 나누지 않습니다 (E-90)
- '후회' 언어를 쓰지 않습니다 (E-90)
- 지킬 만한 소비가 있으면 그것부터 먼저 말합니다 (E-90)
- **판결 어휘(탈락 · 아웃 · `Out` 등)를 어떤 산출물에도 쓰지 않습니다** — 문서·시트·피그마·발표자료 전부 (E-90)
- AI의 설명은 반드시 **규칙 엔진이 반환한 근거(`reasonCode`·집계 수치)를 재구성**한 것이어야 합니다 — 근거 없는 임의 생성 금지 (NFR-02)

---

## 8. 담당 확정

| 역할 | 담당 |
|---|---|
| 팀장 | **오진호** |
| 통합 담당 (Integration Owner, Spring↔FastAPI) | **고현석** |
| 익명화 스크립트 | **정민규** |
| LLM 장애 대체 템플릿 문구 | **오진호** |
| 3사 CSV 헤더 확보 | **석정한** — ⚠️ 실질 기한 **9/2** (9/3 파서 착수) |
| 프론트엔드 | **석정한** (일부 문서의 '김정한'은 오기) |
| 화면 · IA | **석예림** |
| 규칙 엔진 (Spring · Java) | **정민규** — 구현 / **고현석** — 통합·리뷰 (v1.3 — E-18) |
| AI 레포 `sottaejap-ai` (Agent · 폴백 · RAG) | **오진호** (v1.3) |
| 금융 문서 적재 (FR-12 · P2) | **오진호** (⚠️ 잠정 — 문서 소스 확정 필요) |

### 발표·시연 담당 (v1.2 확정)

| 역할 | 확정 |
|---|---|
| 발표 자료 | **전원 공동 — 본선 현장에서 함께 제작** |
| 데모 시연 | **본선 현장에서 결정** |

> ⚠️ **리스크:** 심사표 필수 항목인 '사회적 파급효과'가 현재 0건입니다.
> 현장 30시간 안에서 처음 쓰기 시작하면 개발 시간을 그만큼 잃습니다.
> **최소한 사회적 파급효과 1장만은 9/7 이전에 초안을 만들어 두십시오** (액션시트 §발표 자료).

---

## 9. 화면 · 네비게이션 (v1.2 — 번호 체계 재정비, E-7)

| 항목 | 확정 |
|---|---|
| 하단 탭 | **4탭 — 홈 · 채팅 · 지도 · 거래내역** |
| 설정 진입 | 우측 상단 (마이페이지 · 알림) |
| 최초 진입 | 온보딩 미완료 → 온보딩 / 완료 → 홈 |
| 온보딩 완료 판정 | **4단계**(목표 자금 → 월 예산 + 분석 민감도 → 거래내역 업로드 → 표본 회고) 완료 시 `onboardingCompleted = true` (E-45 — v1.7) |
| 알림 | **인앱 알림 P1 포함** / 푸시(앱 외부)는 범위 외 유지 |
| 로그인 | **카카오 OAuth 1종 실구현** (E-9·E-15). 데모 단일 계정 폴백 유지. 흐름은 E-55 (v2.1) |
| 아이콘 | **Tabler Icons** 단일 세트 (E-43 — v1.7). 이모지·타 세트 혼용 금지 |
| 서체 | **Pretendard** (E-44 — v1.7) |

### 번호 체계 규칙 (v1.2 확정 — 이것이 유일한 정본)

```
1  초기 화면          1L. 로그인/회원가입
2  메인 페이지         2H. 홈  →  2-1. 온보딩(첫 접속 1회 팝업)
3  하단 탭 바          3-1 홈 · 3-2 채팅 · 3-3 지도 · 3-4 거래내역
4  상단 내비게이션      4MP. 마이페이지 · 4A. 알림
```

- **2-1은 온보딩 전용**입니다. 홈 결과 리포트는 `3-1`입니다 (구 2-1 중복 해소).
- **3-3은 만족도 지도**입니다 (구 결번 해소).
- **채팅 하위는 `3-2-x`** 입니다 (구 `4-0~4-3` 폐기 — depth 4는 상단 내비 전용).

| 구 번호(피그마 v1.1) | 신 번호(v1.2) | 화면 |
|---|---|---|
| 2-1 (홈) | **3-1** | 홈 결과 리포트 |
| 3-1 | **3-2** | AI 에이전트와 채팅 |
| 4-0 | **3-2-1** | 초기 메뉴 선택 |
| 4-1 | **3-2-2** | 회고 |
| 4-2 | **3-2-3** | 행동 제안 |
| 4-3 | **3-2-4** | 소비 분석 (FR-11 — E-8) |
| 3-2 | **3-3** | 만족도 지도 |
| 3-4 | **3-4** | 거래내역 (유지) |
| 2-1 (온보딩) | **2-1** | 온보딩 (유지) |

---

## 10. 비기능 잠정치 (⚠️ 잠정 — 9/7 튜닝 시 재확인)

| 항목 | 잠정치 |
|---|---|
| 업로드 파싱 | 3개월분(약 1,000건) 10초 이내 |
| LLM 폴백 | AI 서버 안에서 6초 초과 시 템플릿 전환, 재시도 1회 (E-88) |
| Spring → AI `/chat` 타임아웃 | 15초 (AI 내부의 LLM 6초 + Spring 조회 왕복 포함 — E-88, 05 §3) |
| 클라이언트 | PWA — Chrome 최신 기준, 설치형 진입 지원 (v1.2 — E-9) |
