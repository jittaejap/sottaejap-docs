# AGENTS.md

이 저장소에서 코딩 에이전트(Claude Code · Codex)가 먼저 읽는 파일입니다. 이 저장소는 소때잡
기획·설계 문서 7종의 **정본**이고, 코드 저장소 3개(`sottaejap-client` · `sottaejap-server` ·
`sottaejap-ai`)가 이 문서를 **파일명과 § 번호로** 참조합니다. 여기에는 **모르면 반드시 틀리는
전제**만 적습니다. 규칙의 본문은 문서 안에 있습니다.

## 문서 7종과 우선순위

| 파일                       | 내용                                              | 담당               |
| -------------------------- | ------------------------------------------------- | ------------------ |
| `01_결정로그.md`           | **단일 진실 공급원.** 값이 충돌하면 이 문서가 우선 | 팀 전체            |
| `02_요구사항_정의서.md`    | FR/NFR 번호 · 우선순위 P0~P2 · 회고 표준 태그     |                    |
| `03_IA_화면정의서.md`      | 화면 번호 S·탭 체계 · 표시 규칙 · 피그마 반영 지시 | 석예림             |
| `04_데이터모델_ERD.md`     | 엔티티 · 파생값 산식 · CSV 파싱                    | 고현석             |
| `05_API_명세서.md`         | **계약.** 경로 · DTO · enum · 오류 코드 · AI 연동  |                    |
| `06_액션시트.md`           | 미결 항목 · 담당 · 마감                            |                    |
| `07_기술스택_레포구성.md`  | 스택 버전 · 폴더 구조 · 환경 변수 · OS 규칙 · 브랜치·PR 규칙 | 고현석 · 오진호 |

## 파일명 · 섹션 번호를 바꾸지 않는다

- 코드 저장소 3개의 `AGENTS.md` · `CONTRIBUTING.md` · 소스 주석이 `sottaejap-docs/05_API_명세서.md` §3
  같은 **경로 + § 번호**로 이 문서를 가리킵니다. 파일명 · 번호 접두사 · 기존 § 번호를 바꾸면 세 저장소의
  안내가 한꺼번에 깨집니다.
- 섹션은 **끝에 추가**하거나 `§5-2`처럼 하위 번호로 끼워 넣습니다. 기존 번호를 밀어내지 않습니다.
- 파일을 나누거나 합치지 않습니다. 필요하면 01에 결정을 적고 세 저장소의 참조를 같은 라운드에 고칩니다.

## 값을 바꾸면 01에 먼저 적는다

- 수치 · enum · 경로 · 담당 · 마감처럼 **다른 문서가 인용하는 값**은 `01_결정로그.md`에 `E-번호`로 먼저
  적습니다. 번호는 마지막 번호 + 1이고, 다른 문서는 그 번호를 `(E-46)`처럼 인용만 합니다.
- 문서를 고치면 머리의 **버전 · 기준일**을 올리고, 머리 아래 인용 블록에 `**vX.Y 변경 (날짜):**` 한 줄
  요약을 **위에** 추가합니다. 라운드 번호 vX.Y는 `01_결정로그.md` §0의 최신 번호를 따릅니다.
- 01 §0에는 라운드마다 `## 0. vX.Y 변경 요약` 블록을 새로 만들고 기존 블록은 `0-0` · `0-1`처럼 밀어 내립니다.

## 계약 변경은 05가 먼저다 (07 §6)

enum · DTO · 내부 엔드포인트가 바뀌면 코드 저장소 3개가 동시에 깨집니다. `05_API_명세서.md`를 먼저
고치고, 팀 채널에 `[계약변경] ...` 한 줄을 올리고, 통합 담당(고현석)이 세 저장소 반영을 확인합니다.
공유 enum 문자열 목록은 07 §6이고 세 저장소와 **글자 단위로** 같아야 합니다.

## 코드가 앞서면 문서가 따라간다 ("레포 우선" · 06 v1.3)

- 문서와 코드가 다르면 지어내지 않습니다. 어느 저장소의 어느 커밋(해시)이 다른지 적고, 팀이 정한
  대로 문서를 코드에 맞춥니다. 예: 04의 스키마는 server `V1__init.sql`이 소유합니다.
- 문서가 양보하지 않는 항목은 06에 `R-번호` 레포 조치로 넘깁니다.

## 올리지 않는 것

- `*.xlsx` · `*.docx`는 `.gitignore`에 있습니다. md가 정본이고 오피스 파일은 md에서 만든 산출물입니다.
- 비밀값의 실제 값을 적지 않습니다. 07 §3의 환경 변수는 빈 자리표시자만 둡니다.

## 작업 흐름 (07 §9)

- 코드 저장소와 같습니다. `Issue → 작업 브랜치 → main 대상 PR → 리뷰 → Squash and merge`.
  본선 30시간 동안의 `main` 직접 push 예외도 같습니다.
- 한 PR은 한 라운드의 변경입니다. 여러 문서를 함께 고쳐도 되지만, 라운드가 다른 변경을 섞지 않습니다.
- 줄바꿈은 `.gitattributes`가 LF로 강제합니다. 커밋 메시지에 `Co-Authored-By` 트레일러를 넣지 않습니다.

## 완료 보고

작업을 끝내면 **바꾼 문서와 § · 새로 채번한 E-번호 · 코드 저장소 3개에 영향이 가는 항목 · 확인하지
못한 것**을 구분해 적습니다. "문서 갱신 완료"로 뭉뚱그리지 않습니다.
